# Phase 1 Handoff: Mechanize the heartbeat inside update-phase-status.sh

**Immediate Next Action**: Begin Phase 2 (pid liveness in task-lock.sh — `write_holder`,
`cmd_reap`, `cmd_acquire`'s stale-override).

**Current State**: Phase 1 COMPLETED and committed (commit `0bcc8b65d`). Plan and progress file
are current. `agent-system/extensions/core/scripts/update-phase-status.sh` now derives
`session_id` from `.lock/holder.json` and fires both `task-lock.sh heartbeat` and
`session-heartbeat` on every phase transition, tracing every outcome to
`.agent-logs/heartbeat-trace.log`, never touching stdout/exit code. Verified with manual fixture
smoke tests (no-lock, real-lock advance, 5th-arg mismatch, idempotency-path fire, phase-not-found
fire, PHASE_HEARTBEAT_DISABLE opt-out) — all behaved per plan. Four named regression suites
(`test-update-task-status.sh`, `test-postflight-deploy-gate.sh`, `test-skill-base-lifecycle.sh`,
`test-phase-heading-patterns.sh`) pass with no new failures.

**Key Decisions Made**: None beyond the plan's own design (session_id derived, not threaded).
Combined stdout+stderr capture for `task-lock.sh heartbeat`/`session-heartbeat` calls in a single
subprocess invocation (rather than two separate invocations) since neither subcommand writes to
stdout on any path — verified by reading their bodies.

**Deviations from Plan**: None.

**What NOT to Try**: Do not deploy the source-store changes to `.claude/` yet — deploy is
scoped to Phase 7 (`agent-system/extensions/core/scripts/tests/*` still run against the deployed
tree, which is intentionally still the pre-mechanization script until Phase 7's deploy step).

**References**: Plan at
`specs/081_mechanize_task_lock_and_session_heartbeat_refresh/plans/01_mechanize-heartbeat-refresh.md`,
current phase 2.
