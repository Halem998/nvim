{
  "status": "planned",
  "summary": "Wrote a 7-phase RECORDING plan (not a repair plan) honoring the capstone's own 'fixes nothing structural itself' mandate. GATE VERDICT: FAIL. DEPLOY -- 4 of 6 sub-items PASS; 'byte-identical twice' FAILS in three forms including a content-lossy settings.local.json merge that silently dropped a hooks.PreToolUse block and an mcpServers block between two identical --wipe runs; 'declared-vs-deployed parity' is CONDITIONAL (passes one-directionally as implemented, fails bidirectionally -- 4 orphan files). GATES -- 3 of 4 PASS at hardened defaults; run-all.sh green FAILS (2 source-store / 7 deployed failures); verify-deploy.sh 22/23. LIVE CYCLE -- BLOCKED, not merely unrun: next_project_number is 1007, and hooks/validate-handoff-location.sh's fixed-position [0-9]{3} regex means every 4-digit task's handoff write both exits 2 and emits a spurious HANDOFF_MISLOCATED system_defect, which structurally defeats acceptance sub-item 3's 'NO system_defect event on the clean run' negative test. Separately, the 'deferred-defect surface renders empty' clause already fails on current state (3 pre-existing system_defect events in specs/events.jsonl dated 2026-08-08). UNVERIFIABLE-AS-WRITTEN: 'gate-out reports zero auto-repaired fields' has no reporting surface at all -- command-gate-out.sh (134 lines) has no counter or exit-code effect; the count exists only in validate-artifact.sh's '[FIXED] N field(s) auto-repaired' line, reached via skill_validate_task_artifacts calling it as '--fix 2>/dev/null' and collapsing every non-zero exit into one generic non-blocking WARNING. That gap is recorded as a fifth new defect. The plan appends 5 new errors.json entries via errors-append.sh (never duplicating the 5 already-recorded ids), decides spawn/entry-only dispositions for all 10 confirmed defects, and writes the dated specs/reviews/ closing bookend plus a mechanical re-run procedure.",
  "artifacts": [
    {
      "type": "plan",
      "path": "specs/996_capstone_end_to_end_refactor_verification/plans/01_capstone-gate-recording.md",
      "summary": "7-phase recording-and-follow-up plan with an explicit per-scope verdict table, a declared out-of-directory write scope (specs/errors.json, specs/reviews/, specs/state.json+TODO.md only), and a hard no-fix boundary excluding agent-system/**, .claude/**, lua/**, and .opencode/**."
    }
  ],
  "phases_completed": 0,
  "phases_total": 7,
  "blockers": [
    "LIVE CYCLE scope (capstone verification scope item 3) cannot be exercised cleanly until err_1786349061492_XpY38x lands: next_project_number is 1007, so any scratch task is 4-digit and its handoff write falsely trips validate-handoff-location.sh, emitting a spurious HANDOFF_MISLOCATED system_defect that makes the clean-run negative test structurally unpassable. This is a blocker on the GATE's completeness, not on this plan's execution -- Phase 2 records the blocking condition rather than attempting the cycle.",
    "Acceptance sub-item 'gate-out reports zero format errors and zero auto-repaired fields' is unverifiable as written; no instrumentation exists to assert against. Phase 3 evidences it and Phase 4 records it; resolving it requires either instrumenting the counter or amending the criterion, both out of this task's scope."
  ],
  "next_action_hint": "Run /implement 996. Phase 4 (5 errors.json appends) is the primary deliverable and Phase 6 (specs/reviews/review-2026-08-10-agent-system-refactor-capstone.md) satisfies scope item 4. Phases 1, 2, and 3 are independent and can run in parallel as wave 1. Watch two things: (a) Phase 1's settings.local.json reproduction is explicitly allowed to come back '0 of N' -- recording must proceed either way with the rate stated, never blocked on reproduction; (b) the implementer must not fix anything -- any diff under agent-system/**, .claude/**, lua/**, or .opencode/** is a scope violation to revert before close. Task 996's file_scope is null in state.json; the plan declares its four out-of-directory write targets explicitly, all under specs/**."
}
