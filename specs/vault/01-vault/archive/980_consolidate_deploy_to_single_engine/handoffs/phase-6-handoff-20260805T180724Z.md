# Handoff: Task #980, Phase 6 (in progress)

## Immediate Next Action

Continue Phase 6 (`### Phase 6: Consolidate entry points; retire the second engine [IN PROGRESS]`
in `specs/980_consolidate_deploy_to_single_engine/plans/01_consolidate-deploy-engines.md`).
Remaining sub-tasks, in a sensible order:

1. Add the `[Regenerate]` special picker entry:
   - Producer: `lua/neotex/plugins/ai/claude/commands/picker/display/entries.lua`, alongside the
     existing `is_help = true` (~line 962) and `is_reload_all = true` (~line 976) special
     entries. Add a new entry with e.g. `is_regenerate = true`.
   - Handler: `lua/neotex/plugins/ai/claude/commands/picker/init.lua`. The dead `is_load_all`
     handler block (~lines 108-121, `if selection.value.is_load_all then ... end`) is the
     natural site to replace — but do NOT simply repurpose it in place; read the whole block
     first, since it currently calls `sync.load_all_globally(config)` directly and has an
     `on_load_all` post-hook call that may still matter for OpenCode (`config.on_load_all`,
     defined in `shared/picker/config.lua`'s `M.opencode` preset — installs `opencode.json` /
     runs `npm install`). Decide whether `[Regenerate]` needs an equivalent post-hook call or
     whether `manager.regenerate`/`manager.wipe`'s own return value supersedes it.
   - The new handler should call `manager.wipe` (via `exts.wipe`, since
     `neotex.plugins.ai.claude.extensions`'s `init.lua` just re-exports the `manager` instance —
     see Phase 3's handoff precedent for `exts.resync_all`) behind an explicit confirmation
     dialog (`vim.fn.confirm`), since `manager.wipe` is destructive. Report success/failure via
     `helpers.notify`, similar to the "Reload All" rewiring done in Phase 3.

2. Remove the 6 dead `is_load_all` consumer sites (NOT 5 — see the plan's "Scope Hypothesis
   reconciliation" note in Phase 6 for the corrected count and exact locations):
   - `picker/init.lua`: the Enter-key handler (~line 111-121) and 4 keymap guard conditions
     (~lines 353, 377, 401, 425 — each currently reads
     `selection.value.is_help or selection.value.is_load_all or selection.value.is_reload_all or selection.value.is_heading`;
     remove just the `is_load_all` clause from each, unless the new `[Regenerate]` entry should
     ALSO be excluded from those same keymaps, e.g. edit/delete — decide per-guard).
   - `picker/display/previewer.lua` (~line 751): `elseif entry.value.is_load_all then ...` — a
     6th site not named in the plan's "an Enter-key handler and four keymap guards" prose, found
     by grep. Confirmed `entries.lua` has ZERO `is_load_all` producer (dead code, safe to
     delete). If the new `[Regenerate]` entry needs a preview, add one here in the same style.

3. Remove `sync.load_all_globally` and its 5 EXCLUSIVELY-owned module-local helpers from
   `picker/operations/sync.lua`: `execute_sync`, `count_actions`, `audit_synced_content`,
   `reinject_loaded_extensions`, `run_contract_drift_validator` (all confirmed via grep to have
   no caller outside `load_all_globally`).
   **Do NOT delete `M.scan_all_artifacts` or its allow-list post-filter** without a SEPARATE,
   deliberate decision — see the plan's "`scan_all_artifacts` non-exclusivity discovery" note.
   It is used by `previewer.lua:168` (which step 2 above is about to make dead too, once the
   `is_load_all` preview branch is removed) and by 5 test cases in
   `picker/operations/sync_spec.lua`. Once step 2 removes its other live caller, decide: keep
   `scan_all_artifacts` as a tested-but-currently-uncalled utility (fine), or remove it and
   rewrite/delete the 5 `sync_spec.lua` cases (more invasive, not yet scoped).

4. Re-run `grep -rn 'load_all_globally\|is_load_all' lua/` — must return nothing (per the plan's
   own Verification bullet) once steps 2-3 land.

5. Verify: picker opens without error (`nvim --headless -c "lua require(...)"` smoke test at
   minimum; ideally exercise the picker's entry list construction directly). `[Regenerate]` is
   reachable and, behind confirmation, performs a full wipe+regenerate round trip matching
   Phase 5's survival checks (settings.local.json, `.syncprotect` paths, merge-source hooks).

6. Once ALL of Phase 6's `Commit Mode: atomic-batch` file set is green together (deploy-
   headless.sh — already done — plus sync.lua, init.lua, entries.lua, previewer.lua), mark the
   phase `[COMPLETED]` via `update-phase-status.sh` and make the phase-level commit. Redeploy
   this repo's own `.claude/` again afterward (same sanctioned mechanism used this dispatch) so
   the picker changes are live, and re-run `test-deploy-propagation.sh` + `verify-deploy.sh` as
   a final regression check before moving to Phase 7.

Then proceed to **Phase 7** (`### Phase 7: Full-category verification with content-hash parity
[NOT STARTED]`, depends on 5 — already satisfied): extend `verify.lua` to the 6 uncovered
categories with manifest-driven parity + content-hash equality, routed through
`verify-deploy.sh`'s existing `FINDINGS_LIST` accumulator (do not modify that script's gate
structure/exit codes). Then **Phase 8** (prose sweep — mis-diagnosis in
`no-task-references-in-deliverables.md`, `regeneration-is-manual-only.md` rewrite,
`deploy-root-guard.sh`, `check-extension-docs.sh` advisory strings, plus a confirmed lower-
priority sweep set). Then **Phase 9** (execute the full union verification bar end to end and
record evidence in the implementation summary).

## Current State

- Phases 1-5 fully `[COMPLETED]` with real empirical verification (see each phase's notes in the
  plan file and its `progress/phase-{N}-progress.json`). Phase 6 `[IN PROGRESS]`, roughly half
  done (the `deploy-headless.sh` rewrite — the highest-risk, highest-value piece — is complete
  and verified; the picker-side entry-point consolidation is not started).
- **The core defect this entire task exists to fix is DEMONSTRABLY CLOSED**: the Phase 1
  regression harness (`test-deploy-propagation.sh`) now reports `4 passed, 0 failed` (was
  `1 passed, 3 failed` at Phase 1 authorship). A from-scratch scratch git repo, deployed via the
  now-fixed `deploy-headless.sh` with no flags, correctly receives all `scripts/lib/*.sh`
  subdirectory-declared entries — the exact class of file the old `load_all_globally` engine
  silently dropped.
- This repository's own `.claude/` has been redeployed via the fixed `deploy-headless.sh`
  (sanctioned use of the deploy mechanism itself, not a hand-edit — see
  `.claude/rules/source-store-deploy-boundary.md`). `verify-deploy.sh` reports 14/15 passing; the
  1 failure is pre-existing and unrelated (confirmed via `git log`).
- Git checkpoint: this handoff is written immediately before a commit of all Phase 6 work done
  so far (`agent-system/extensions/core/scripts/deploy-headless.sh`,
  `agent-system/extensions/core/manifest.json`,
  `agent-system/extensions/core/scripts/tests/test-deploy-propagation.sh`'s cosmetic regex fix,
  the plan file, and this handoff/progress file). The commit message is
  `task 980 phase 6: deploy-headless.sh rewrite (bootstrap-safe default + --wipe); manifest gap fix`.
  Working tree also has an UNRELATED pre-existing dirty file,
  `lua/neotex/plugins/editor/which-key.lua` (present in git status before this dispatch began,
  per the session's initial git status) — this commit deliberately excludes it via targeted
  staging; do not sweep it in on a future commit for this task.

## Key Decisions Made

1. **Bootstrap-safety fix to the plan's literal Phase 6 wording** (see plan's "Bootstrap-safety
   deviation" note): default invocation is `manager.load('core', {force=true})` then
   `manager.resync_all`, not a bare `resync_all`. This was necessary, not optional — a bare
   `resync_all` breaks the primary real-world use case (first deploy into any repo).
2. **`scan_all_artifacts` is not exclusive to `load_all_globally`** (previewer.lua + a spec test
   file also depend on it) — the sync.lua removal task is narrower than the plan assumed (delete
   `load_all_globally` + 5 exclusively-owned helpers; `scan_all_artifacts` and `load_syncprotect`
   need a separate, deliberate decision, not automatic deletion).
3. **`is_load_all` site count is 6, not the hypothesized 5** — a `previewer.lua` site the plan's
   prose didn't name. Confirmed zero producer (dead code) either way.
4. `settings_backup.backup`'s underlying `helpers.ensure_directory` can throw rather than return
   `false` on some failures (Phase 5 discovery) — `manager.wipe` now pcall-wraps it so a snapshot
   failure is always a clean refusal, never an uncaught Lua error. This hardening is already
   landed (Phase 5, committed).

## What NOT to Try

- Do not implement `deploy-headless.sh`'s default as a bare `manager.resync_all` call — see the
  bootstrap-safety deviation above. This was tested and empirically confirmed to break fresh
  deploys (0 files) before the fix.
- Do not hand-edit anything under `.claude/**` directly — it is the disposable, regenerated
  deploy artifact. To refresh it, run
  `bash agent-system/extensions/core/scripts/deploy-headless.sh /home/benjamin/.config/nvim`
  (sanctioned; this dispatch did so twice).
- Do not delete `M.scan_all_artifacts` casually alongside `load_all_globally` — check its other
  callers first (see discovery note above).

## Critical Context

- The Phase 1 harness (`agent-system/extensions/core/scripts/tests/test-deploy-propagation.sh`)
  resolves `deploy-headless.sh` from the DEPLOYED copy (`.claude/scripts/...`) first, falling
  back to the source-store copy. This means editing the source-store script alone is NOT enough
  to make the harness observe a fix — a redeploy of this repo's own `.claude/` is required first
  (as this dispatch did). Remember this when finishing Phase 6's remaining picker-side changes:
  they don't need a redeploy to be correct (nothing in the harness touches the picker), but a
  final redeploy + harness/verify-deploy re-run is still good practice once the phase is fully
  green (see "Immediate Next Action" step 6 above).
- `neotex.plugins.ai.claude.extensions` (the `extensions_module` picker code requires as `exts`)
  is a thin re-export of the `manager` instance from `shared/extensions/init.lua` — any new
  `manager.*` function (e.g. a future `manager.wipe` caller) is automatically available as
  `exts.*` with no separate wrapper needed. Verified for `resync_all` in Phase 3; the same holds
  for `wipe`.

## References

- Plan: `specs/980_consolidate_deploy_to_single_engine/plans/01_consolidate-deploy-engines.md`
  (Phase 6 section has the full reconciliation/discovery notes referenced above)
- Progress files: `specs/980_consolidate_deploy_to_single_engine/progress/phase-{1..6}-progress.json`
- Research report: `specs/980_consolidate_deploy_to_single_engine/reports/01_consolidate-deploy-engines.md`
- Modified so far: `lua/neotex/plugins/ai/shared/extensions/{loader,init,settings_backup}.lua`,
  `lua/neotex/plugins/ai/claude/commands/picker/init.lua` (Phase 3's Reload All rewiring only),
  `agent-system/extensions/core/scripts/deploy-headless.sh`,
  `agent-system/extensions/core/scripts/tests/test-deploy-propagation.sh`,
  `agent-system/extensions/core/manifest.json`
