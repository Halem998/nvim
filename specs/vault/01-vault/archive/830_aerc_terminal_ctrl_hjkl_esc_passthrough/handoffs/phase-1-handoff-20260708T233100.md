# Phase 1 Handoff: Special-case aerc in set_terminal_keymaps()

**Immediate Next Action**: All phases complete — proceed to Stage 5 (final verification, already done) and Stage 6 (summary, already written).

**Current State**: Phase 1 completed. Plan file is current (all Phase 1 checklist items marked `[x]`, phase heading `[COMPLETED]`).

**Key Decisions Made**:
- `is_aerc` computed via `bufname:match("aerc") ~= nil`, mirroring the `is_claude` idiom, placed immediately after the `is_opencode` detection block.
- `<C-hjkl>` pass-through implemented as an intentionally-empty `elseif is_aerc then` branch (comment only, no `buf_map` calls, no early return) so the unconditional `<M-*>` resize binds after the if/elseif/else block still execute for aerc terminals.

**Deviations from Plan**:
- None

**What NOT to Try**:
- Do not use an early `return` in the `is_aerc` branch — it would skip the unconditional `<M-*>` resize binds at the end of the function.

**References**: Plan path `specs/830_aerc_terminal_ctrl_hjkl_esc_passthrough/plans/01_aerc-terminal-passthrough.md`, Phase 1 (only phase).
