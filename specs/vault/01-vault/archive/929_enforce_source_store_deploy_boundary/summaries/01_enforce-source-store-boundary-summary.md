# Implementation Summary: Task #929

**Completed**: 2026-07-27
**Duration**: ~1.5 hours

## Overview

Closed the source-store/deploy-boundary gap by three coordinated changes: registering the
previously-deployed-but-never-wired `validate-meta-write.sh` PostToolUse hook, widening its path
coverage and rewriting its advisory message to name the source store as the correct edit target
(removing a backwards `/implement` exemption), and encoding the rule durably as one core rule
file referenced by a one-line MUST NOT bullet in all 15 implementer-agent contracts (2 core + 13
extension). All edits landed exclusively under `agent-system/extensions/**`; no `.claude/**` path
was created or modified.

## What Changed

- `agent-system/extensions/core/rules/source-store-deploy-boundary.md` — new canonical rule file
  (Path Pattern, Principle, Correct Edit Target, Exceptions, Enforcement with the known
  hook-advisory limitation stated explicitly).
- `agent-system/extensions/core/manifest.json` — registered the new rule in `provides.rules`.
- `agent-system/extensions/core/merge-sources/claudemd.md` — added the Rules References bullet.
- `agent-system/extensions/core/merge-sources/settings-hooks.json` — added
  `validate-meta-write.sh` as a third command in the existing `Write|Edit` PostToolUse block.
- `agent-system/extensions/core/hooks/validate-meta-write.sh` — added `.claude/scripts/*` and
  `.claude/hooks/*` cases to `is_meta_path`; rewrote the advisory message to state the
  source-store rule and name `agent-system/extensions/<ext>/...` as the target; removed the
  `/implement`/`general-implementation-agent` exemption clause.
- `agent-system/extensions/core/agents/general-implementation-agent.md`,
  `general-implementation-hard-agent.md` — appended a MUST NOT bullet pointing at the new rule.
- `agent-system/extensions/core/agents/meta-builder-agent.md` — collapsed Rule 2's general
  explanation and the standalone "Hook limitation" paragraph into a pointer to the new rule file;
  preserved the agent-specific task-authoring clause inline.
- 13 extension implementer-agent contracts (cslib x3, python, web, latex, lean x2, typst, z3,
  email, nvim, nix) — appended the identical one-line MUST NOT bullet each.
- `agent-system/extensions/core/commands/meta.md` — collapsed a second multi-paragraph prose
  copy of the same rule (found during the Phase 6 anti-copy-paste audit) to a short pointer,
  preserving the `/meta`-specific task-authoring and multi-repo `<leader>al` regeneration detail.

## Decisions

- Followed the plan's decision to widen scope to all 15 implementer-agent contracts (not only
  the 2 under `core/`), since the task's REQUIRED WORK item names "the implementer agent
  contracts" unqualified and `file_scope` is a descriptive, non-binding estimate.
- During the Phase 6 anti-copy-paste audit, found a second full prose copy of the rule in
  `agent-system/extensions/core/commands/meta.md` (pre-existing, unrelated to this task's
  authorship) and collapsed it per the plan's explicit Phase 6 instruction ("If a second full
  copy exists, collapse it to a pointer"), preserving the command-specific detail that has no
  home in the generic rule file.

## Plan Deviations

- None (implementation followed plan). The `meta.md` collapse was not in the plan's enumerated
  `Files to modify` list but is directly mandated by Phase 6's own anti-copy-paste audit task,
  so it is treated as plan-compliant scope discovery rather than a deviation.

## Verification

- Build: N/A (no build system for this change)
- Tests: N/A — verified via `jq .` parse checks, `bash -n` syntax check, a 5-case synthetic-stdin
  matrix against `validate-meta-write.sh` (all exit 0, all JSON-parseable), a `grep -L` exhaustive
  pointer sweep (empty), and an anti-copy-paste `grep -rln 'disposable deploy artifact'`
  audit (surfaces only the rule file and short pointers).
- Files verified: Yes — every phase's verification criteria were re-run and passed.
- `git status --short | grep '\.claude/'` returned empty at every phase checkpoint; the task's
  full commit set touches only `agent-system/extensions/**` and `specs/**` paths.
- `bash .claude/scripts/check-extension-docs.sh` was run; its 2 FAIL-level findings (deployed
  script content drift on `scripts/check-extension-docs.sh` and `scripts/roadmap-integration.sh`)
  pre-date this task and are unrelated to the source-store-deploy-boundary rule, hook, or agent
  contracts — confirmed via `git log` showing those files were last touched by a different,
  concurrently in-flight task. The script's own line `rule not deployed, skipping drift check:
  source-store-deploy-boundary.md` is expected: `.claude/` is regenerated by the loader, not by
  this task, per the plan's Non-Goals.

## Notes

The hook remains strictly advisory (`additionalContext`, `exit 0` on every path, no
`exit 2`/`decision`/`permissionDecision` key); the `specs/*|*/specs/*` skip is unmodified. The
deploy/reload path writes `.claude/` via a Lua file-copy engine
(`lua/neotex/plugins/ai/shared/extensions/loader.lua`), not through the Claude Code Write/Edit
tool, so the widened hook structurally cannot observe it — no carve-out was needed, matching the
plan's risk mitigation.
