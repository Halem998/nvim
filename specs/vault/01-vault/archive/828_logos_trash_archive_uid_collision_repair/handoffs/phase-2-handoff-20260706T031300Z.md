# Phase 2 Handoff — task 828

**Immediate Next Action**: Phase 3 — build the dry-run-capable, rename-only repair script
(`repair-uid-collisions.py`) implementing the MATCHED/UNVERIFIED decision rule.

**Current State**: Phase 2 completed. UIDVALIDITY gate PASSED for both folders. Plan file
current (Phase 2 marked [COMPLETED]).

**Key Decisions Made**:
- Corrected the UIDVALIDITY comparison target: use `FarUidValidity` from each folder's
  `.mbsyncstate` (the value mbsync itself last observed from the server), not the maildir-root
  `.uidvalidity` file (which is `NearUidValidity`, the local side's own value — comparing against
  it would have been a category error and would have produced a false STOP).
- Confirmed: Trash server UIDVALIDITY=95457113 == FarUidValidity=95457113 (match). Archive server
  UIDVALIDITY=95457115 == FarUidValidity=95457115 (match). Gate PASSES — safe to proceed.
- Confirmed 0 Near/Far UID divergence across all 924 (.Trash) + 39 (.Archive) tracked
  `.mbsyncstate` pairs — the local `U=NNN` filename token corresponds directly to server UID
  `NNN`, validating the whole technique's premise.
- IMAP probe (`~/Mail/.logos-backup-20260706/task-828/imap-probe.py`, log at
  `imap-probe.log`) used only LOGIN/LIST/EXAMINE(readonly=True)/UID FETCH BODY.PEEK/LOGOUT.

**Deviations from Plan**: One altered task (see Phase 2 checklist annotation above) — comparison
source corrected from `.uidvalidity` to `.mbsyncstate`'s `FarUidValidity`. This is a correction of
an ambiguous plan reference, not a weakening of the safety gate; the gate was evaluated and it
passed.

**What NOT to Try**: Do not compare server UIDVALIDITY against the maildir-root `.uidvalidity`
file — it holds `NearUidValidity`, a different value entirely, and comparing against it produces
a false-positive mismatch (observed first-hand: 95457113 vs 1770669126 looked like a mismatch
but was comparing the wrong quantities).

**References**: Plan at
`specs/828_logos_trash_archive_uid_collision_repair/plans/01_logos-uid-collision-repair.md`.
Current phase: 3. Probe artifacts under `~/Mail/.logos-backup-20260706/task-828/`.
