# Phase 1 Handoff

**Immediate Next Action**: Begin Phase 2 — reconcile the five-reasons doc claims in
context/patterns/task-lock.md, docs/architecture/batch-admit-schema.md, and
scripts/orchestrate-batch-admit.sh, then redeploy and run the full verification gate.

**Current State**: Phase 1 completed and committed (task-lock.sh ladder fix + docstring, new
test case 5a and extended Case 6 in test-session-registry.sh, new case 4.2b in
test-conflict-predicate.sh). Both suites pass green (11/11 and 24/24). Falsifiability check
performed via `git stash push --keep-index -- task-lock.sh` and confirmed both new assertions
FAIL against the pre-fix ladder. Plan and progress file are current.

**Key Decisions Made**:
- No changes needed to cmd_session_list, cmd_session_reap, or file-scope-overlap.sh — confirmed
  by grep sweep and git diff --stat (three-file scope holds).
- file-scope-overlap.sh's session_contention() D4 exclusion filters on the `.live` boolean, not
  the reason string, so `dead-pid-within-grace` (live:true) automatically contends with zero
  code change — proven empirically by test-conflict-predicate.sh case 4.2b.

**Deviations from Plan**: None.

**What NOT to Try**: Do not attempt to edit cmd_session_list's live_flag case or
cmd_session_reap's reap set — the plan's non-goals explicitly forbid this; wildcard defaults
already produce the correct verdict.

**References**:
- Plan: specs/997_fix_session_liveness_reason_mislabel/plans/01_dead-pid-within-grace-reason.md
- Progress: specs/997_fix_session_liveness_reason_mislabel/progress/phase-1-progress.json
- Current phase: 2 (NOT STARTED)
