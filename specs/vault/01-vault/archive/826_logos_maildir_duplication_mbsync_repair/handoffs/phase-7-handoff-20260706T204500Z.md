# Phase 7 Handoff: Reconcile — Scoped `mbsync logos`

**Status**: BLOCKED (new findings, not the original Phase 5 blocker — that one is resolved)
**Timestamp**: 2026-07-06T20:45:00Z

## Immediate Next Action

Do not run `mbsync logos` (group or any Trash/Archive/Sent scope) until a human/orchestrator
decision is made on the two open questions in `handoffs/phase-7-blocker-report.md`:
(1) scope/timeline for `logos-sent`/`logos-archive`'s ~62,000-message first-ever pull, and
(2) a verified-safe push path for the 862 task-828-renamed messages (quarantine-then-push, or a
single-message pilot APPEND to empirically confirm Bridge's dedup behavior).

## Current State

Task 828's Phase 5 fix re-verified clean this session (0 duplicate UIDs in both folders, freeze
intact, backup intact). Attempted the Phase 7 reconcile per the plan's own dry-run-first
discipline, scoped even narrower than the plan specifies (`logos-trash logos-archive` channels
only, not the full `logos` group) as an extra precaution. The dry-run hard-errored before
proposing any actions ("Maildir error: UID X is beyond highest assigned UID Y" for both
channels) — this is the plan's own anticipated Phase-5-leftover narrow-state-reset trigger.
Before attempting that reset, did read-only IMAP investigation (EXAMINE/UID FETCH/UID SEARCH
only — no writes) to understand what a real reconcile would actually do, and found two
independent reasons to stop rather than proceed:
- `logos-sent`/`logos-archive` are 100-1000x under-synced vs the live server (a scope explosion
  nowhere in this plan).
- Only ~22 of 862 task-828-renamed messages are confirmed to already carry Trash/Archive folder
  membership server-side; the push-safety of the rest is unverifiable via read-only means alone.

No file or state was modified. Full evidence in `handoffs/phase-7-blocker-report.md`.

## Key Decisions Made

- Scoped the exploratory dry-run to `logos-trash logos-archive` (not the `logos` group) as an
  extra safety margin beyond the plan's literal instruction, specifically because Sent/Archive's
  sync status was unknown at that point.
- Built read-only IMAP cross-check tooling (reusing task 828's `secret-tool`/`imaplib` pattern)
  to determine server-side folder-membership disposition for the untracked local UID groups,
  rather than guessing or trusting the "All Mail" aggregate view (which was empirically confirmed
  to be uninformative — it contains ~100% of every local group, control-verified against a
  fabricated Message-Id, because it functions as this account's full canonical message store).

## Deviations from Plan

- Phase 7 task 2 (run scoped `mbsync logos`) was altered to a narrower per-channel dry-run rather
  than the literal group-scoped command, and did not proceed past the dry-run once it hard-errored
  and further investigation revealed new blocking findings. See plan file Phase 7 section for the
  full annotated deviation.

## What NOT to Try

- Do not run `mbsync logos` (the group) blind — it will attempt to pull ~62,000 new Sent/Archive
  messages in addition to whatever Trash/Archive push behavior occurs.
- Do not just delete/reset `.Trash`/`.Archive` `.mbsyncstate`/`.uidvalidity` and push live without
  first resolving what happens to the 862 task-828-renamed messages — their duplicate-push risk
  is unverified, not ruled out.
- Do not trust an "All Mail" or big-Label Message-Id presence check as evidence a message already
  has Trash/Archive folder membership on this account — it does not (verified empirically).

## References

- Plan: `specs/826_logos_maildir_duplication_mbsync_repair/plans/01_logos-mbsync-maildir-repair.md`
- Current phase: 7 of 8 (blocked)
- Blocker report: `handoffs/phase-7-blocker-report.md`
- Backup/evidence dir: `~/Mail/.logos-backup-20260706/task-826-phase7/`
