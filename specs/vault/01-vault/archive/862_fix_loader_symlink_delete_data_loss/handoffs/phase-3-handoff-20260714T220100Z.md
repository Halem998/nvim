# Phase 3 Handoff: Filter .syncprotect at the init.lua call sites

**Immediate Next Action**: Begin Phase 4 — preserve symlinks on the copy path
(`M.copy_simple_files` and `M.copy_skill_dirs` in `loader.lua`), so agents/commands/skills
symlinks stop silently flattening into regular files on every load.

**Current State**: Phase 3 completed. `init.lua`'s `manager.unload` now loads `.syncprotect`,
filters `installed_files`/`data_skeleton_files`/`installed_dirs` before building
`abs_files`/`abs_dirs`, threads `opts.project_dir` into the `remove_installed_files` call, and
reports protected/symlink skip counts in the unload notification. The rollback call site inside
`manager.load` (`init.lua:517` originally) also threads `opts.project_dir` and applies the same
(defense-in-depth) `.syncprotect` filter. Plan file is current (Phase 3 heading `[COMPLETED]`, all
checklist items checked with scratch-test evidence).

**Key Decisions Made**: The rollback-site `.syncprotect` filtering is documented as
defense-in-depth (not the primary mechanism) because `copy_file()` already excludes protected
paths from `all_files`/`all_dirs` under normal operation — the load-bearing part of that call site
is threading `opts.project_dir` for the symlink-ancestor bound.

**Deviations from Plan**: None — implementation followed the plan's Phase 3 tasks exactly.

**What NOT to Try**: Do not derive the `.syncprotect` key by stripping `project_dir` from an
absolute path directly — the correct formula depends on whether the path is project-root-relative
(`rel_path:sub(#config.base_dir + 2)`, used in `manager.unload`) or an absolute deployed path
(`path:sub(#target_dir + 2)`, used at the rollback site) — these differ and both were verified
against `copy_file`'s `rel_path` convention (`target_category_name .. "/" .. filename`).

**References**: Plan at
`specs/862_fix_loader_symlink_delete_data_loss/plans/01_loader-symlink-delete-data-loss.md`.
Current phase: 3 of 6, completed. Modified files so far:
`lua/neotex/plugins/ai/shared/extensions/loader.lua`,
`lua/neotex/plugins/ai/shared/extensions/init.lua`.
