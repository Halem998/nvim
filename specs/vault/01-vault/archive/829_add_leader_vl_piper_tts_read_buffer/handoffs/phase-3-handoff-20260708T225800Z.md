# Phase 3 Handoff: Task #829

**Immediate Next Action**: All phases complete — proceed to Stage 5 (final verification) and
Stage 6 (implementation summary).

**Current State**: Phase 3 completed. `<leader>vl` registered in the `<leader>v` voice which-key
group (`lua/neotex/plugins/editor/which-key.lua`), section banner updated to "(STT + TTS)". Full
headless config load is error-free. All three plan phases are `[COMPLETED]`.

**Key Decisions Made**:
- Used icon `""` (speaker) to visually distinguish the TTS entry from the STT mic-family
  icons (`h`, `r`, `s`, `v` all use mic-related glyphs).
- Kept the entry normal-mode only (no `mode = {"n","v"}`), matching plan intent (cursor-to-EOF,
  no range/visual support).

**Deviations from Plan**:
- The plan's headless `verbose nmap <leader>vl` verification step is infeasible in this
  environment: which-key.nvim v3's `wk.add()` queues mapping specs and only converts them to
  real `vim.keymap.set` calls via a `vim.schedule_wrap`'d loader that requires a genuine
  interactive event loop to fire — it never runs in `nvim --headless -c ... -c "qa"`, confirmed
  by testing the pre-existing `<leader>vh` mapping (same "No mapping found" result, independent
  of this change). This is a pre-existing environment/tooling limitation, not a regression.
  Substituted verification: `grep` confirms exactly one `<leader>vl` binding, structurally
  identical to the working `vh`/`vr`/`vs`/`vv` entries, plus a clean full-config headless load.
- The plan's "manual" end-to-end tests were exercised as direct headless calls to
  `require('neotex.util.tts').start()/.stop()/.is_playing()` against the real `piper | play`
  pipeline (not stubbed) rather than literal `<leader>vl` keypresses, since which-key dispatch
  itself cannot be triggered headlessly (see above). This covers all playback-engine behavior;
  the only untested surface is the which-key key-dispatch layer, which is structurally identical
  to already-functioning entries.

**What NOT to Try**: Do not attempt further headless which-key dispatch verification — it is a
tooling limitation, not a bug, and retrying will not produce a different result. True end-to-end
confirmation of the `<leader>vl` keypress requires an interactive Neovim session.

**References**:
- Plan: `specs/829_add_leader_vl_piper_tts_read_buffer/plans/01_piper-tts-toggle.md`
- Current phase: 3 of 3 (completed) — all phases done
- Files: `lua/neotex/util/tts.lua`, `lua/neotex/plugins/editor/which-key.lua`
