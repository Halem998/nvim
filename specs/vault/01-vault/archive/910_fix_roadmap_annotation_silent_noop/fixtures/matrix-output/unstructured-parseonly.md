# Unstructured Notes

This fixture has headings only -- no `## Phase N:` headings, no `- [ ]`/`- [x]` checkboxes, and
no pipe-delimited tables. It exists to exercise the `[UNPARSEABLE ROADMAP ...]` banner and the
`unparseable_roadmap` warning code.

## Background

Some prose describing background context, with no recognizable structure for the parser.

### Details

More prose. Nothing here should be picked up as a phase, a checkbox, or a table row.
