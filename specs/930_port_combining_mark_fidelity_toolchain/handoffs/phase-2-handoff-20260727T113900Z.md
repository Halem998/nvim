# Handoff: Task #930 Phase 2

## Immediate Next Action

Begin Phase 3 (Doc-Lint Verification): run
`bash agent-system/extensions/core/scripts/check-extension-docs.sh` from repo root.

## Current State

Phase 2 completed. `provides.scripts` in `agent-system/extensions/literature/manifest.json` now
has 37 entries (up from 33), with the 4 new flat names inserted immediately after
`literature-convert.sh` and before `literature-chunk.sh` (adjacent to the fidelity/convert
cluster). JSON validated, no duplicates, every entry resolves to an existing file, and all 6
ported files (Phase 1 + Phase 2 combined) are present in `provides.scripts`.

## Key Decisions Made

- Insertion point chosen as immediately after `literature-convert.sh` (rather than after
  `literature-normalize-authors.sh`) since that keeps the 4 new tightly-coupled entries visually
  grouped together right where the convert/fidelity logic they attach to lives, still within the
  cluster boundary named in the plan.

## Deviations from Plan

- None

## What NOT to Try

- Do not add duplicate entries for `literature-convert.sh` or `literature-fidelity-audit.sh` —
  both already registered before this port; only their content changed.
- Do not add a path prefix to the 4 new entries.

## References

- Plan: `specs/930_port_combining_mark_fidelity_toolchain/plans/01_port-combining-mark-toolchain.md`
- Current phase: 2 (complete) -> next is Phase 3 (may land [PARTIAL] without blocking Phase 4/5)
