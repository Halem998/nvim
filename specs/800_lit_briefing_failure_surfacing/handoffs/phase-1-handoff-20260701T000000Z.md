# Phase 1 Handoff: Create the shared wrapper script

**Immediate Next Action**: Proceed to Phase 2 - update Stage 4a in skill-researcher, skill-planner,
skill-implementer (three sites each, plus trailing comment rewrite).

**Current State**: Phase 1 completed. `.claude/scripts/literature-briefing-invoke.sh` created,
executable, passes `bash -n`, verified success path (empty stdout for empty per-repo index) and
failure path (`--global` with no query -> `[lit] briefing generation failed (exit 1)` on stderr,
empty stdout, wrapper exit 0). Plan and progress file are current.

**Key Decisions Made**:
- Wrapper uses `set -uo pipefail` (no `-e`) so exit-code capture from
  `output="$(bash "$SCRIPT_DIR/literature-briefing.sh" "$@")"; exit_code=$?` works reliably.
- stderr is never redirected/captured by the wrapper — it flows through to the terminal/caller
  unmodified, satisfying "lets stderr flow" from the plan.

**Deviations from Plan**: None.

**What NOT to Try**: Do not add `2>/dev/null` anywhere in the wrapper or its call sites — that
defeats the purpose of this task.

**References**:
- Plan: specs/800_lit_briefing_failure_surfacing/plans/01_lit-briefing-failure-surfacing.md
- Current phase: 1 (of 4)
- New file: .claude/scripts/literature-briefing-invoke.sh
