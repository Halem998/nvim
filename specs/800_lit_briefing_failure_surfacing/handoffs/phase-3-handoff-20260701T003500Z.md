# Phase 3 Handoff: Update Stage 4a in the three hard skills

**Immediate Next Action**: Proceed to Phase 4 - repo-wide verification and scope confirmation
(grep for zero residual `literature-briefing.sh 2>/dev/null` occurrences, confirm
literature-briefing.sh and skill-cslib-* skills untouched, confirm literature-lit-flag-resolve.sh
untouched).

**Current State**: Phases 1-3 completed. All six target skills (skill-researcher, skill-planner,
skill-implementer, skill-researcher-hard, skill-planner-hard, skill-implementer-hard) now call
`.claude/scripts/literature-briefing-invoke.sh` at all 3 Stage 4a sites each (18 total), with
`2>/dev/null` removed everywhere. Plan and progress files are current for phases 1-3.

**Key Decisions Made**:
- Used Edit tool `replace_all: true` on the literal substring
  `bash .claude/scripts/literature-briefing.sh 2>/dev/null` -> `bash
  .claude/scripts/literature-briefing-invoke.sh` per file (all 3 sites share this exact
  substring, confirmed identical across files via grep before editing).
- The trailing comment rewrite targeted only the literature-specific
  `# lit_context will be empty string if: ... # - script exited with error` block, NOT the
  unrelated `# memory_context will be empty string if: ... # - script exited with error` block
  that appears earlier in each of the six files (Stage 4a memory retrieval, out of scope per
  the plan's Non-Goals). Disambiguated via surrounding multi-line context in each Edit call.

**Deviations from Plan**:
- Task 3.4: skill-implementer-hard has no `# - script exited with error` comment block in its
  live literature-briefing bash block at all, so nothing was rewritten there. See
  specs/800_lit_briefing_failure_surfacing/progress/phase-3-progress.json deviations array.

**What NOT to Try**: Do not touch the memory_context comment blocks (the first `# - script
exited with error` occurrence in each file) — that is Stage 4a memory retrieval, unrelated to
this task's literature-briefing scope.

**References**:
- Plan: specs/800_lit_briefing_failure_surfacing/plans/01_lit-briefing-failure-surfacing.md
- Current phase: 3 (of 4), next is Phase 4 (verification only, no files modified)
