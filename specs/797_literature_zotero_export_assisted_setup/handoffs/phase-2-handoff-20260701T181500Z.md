# Handoff: Task 797, Phase 2 Complete

## Immediate Next Action

Begin Phase 3: wire the assisted offer into `.claude/extensions/literature/commands/literature.md`
Mode A discover-mode step_2, adding a separate `zotero-export-status.sh` invocation (stdout AND
stderr captured, explicitly not `2>/dev/null`) before the existing `literature-discover.sh ...
2>/dev/null` call, with branches for all four directive tokens plus the orchestrator-mode default.

## Current State

Phases 1-2 complete. Plan and progress files are current (see
`plans/01_assisted-zotero-export-generation.md`, `progress/phase-1-progress.json`,
`progress/phase-2-progress.json`).

- `.claude/extensions/literature/scripts/zotero-generate-export.sh` (canonical, Phase 1):
  generator with 3 fallback paths, citekey synthesis, staleness stamp. Verified structurally
  (empty on-box zotero.sqlite + a synthetic 2-item fixture whose output round-trips through
  `zotero-search.sh --format=json`).
- `.claude/extensions/literature/scripts/zotero-export-status.sh` (canonical, Phase 2):
  stateless classifier, 4 directive tokens on stdout, rationale on stderr, never calls
  AskUserQuestion. Verified 3 of 4 states live (PRESENT, MISSING_NOT_RUNNING, UNAVAILABLE);
  MISSING_RUNNING is untestable on this box (no local Zotero API reachable).
- `.claude/extensions/literature/scripts/literature-discover.sh` (canonical, edited):
  `tier2_search()`'s missing-export stderr hint now also points at the assisted-generation
  entry point. Verified: stdout is still a pure JSON array (`jq empty` passes); hint appears
  only on stderr.

Neither new script has a flat `.claude/scripts/` copy yet -- that re-sync is Phase 4's job
(the plan explicitly sequences dual-copy re-sync AFTER all edits land, to avoid re-syncing
mid-edit).

## Key Decisions Made

- The classifier's `--orchestrator-mode` flag is accepted and echoed into the stderr rationale
  for consistency with the `--lit` precedent, but it does NOT change which of the 4 directive
  tokens is emitted -- classification is purely about local data-source state. The
  interactive-vs-autonomous decision (AskUserQuestion vs. visible default) is left entirely to
  the Phase 3 caller (`/literature`), matching the research report's Section 4 recommendation.
- The latent inconsistency (discover's tier2_search only checks
  `$LITERATURE_DIR/zotero-library.json`, not the full `resolve_library_path()` chain) is
  documented in the classifier's header comment, not fixed -- this matches the plan's explicit
  non-goal.

## Deviations from Plan

- None.

## What NOT to Try

- Do not attempt to live-test Path 1 (Zotero 7 API) or Path 2 (Better BibTeX JSON-RPC) or the
  `ZOTERO_EXPORT_MISSING_RUNNING` classifier state on this box -- Zotero is not running and
  there is no way to start it in this environment. This was already confirmed unreachable in
  Phase 1's research and re-confirmed in Phase 2; do not spend further tool calls probing it.
- Do not modify the main discover call's `2>/dev/null` capture in `literature.md` itself --
  that is explicitly out of scope (a separate follow-up per the plan's non-goals). Only ADD a
  preceding classifier invocation with its own, non-suppressed capture.

## References

- Plan: `specs/797_literature_zotero_export_assisted_setup/plans/01_assisted-zotero-export-generation.md`
- Research: `specs/797_literature_zotero_export_assisted_setup/reports/01_zotero-export-assisted-generation.md`
- Current phase: 3 of 4 (Phases 1-2 complete)
