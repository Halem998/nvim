# Phase-End Handoff: Phase 1 Complete

**Immediate Next Action**: Proceed to Phase 2 -- extend the union fix to the
`contracts/reference-grounding.md` and `contracts/anti-analysis.md` sibling entries in
`agent-system/extensions/lean/index-entries.json`.

**Current State**: Phase 1 completed and committed (commit `ecac3eb6b`). Lean's
`contracts/adversarial-verification.md` entry now declares
`load_when.agents: ["lean-research-hard-agent", "general-research-hard-agent"]`. Proved via an
executed headless-Neovim merge reconstruction (`neotex.plugins.ai.shared.extensions.merge.append_index_entries`,
core entries applied first, lean second) against the real on-disk index files: the post-edit run
shows the single surviving entry carrying both agent names; the pre-edit run (captured before the
edit) showed only `lean-research-hard-agent`, confirming the assertion fails pre-edit. Plan file
and progress file updated; scratch script retained at
`/tmp/claude-1000/-home-benjamin--config-nvim/0a7bf794-d1e4-4674-80e4-cccb5ea76880/scratchpad/phase1_merge_recon.lua`
for reuse in Phase 2 (extended to all three paths), to be deleted in Phase 4.

**Key Decisions Made**: None beyond what the plan already specified -- Phase 1 followed the plan
exactly, no deviations.

**Deviations from Plan**: None.

**What NOT to Try**: Do not substitute a static `jq` read of the edited JSON for the merge
reconstruction -- the plan explicitly forbids this and it does not satisfy the verification bar.

**References**:
- Plan: `specs/1001_lean_mirror_entry_load_order/plans/01_lean-mirror-load-order-fix.md` (Phase 2)
- Progress: `specs/1001_lean_mirror_entry_load_order/progress/phase-1-progress.json`
