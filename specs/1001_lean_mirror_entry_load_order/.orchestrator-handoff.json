{
  "status": "planned",
  "phase": "plan",
  "summary": "Created a 4-phase implementation plan. Research found 4 findings, not 1: the named lean/core contracts/adversarial-verification.md pair, two further identical-shape lean/core pairs (reference-grounding.md, anti-analysis.md), and a same-file duplicate inside cslib's own index. The plan resolves both scope boundaries explicitly: the two sibling lean pairs are IN scope (they live in the declared file_scope file and WORK item 1 says 'at minimum'), fixed in a separate phase after the mandated one; the cslib finding is OUT of scope for any fix (different file, outside file_scope, different collision mechanism) but recorded per WORK item 2. Verification is an executed merge reconstruction plus a proof it fails pre-edit, never a JSON read.",
  "artifacts": [
    {
      "type": "plan",
      "path": "specs/1001_lean_mirror_entry_load_order/plans/01_lean-mirror-load-order-fix.md",
      "summary": "Four-phase plan: union-value the named entry, extend to two siblings in the same file, record the audit and two follow-up candidates, closing gate"
    }
  ],
  "phases_completed": 0,
  "phases_total": 4,
  "blockers": [],
  "continuation_path": null,
  "next_action_hint": "Dispatch /implement 1001. Phase 1 is the mandated fix and must be verified by an executed merge reconstruction (core entries processed first, lean second) whose assertion is on the merged output, not on the edited JSON.",
  "decisions_made": [
    "IN SCOPE: contracts/reference-grounding.md and contracts/anti-analysis.md lean entries also get union-valued load_when.agents -- they are in agent-system/extensions/lean/index-entries.json, which is the entire declared file_scope, and WORK item 1's phrasing is a floor ('at minimum'), not a ceiling. Isolated in Phase 2 so the mandated Phase 1 fix lands and is independently verifiable first.",
    "OUT OF SCOPE for fix, IN SCOPE for the record: the cslib project/cslib/standards/ci-pipeline.md internal duplicate. agent-system/extensions/cslib/index-entries.json is outside this task's file_scope (a prospective lock-overlap declaration), and the same-file array-order collision is a different mechanism warranting its own verification. Phase 3 records it per WORK item 2's 'even if this task only fixes the lean instance'.",
    "WORK item 3 (loader warning / lint rule) is recorded as a follow-up candidate only, not implemented, per the task's explicit instruction to record rather than widen.",
    "Second follow-up candidate surfaced at plan time: lean's manifest provides.context includes 'contracts' and copy_category copies by path, so lean's context/contracts/*.md copies overwrite core's at the same deploy paths. The union index entry restores the HOOK for general-research-hard-agent but that agent then receives lean's lean4-specialized content, not core's generic contract. Recorded, not fixed."
  ],
  "plan_markers_verified": true
}
