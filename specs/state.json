{
  "version": "1.1.0",
  "next_project_number": 866,
  "active_projects": [
    {
      "project_number": 863,
      "project_name": "relocate_extension_source_store_out_of_claude",
      "status": "completed",
      "task_type": "neovim",
      "topic": "extensions",
      "title": "Relocate extension source store out of .claude/",
      "description": "Relocate the extension source store OUT of any deployed .claude/ tree so that .claude/ can become a pure, disposable copy-deploy build artifact regenerable from the <leader>al picker after deletion. This is the UNLOCKING change and must land FIRST.\n\nCONTEXT: The loader already deploys into the current working directory (lua/neotex/plugins/ai/shared/extensions/init.lua:238 `local project_dir = opts.project_dir or vim.fn.getcwd()`; deploy target = project_dir .. \"/\" .. config.base_dir = $CWD/.claude, init.lua:79). Sources are read from a GLOBAL root `global_extensions_dir` defaulting to ~/.config/nvim/.claude/extensions (config.lua:55 in the M.claude preset; global_dir defaults to vim.fn.expand(\"~/.config/nvim\")). The picker `list_extensions` reads from config.global_extensions_dir and returns empty if that dir is absent (manifest.lua:171-178). The ONLY reason ~/.config/nvim cannot self-rebuild today is that its source store (.claude/extensions/) physically lives INSIDE its own deploy target; every OTHER working directory can already delete-and-regenerate because source and target are different trees.\n\nREQUIRED: Physically move .claude/extensions/ to a sibling location NOT under any deployed .claude/ (candidate: ~/.config/nvim/agent-system/extensions/, or another non-.claude path the research phase should evaluate). Repoint global_extensions_dir in config.lua:55 (M.claude preset) to the new store. Adjust how manifest.lua and init.lua resolve the store ONLY if needed. Verify: (1) the picker lists extensions from the new location; (2) deploying into an arbitrary $CWD/.claude still works; (3) ~/.config/nvim itself now rebuilds like any other project (its .claude/ is a disposable deploy target with no source inside it).\n\nFILE SCOPE centers on lua/neotex/plugins/ai/shared/extensions/config.lua plus the physical relocation of the store; touches manifest.lua/init.lua store-resolution only if required.\n\nCROSS-CUTTING CONSTRAINTS: Copy-deploy only, NO symlink farm (the user explicitly rejected symlinks; absolute source paths would also leak across arbitrary project directories). All destructive loader testing happens in the scratchpad against fake project dirs, NEVER against the real ~/.config/nvim/.claude tree (this loader had a live data-loss bug recently). Honor the no-task-references-in-deliverables rule: no task-number citations in any file outside specs/**.\n\nDESIRED END STATE (shared across this sequence): .claude/ in any CWD is 100% disposable build output (nothing hand-authored, no source, no state, no orphans); one global source store lives OUTSIDE any .claude/; the invariant `.claude/ == deploy(store, selection)` holds. Foundation already landed and MUST be built on, not recreated: a deployed-vs-source rule drift check (check_deployed_rule_drift) and a symlink-safe remove path in loader.lua.",
      "dependencies": [],
      "file_scope": [
        "lua/neotex/plugins/ai/shared/extensions/config.lua",
        "lua/neotex/plugins/ai/shared/extensions/manifest.lua",
        "lua/neotex/plugins/ai/shared/extensions/init.lua"
      ],
      "created": "2026-07-14T22:58:04Z",
      "last_updated": "2026-07-14T23:32:24Z",
      "session_id": "sess_1784070109_39d07a_863",
      "artifacts": [
        {
          "path": "specs/863_relocate_extension_source_store_out_of_claude/reports/01_relocate-extension-source-store.md",
          "type": "report",
          "summary": "Traced every place resolving the global extension source store path. Single canonical default in config.lua:55 drives load/unload/list/verify, but sync.lua:909 duplicates the literal for the 'Load Core' picker sync, and two shell scripts (check-extension-docs.sh, validate-extension-index.sh) independently default to the old path. Recommended new store: ~/.config/nvim/agent-system/extensions/. Deployed per-project .claude/extensions/{name}/manifest.json stub (used by command-route-skill.sh, skill-base.sh) is confirmed unrelated and must not change."
        },
        {
          "path": "specs/863_relocate_extension_source_store_out_of_claude/plans/01_relocate-extension-store.md",
          "type": "plan",
          "summary": "Created a 6-phase implementation plan (3 dependency waves) to relocate the global extension source store from ~/.config/nvim/.claude/extensions/ to ~/.config/nvim/agent-system/extensions/ via history-preserving git mv. Wave 1 (Phase 1) moves the store and repoints the single canonical default (config.lua:55, M.claude preset). Wave 2 (Phases 2,3,4,6, parallel) derives sync.lua core_source_base from config to close the drift class, repoints the two shell scripts in both source and deployed copies (byte-identical), updates the manifest_spec.lua fixture, and sweeps docs. Wave 3 (Phase 5) verifies the three acceptance properties strictly in scratchpad fake project dirs."
        },
        {
          "path": "specs/863_relocate_extension_source_store_out_of_claude/summaries/01_relocate-extension-store-summary.md",
          "type": "summary",
          "summary": "Relocated the global extension source store from ~/.config/nvim/.claude/extensions/ to ~/.config/nvim/agent-system/extensions/ via history-preserving git mv (981 renamed files), repointed the single canonical config default (config.lua:55, M.claude preset), derived sync.lua's core_source_base from that canonical value to permanently close the drift class, repointed both shell scripts' defaults in source and deployed copies while keeping the pairs byte-identical, updated the manifest_spec.lua test fixture, swept documentation prose naming the source location (narrowing an initial 1506-hit grep to ~15 genuine source-store descriptions after classifying the remainder as out-of-scope: specs/** historical artifacts, .claude/** disposable deploy-tree copies, do-not-touch manifest.json runtime stubs, and a pre-existing unrelated @-reference citation pattern), and verified all three acceptance properties (picker listing, arbitrary-CWD deploy, full self-rebuild) strictly against scratchpad fake project dirs and a full rsync scratch copy of the repo. The real ~/.config/nvim/.claude tree was never destructively touched."
        }
      ],
      "completion_summary": "Relocated the global extension source store from ~/.config/nvim/.claude/extensions/ to ~/.config/nvim/agent-system/extensions/ via history-preserving git mv, repointed the canonical config default and the sync.lua drift-prone literal, kept both shell scripts byte-identical across source and deployed copies, updated the manifest_spec.lua fixture, swept documentation prose naming the source location, and verified all three acceptance properties (picker listing, arbitrary-CWD deploy, and full self-rebuild) strictly against scratchpad fake project dirs.",
      "roadmap_items": [
        "Relocate the extension source store out of any deployed .claude/ tree so .claude/ is a 100%-disposable copy-deploy build artifact"
      ]
    },
    {
      "project_number": 864,
      "project_name": "enforce_every_deployed_file_has_a_source",
      "status": "completed",
      "task_type": "meta",
      "topic": "extensions",
      "title": "Enforce every deployed file has a source (hard drift gate)",
      "description": "Enforce the invariant \"every deployed file has a source\" by extending the deployed-vs-source drift check into a hard gate, and by giving every deployed-only orphan a real source home in the core extension. Order this AFTER the store-relocation task (the check compares deployed output against the relocated store).\n\nCONTEXT: The existing rule-drift check (check_deployed_rule_drift in .claude/scripts/check-extension-docs.sh) currently covers only provides.rules. Deployed-only ORPHANS with no source exist today and would vanish on a clean rebuild: .claude/rules/no-task-references-in-deliverables.md (declared in core manifest but no source file under core/rules/), .claude/commands/README.md, and ~4 standalone scripts under .claude/scripts/.\n\nREQUIRED: (1) Extend the deployed-vs-source drift check in .claude/scripts/check-extension-docs.sh beyond provides.rules to ALL provides categories (agents, commands, context, scripts), following the per-category convention divergences already documented: agents needs an agents_subdir parameter; context needs recursive directory comparison. (2) Give the deployed-only orphans a source home in the core extension so nothing deployed is unsourced: no-task-references-in-deliverables.md, commands/README.md, and the ~4 standalone scripts. (3) Make the check a HARD gate (exit non-zero on any unsourced deployed file), proving the disposable-output invariant holds.\n\nDUAL-WRITE HAZARD (mandatory): check-extension-docs.sh has a dual copy at .claude/extensions/core/scripts/check-extension-docs.sh and the script lints itself. Every edit must land BYTE-IDENTICAL in both .claude/scripts/check-extension-docs.sh and .claude/extensions/core/scripts/check-extension-docs.sh.\n\nCROSS-CUTTING CONSTRAINTS: Copy-deploy only, no symlink farm. All destructive loader testing in the scratchpad against fake project dirs, never against the real .claude tree. Honor the no-task-references-in-deliverables rule: no task-number citations in any file outside specs/**. Build on (do not recreate) the already-landed check_deployed_rule_drift and the symlink-safe remove path in loader.lua.",
      "dependencies": [
        863
      ],
      "file_scope": [
        ".claude/scripts/check-extension-docs.sh",
        ".claude/extensions/core/scripts/check-extension-docs.sh",
        ".claude/extensions/core/rules/no-task-references-in-deliverables.md",
        ".claude/extensions/core/commands/README.md"
      ],
      "created": "2026-07-14T22:58:04Z",
      "last_updated": "2026-07-15T00:14:03Z",
      "session_id": "sess_1784073170_af81356c",
      "artifacts": [
        {
          "path": "specs/864_enforce_every_deployed_file_has_a_source/reports/01_enforce-every-deployed-file-source.md",
          "type": "report",
          "summary": "Verified current on-disk layout post-863 (core extension source now at agent-system/extensions/core/; check-extension-docs.sh's two .claude-scoped copies (.claude/scripts/ and agent-system/extensions/core/scripts/) are confirmed byte-identical; a third same-named .opencode script is a materially different, out-of-scope tool for a parallel deploy tree). Fully audited every provides category (agents, commands, context, scripts; rules already covered by the landed check_deployed_rule_drift) for deployed files with zero manifest source anywhere, using git ls-files diffed against the union of every extension's provides.<category> entries (recursive expansion for context, since copy_context_dirs deploys directory entries recursively). Found 0 agent orphans, 2 command orphans, 10 context orphans, and 8 script orphans (23 total) spanning three extensions (core, literature, nvim) -- not just core as the stale task description assumed. Also discovered and evidenced a critical, separate regression: 19 of ~21 symlinks under .claude/{agents,commands,skills}/ (the intentional 'symlink deploy mode' for cslib/literature/pr-review, created by install-extension.sh) are broken because that script hardcodes a relative target path '../extensions/$EXT_NAME/...' that resolved against the old .claude/extensions/ location and no longer resolves against agent-system/extensions/. Cross-referenced task 862's handoff, which explicitly restored and verified literature-agent.md and literature.md as working symlinks -- both are broken again now by 863's relocation, meaning the /literature command and its research agent may not currently resolve. Full findings, proposed source homes per orphan, and per-category comparison-logic design are in the research report."
        },
        {
          "path": "specs/864_enforce_every_deployed_file_has_a_source/plans/01_deployed-file-source-hard-gate.md",
          "type": "plan",
          "summary": "Created a 6-phase implementation plan for enforcing 'every deployed file has a source' as a hard gate. The plan sequences remediation before enforcement: Wave 1 runs four independent, disjoint-territory phases in parallel -- (1) repair the 19 broken install-extension.sh symlinks (fix the hardcoded '../extensions/$EXT_NAME/' relative target to '../../agent-system/extensions/$EXT_NAME/' byte-identically in BOTH copies of install-extension.sh, then delete-and-recreate the broken cslib/literature/pr-review symlinks since the script cannot self-repair them, and delete the dead zotero symlinks); (2) backfill the 4 genuinely core-owned orphans (no-task-references-in-deliverables.md, commands/README.md with a content rewrite, lint-contract-compliance.sh, validate-handoff.sh); (3) backfill the 8 literature-owned orphans and resolve literature-organization.md ownership to literature; (4) backfill the nvim orphan (extension-deploy-modes.md) plus the remaining core context/patterns orphans and resolve the fork-patterns.md duplicate via content diff. Wave 2 (Phase 5) extends check-extension-docs.sh with per-category orphan checks (agents/commands/context/scripts, honoring agents_subdir, recursive context comparison, scripts subdir entries, and merge_targets/index.json exclusion) plus a distinct broken-deployed-symlink check, at advisory level, and confirms 0 orphans / 0 broken symlinks on the remediated tree. Wave 3 (Phase 6) promotes those checks to blocking (FAILURES-contributing, non-zero exit) and verifies a clean exit-0 run. All check-extension-docs.sh and install-extension.sh edits are byte-identical dual-writes; all destructive check-logic testing happens in scratchpad against fake dirs."
        },
        {
          "path": "specs/864_enforce_every_deployed_file_has_a_source/summaries/01_deployed-file-source-hard-gate-summary.md",
          "type": "summary",
          "summary": "Executed all 6 phases of the plan across 3 waves. Wave 1 (phases 1-4, parallel territory): repaired the install-extension.sh symlink-deploy regression (fixed the hardcoded rel_path at all 5 sites in both byte-identical copies, deleted and recreated the 19 broken cslib/literature/pr-review symlinks, deleted 2 dead zotero symlinks); backfilled the 4 core-owned orphans (no-task-references-in-deliverables.md, commands/README.md with rewritten content, lint-contract-compliance.sh, validate-handoff.sh); backfilled the 8 literature-owned orphans (6 scripts + 2 context patterns) and resolved literature-organization.md ownership to literature's own context/guides/ tree; backfilled the nvim orphan (extension-deploy-modes.md, content extended) plus 4 more core context/patterns+guides orphans, and resolved the fork-patterns.md duplicate (byte-identical to existing core docs/fork-patterns.md -- deleted the deployed copy and its stale index.json entry). Wave 2 (phase 5): added 5 new project-wide checks to check-extension-docs.sh (Rules J/K/L/M for per-category orphans, Rule N for broken deployed symlinks), landed byte-identical in both copies, gated by a single ORPHAN_GATE_MODE toggle defaulting to advisory; verified 0 orphans and 0 broken symlinks on the remediated tree via scratchpad-only destructive testing. Wave 3 (phase 6): flipped ORPHAN_GATE_MODE's default to hard, added a Rule A-N header index, confirmed exit 0 on the real tree and non-zero exit with a git-tracked-then-reverted throwaway orphan, added minimal CI wiring (.github/workflows/check-extension-docs.yml, none existed before), and wrote the implementation summary. Task status transitioned planned -> implementing -> completed."
        }
      ],
      "completion_summary": "Repaired the 19+2 broken/dead install-extension.sh symlinks (relative-path fix in both dual-write copies), backfilled all 23 deployed-only orphans into their correct owning extensions (4 core, 8 literature, 1 nvim, plus 4 additional core context/patterns backfills and the fork-patterns.md/literature-organization.md ambiguous-case resolutions), and extended check-extension-docs.sh with 5 new per-category orphan + broken-symlink checks (Rules J-N), landed advisory-first then promoted to a hard, blocking gate that exits 0 on the remediated tree. Added minimal CI wiring (.github/workflows/check-extension-docs.yml) since none existed."
    },
    {
      "project_number": 865,
      "project_name": "make_claude_wipe_lossless_and_regenerable",
      "status": "completed",
      "task_type": "neovim",
      "topic": "extensions",
      "title": "Make .claude/ wipe lossless and one-keystroke regenerable",
      "description": "Make a .claude/ wipe lossless and one-keystroke regenerable by lifting all wipe-surviving state OUT of .claude/. Depends on the store-relocation task (the deploy/target model); can land in parallel with or after the drift-check-hardening task.\n\nCONTEXT: Selection/state is currently written INSIDE the deployed tree, so a wipe destroys it. get_state_path = project_dir .. \"/\" .. config.base_dir .. \"/\" .. config.state_file = $CWD/.claude/extensions.json (state.lua:71). settings.json and logs/ also currently live under .claude/ but are input/runtime, not build output. specs/ is already correctly at project root and is the model to follow.\n\nREQUIRED: (1) Move the selection manifest extensions.json from $CWD/.claude/extensions.json to a project-root location that survives a wipe (candidate: $CWD/.agent-extensions.json; research to confirm the name) so regenerate can skip re-picking, and so a project can optionally commit the file to pin its extension set. (2) Move settings.json and logs/ out of .claude/ (they are input/runtime, not output). (3) Update state.lua get_state_path and ALL readers/writers of the moved state. (4) Include a headless-nvim verification that constructs a FAKE project dir in the scratchpad, deploys, deletes .claude/, and regenerates identically from the surviving selection.\n\nTESTING SAFETY (mandatory): NEVER run destructive testing against the real ~/.config/nvim/.claude tree. This loader had a live data-loss bug recently; ALL destructive testing is scratch-only, against fake project dirs constructed in the scratchpad.\n\nCROSS-CUTTING CONSTRAINTS: Copy-deploy only, no symlink farm. Honor the no-task-references-in-deliverables rule: no task-number citations in any file outside specs/**. Build on (do not recreate) the already-landed check_deployed_rule_drift and the symlink-safe remove path in loader.lua. DESIRED END STATE: after a .claude/ wipe, nothing hand-authored or stateful is lost; regenerate from the surviving selection reproduces .claude/ identically, upholding `.claude/ == deploy(store, selection)`.",
      "dependencies": [
        863
      ],
      "file_scope": [
        "lua/neotex/plugins/ai/shared/extensions/state.lua",
        "lua/neotex/plugins/ai/shared/extensions/init.lua"
      ],
      "created": "2026-07-14T22:58:04Z",
      "last_updated": "2026-07-15T00:44:18Z",
      "session_id": "sess_1784070109_39d07a_865",
      "artifacts": [
        {
          "path": "specs/865_make_claude_wipe_lossless_and_regenerable/reports/01_wipe-lossless-regenerable-research.md",
          "type": "report",
          "summary": "extensions.json can move cleanly to a system-scoped project-root dotfile (.claude-extensions.json / .opencode-extensions.json via a new config.lua field) with all Lua and shell readers/writers enumerated. settings.json/settings.local.json CANNOT literally relocate -- Claude Code hardcodes the .claude/ path for project settings; the real fix is install-once semantics in loader.lua (matching sync.lua's existing OpenCode pattern) plus an explicit backup/restore step around the wipe operation. logs/ has no deploy skeleton at all and is a simple path-constant change across 5 source scripts plus deployed copies."
        },
        {
          "path": "specs/865_make_claude_wipe_lossless_and_regenerable/plans/01_wipe-lossless-regenerable.md",
          "type": "plan",
          "summary": "7-phase implementation plan created. Extensions.json relocates to preset-scoped project-root dotfiles (.claude-extensions.json / .opencode-extensions.json) via a new config.lua root_state_file field at the single state.lua get_state_path choke point, with all shell readers (skill-base.sh, validate-wiring.sh) and deployed copies tracked. New manager.regenerate logic reads the surviving manifest's active extensions and iterates manager.load to rebuild without re-picking. Settings.json/settings.local.json keep their hardcoded .claude/ path (host-app constraint) but gain install-once semantics in loader.lua copy_root_files (mirroring sync.lua's OpenCode pattern) plus a wipe-time backup/restore wrapper. logs/ relocates to a project-root directory via path-constant changes across 5 source scripts and deployed copies. Full wipe-and-regenerate cycle plus settings-reload regression verified headlessly in a scratchpad fake project dir, never against the real tree."
        },
        {
          "path": "specs/865_make_claude_wipe_lossless_and_regenerable/summaries/01_wipe-lossless-regenerable-summary.md",
          "type": "summary",
          "summary": "All 7 phases across 4 waves complete. Extensions.json relocated to preset-scoped project-root dotfiles (.claude-extensions.json / .opencode-extensions.json) via config.lua's new root_state_file field at the single state.lua get_state_path choke point; all shell readers (skill-base.sh, validate-wiring.sh) and their .claude/.opencode deployed copies updated in lockstep. manager.regenerate (init.lua) reads the surviving manifest's active extensions, resets state, and reloads each via manager.load, whose existing recursive dependency resolution handles ordering. settings.json/settings.local.json keep their hardcoded .claude/ path (host-app constraint): loader.lua's copy_root_files gained an install-once guard, and a post-hoc gap was found and closed where manager.unload was deleting these files before install-once could ever apply on the next load, defeating it for the unload-then-load reload cycle. A new settings_backup.lua module provides true zero-loss across a full wipe (backup -> rm -rf base_dir -> regenerate -> restore), wired into manager.regenerate. logs/ relocated to project-root .agent-logs/ across 5 source scripts and all deployed copies. Documentation swept in EXTENSION.md, README.md, extension-system.md, permission-configuration.md, and the literature extension's README.md. Phase 7 proved the full cycle headlessly in scratchpad fake project dirs: 20/20 assertions passed, all 292 pre-wipe files reproduced byte-identically post-regenerate (modulo a discovered pre-existing, out-of-scope index.json key-order non-determinism), and the real ~/.config/nvim/.claude tree was never touched destructively. check-extension-docs.sh (the hard orphan/broken-symlink gate) passes after every phase."
        }
      ]
    },
    {
      "project_number": 862,
      "project_name": "fix_loader_symlink_delete_data_loss",
      "status": "completed",
      "task_type": "neovim",
      "topic": "extensions",
      "description": "Fix data loss in the extension loader: remove_installed_files() deletes through symlinks, destroying tracked extension-source files.\n\nOBSERVED DATA LOSS: an extension reload deleted .claude/extensions/literature/skills/skill-literature/SKILL.md (2265 lines, git-tracked). Evidence: a check-extension-docs.sh run immediately before the reload recorded \"literature PASS\"; immediately after, \"literature FAIL: manifest skill entry missing on disk: skills/skill-literature/SKILL.md\". The extension-source directory mtime matched the reload timestamp to the second. The file was recovered from git HEAD; /literature was broken until then.\n\nROOT CAUSE: deployed skills are symlinks into extension sources, e.g. .claude/skills/skill-literature -> ../extensions/literature/skills/skill-literature. The unload step in lua/neotex/plugins/ai/shared/extensions/loader.lua (remove_installed_files, approx lines 755-784) iterates installed_files and calls vim.fn.delete(filepath) on the DEPLOYED path. Because the deployed path resolves through the symlink, the delete lands on the real extension-source file. The subsequent copy step then has nothing to copy and leaves an empty directory. Deployed and source are the same inode, so \"remove the deployed copy\" means \"destroy the source\".\n\nSTILL LIVE: the symlink is unchanged, so another reload destroys the file again. Nine other deployed skills are symlinks into extension sources and are exposed whenever their extension unloads: skill-cslib-implementation, skill-cslib-implementation-hard, skill-cslib-research, skill-cslib-research-hard, skill-cslib-vet, skill-pr-implementation, skill-pr-review-implementation, skill-pr-review-research, skill-zotero.\n\nSECOND GAP: remove_installed_files() takes no protected_paths argument, so .syncprotect does not guard the delete path at all. copy_file() honors protected_paths; the removal path does not. A user who lists a file in .syncprotect is protected on sync but not on unload.\n\nTHIRD SYMPTOM (same interaction, lower severity): the reload replaced .claude/agents/literature-agent.md and .claude/commands/literature.md, which are mode 120000 (symlinks) in HEAD, with regular files. Content was preserved but the symlink setup was silently flattened; these show as typechanges (T) in git status.\n\nREQUIRED: make the removal path symlink-aware so it never deletes through a symlink into an extension source -- e.g. skip paths where the deployed entry is a symlink (or resolves outside the deployed tree), or unlink the symlink itself rather than its target. Decide and document whether the symlink deploy mode is supported: if supported, both copy and remove must handle it; if not, the loader should refuse to install over a symlink rather than silently destroying it. Also thread protected_paths/.syncprotect through remove_installed_files() so protection is symmetric across copy and remove. Verify with nvim --headless: reload an extension whose deployed skill is a symlink and assert the source file survives.",
      "dependencies": [],
      "file_scope": [
        "lua/neotex/plugins/ai/shared/extensions/loader.lua",
        "lua/neotex/plugins/ai/shared/extensions/init.lua"
      ],
      "created": "2026-07-14T21:24:17Z",
      "last_updated": "2026-07-14T22:25:26Z",
      "session_id": "sess_1784064322_70bafc_862",
      "artifacts": [
        {
          "path": "specs/862_fix_loader_symlink_delete_data_loss/reports/01_loader-symlink-delete-data-loss.md",
          "type": "report",
          "summary": "Loader symlink delete research"
        },
        {
          "path": "specs/862_fix_loader_symlink_delete_data_loss/plans/01_loader-symlink-delete-data-loss.md",
          "type": "plan",
          "summary": "Loader symlink fix plan"
        },
        {
          "path": "specs/862_fix_loader_symlink_delete_data_loss/summaries/01_loader-symlink-delete-data-loss-summary.md",
          "type": "summary",
          "summary": "Loader symlink fix"
        }
      ]
    },
    {
      "project_number": 861,
      "project_name": "add_rule_drift_check_to_extension_lint",
      "status": "completed",
      "task_type": "meta",
      "topic": "agent-system",
      "description": "Add a deployed-vs-source content drift check for extension rules in check-extension-docs.sh. The script's check_deployed_script_drift covers manifest.provides.scripts only, comparing the deployed .claude/scripts/<name> against the extension source <ext>/scripts/<name>. There is no equivalent check for provides.rules, so a rule file whose deployed .claude/rules/<name>.md has diverged from its extension source is invisible to lint.\n\nThis is not hypothetical. core/rules/pr-prohibition.md had silently accumulated 35 lines in its deployed copy (the \"/pr --review Workflow\" section) that were absent from the extension source. The divergence survived precisely because the rule was unregistered in provides.rules, so the loader never overwrote the deployed copy. Once registered, the loader's byte-for-byte copy_file() overwrite would have destroyed that content -- the reconciliation had to be performed by hand first. A drift check would have surfaced this years earlier.\n\nImplement check_deployed_rule_drift mirroring check_deployed_script_drift: for each manifest.provides.rules entry where BOTH the deployed .claude/rules/<name> and the extension source <ext>/rules/<name> exist, fail on content mismatch; skip with an info note (never fail) when the deployed copy is absent, since an extension's rules are not deployed in every consuming repo.\n\nAlso evaluate whether the same deployed-vs-source asymmetry applies to provides.agents, provides.commands, and provides.context, and whether a single drift helper parameterized by category is warranted rather than adding a fourth near-duplicate function.\n\nNote: check-extension-docs.sh is itself declared in core's provides.scripts, so any edit must be written to BOTH .claude/scripts/ and .claude/extensions/core/scripts/ or the drift check will flag the change itself.",
      "dependencies": [],
      "file_scope": [
        ".claude/scripts/check-extension-docs.sh",
        ".claude/extensions/core/scripts/check-extension-docs.sh"
      ],
      "created": "2026-07-14T21:14:41Z",
      "last_updated": "2026-07-14T21:41:33Z",
      "session_id": "sess_1784064322_70bafc_861",
      "artifacts": [
        {
          "path": "specs/861_add_rule_drift_check_to_extension_lint/reports/01_rule-drift-check-lint.md",
          "type": "report",
          "summary": "Rule drift check research"
        },
        {
          "path": "specs/861_add_rule_drift_check_to_extension_lint/plans/01_rule-drift-check-lint.md",
          "type": "plan",
          "summary": "Rule drift check plan"
        },
        {
          "path": "specs/861_add_rule_drift_check_to_extension_lint/summaries/01_rule-drift-check-lint-summary.md",
          "type": "summary",
          "summary": "Rule drift check implementation"
        }
      ]
    },
    {
      "project_number": 859,
      "project_name": "fix_task_order_topic_case_indentation",
      "title": "Fix generate-task-order.sh: dependency tree renders flat for non-lowercase topic strings (topic-key case mismatch)",
      "status": "completed",
      "task_type": "meta",
      "topic": "agent-system",
      "effort": "low",
      "dependencies": [],
      "artifacts": [
        {
          "type": "report",
          "path": "specs/859_fix_task_order_topic_case_indentation/reports/01_topic-standardization.md",
          "summary": "Full topic-standardization audit: origin case-mismatch bug plus schema, write/read-path, canonical-form, autonomous-context, and .opencode parity gaps"
        },
        {
          "type": "plan",
          "path": "specs/859_fix_task_order_topic_case_indentation/plans/01_topic-normalization.md",
          "summary": "6-phase plan: single normalize_topic() helper (lowercase kebab-case) applied at manage-topics.sh write chokepoint and both generate-task-order.sh renderers (.claude core + .opencode), autonomous-context directive, schema docs, and local verification (no migration)"
        },
        {
          "type": "summary",
          "path": "specs/859_fix_task_order_topic_case_indentation/summaries/01_topic-normalization-summary.md",
          "summary": "Execution summary: normalize_topic() spine applied at write chokepoint and both renderers; autonomous-context directive; schema docs; zero-migration verification"
        }
      ],
      "created": "2026-07-14T15:37:34Z",
      "next_artifact_number": 2,
      "file_scope": [
        ".claude/extensions/core/scripts/generate-task-order.sh",
        ".opencode/scripts/generate-task-order.sh"
      ],
      "description": "generate-task-order.sh renders the Grouped-by-Topic dependency tree FLAT (no indented '└─' children) for any topic whose raw string is not already all-lowercase. ROOT CAUSE: topic sections are grouped under a LOWERCASED key (`_current_section_topic` = the lowercased topic, set from the grouping key at generate_grouped_section), but the two guards that decide child recursion / cross-topic annotation compare that lowercased key against the RAW topic string from state.json. In .claude/extensions/core/scripts/generate-task-order.sh: line 578 `\"$dep_topic\" != \"$_current_section_topic\"` (successor-skip in _print_topic_node) and line 553 `\"$task_topic_val\" != \"$_current_section_topic\"` (cross-topic 'see above' annotation). For a task with topic 'Modal Logic', dep_topic='Modal Logic' but _current_section_topic='modal logic', so the strings never match, every successor is skipped, and the tree collapses to a flat list. Only all-lowercase topics (e.g. 'modal-logic') ever showed the tree; Title-Case topics ('Modal Logic', 'Code Hygiene', etc.) always rendered flat. FIX (already validated live in the deployed Projects/cslib copy): lowercase both sides of both comparisons — line 578 -> `\"${dep_topic,,}\" != \"$_current_section_topic\"` and line 553 -> `\"${task_topic_val,,}\" != \"$_current_section_topic\"`. After the fix the 491->495->496->484 chain indents correctly under a single Modal Logic heading. APPLY TO: (1) source-of-truth .claude/extensions/core/scripts/generate-task-order.sh (lines 553, 578); (2) mirror the same two-line change to .opencode/scripts/generate-task-order.sh (lines 481, 506) for OpenCode parity. The deployed .claude/scripts/generate-task-order.sh is regenerated from the core extension on load, so editing the core source is what persists; do not rely on the standalone .claude/scripts copy. VERIFY: after editing, run `bash .claude/scripts/generate-todo.sh` (or a fixture) on a state.json containing a Title-Case topic with an intra-topic dependency chain and confirm the '└─' indentation appears. RELATED REFINEMENT (optional, same file): the topic grouping key lowercases but does NOT normalize separators, so 'Modal Logic' and 'modal-logic' hash to different keys ('modal logic' vs 'modal-logic') and produce TWO identical-looking '### Modal Logic' headings. Consider normalizing hyphen<->space (and maybe collapsing whitespace) in the grouping key so case- and separator-variant topic strings collapse into one section, preventing duplicate headings. CONTEXT: discovered in Projects/cslib while normalizing kebab-case topic values to Title-Case, which merged two duplicate Modal Logic sections but exposed this latent case-sensitivity bug.",
      "last_updated": "2026-07-14T16:14:03Z",
      "session_id": "sess_859_implement_topic_normalization",
      "completion_summary": "Introduced a single canonical normalize_topic() helper (lowercase kebab-case) and routed it through manage-topics.sh (write) and both generate-task-order.sh renderers (.claude core + .opencode, read/grouping), fixing the origin case-mismatch bug, preventing duplicate case/separator headings, adding an autonomous-context directive, and documenting the topic/active_topics schema fields. Verified zero migration needed for this repo's existing topic data."
    },
    {
      "project_number": 857,
      "project_name": "mail_reindex_on_failure_gate",
      "title": "Reindex-on-failure for the aerc launch gate",
      "status": "completed",
      "task_type": "neovim",
      "topic": "neovim",
      "effort": "small",
      "dependencies": [],
      "artifacts": [
        {
          "path": "specs/857_mail_reindex_on_failure_gate/reports/01_reindex-on-failure-gate.md",
          "type": "report",
          "summary": "Research report with verified line-numbered snippets, exact before/after Lua for sync_all_mail(), callback-contract decision (on_done(false) on mbsync failure regardless of reindex outcome), call-site safety confirmation (only <leader>me and <leader>mN, both safe), and the census_freshness_ok doc-comment text."
        },
        {
          "path": "specs/857_mail_reindex_on_failure_gate/plans/01_reindex-on-failure-gate.md",
          "type": "plan",
          "summary": "2-phase plan (1h total) with exact before/after Lua snippets for the sync_all_mail replacement and the census_freshness_ok comment extension, explicit success-criteria checklist, gate-untouched guarantee, and rollback via single-file revert."
        },
        {
          "path": "specs/857_mail_reindex_on_failure_gate/summaries/01_reindex-on-failure-gate-summary.md",
          "type": "summary",
          "summary": "Execution summary: both-branch reindex helper, verification results, constraint confirmation"
        }
      ],
      "created": "2026-07-13T19:43:53Z",
      "next_artifact_number": 1,
      "file_scope": [
        "lua/neotex/plugins/tools/mail.lua"
      ],
      "description": "Harden the <leader>me aerc launch gate in lua/neotex/plugins/tools/mail.lua so a failed `mbsync -a` no longer leaves notmuch stale before the freshness decision, providing a minimal, durable fix without unnecessary complexity.\n\nCURRENT BEHAVIOR (verified in mail.lua): sync_all_mail() runs `notmuch new` ONLY when `mbsync -a` exits 0; on mbsync failure it notifies the error and returns via on_done(false) WITHOUT reindexing. The gate itself already refuses-and-reports rather than failing open -- it opens aerc only on a clean sync+reindex, or when census_freshness_ok() reads [ok] on BOTH accounts, otherwise it refuses with remediation guidance. That refuse-and-report barrier is sound and must be preserved.\n\nGAP TO FIX (the real durable change): on the mbsync-failure path, run `notmuch new` BEFORE the freshness decision, so maildir files that already landed on disk -- or mailboxes that synced before an abort such as a duplicate-UID collision -- get reconciled into the index. Then let the existing authoritative barrier make the launch decision against a freshly reindexed state instead of a stale one.\n\nSCOPE CONSTRAINTS:\n- Keep the change minimal and cohesive (one function/gate); do not duplicate freshness logic that belongs in the external census wrapper.\n- Preserve the existing refuse-and-report barrier and its remediation messaging.\n- The fallback signal census_freshness_ok() reads the \"INBOX freshness ... [ok]\" line, a count-with-tolerance proxy that cannot detect flag-renames or phantom drift -- this is what produced the false green. Making that signal authoritative is an EXTERNAL follow-up in ~/.dotfiles modules/home/email/agent-tools/census.nix (rename/deletion-aware freshness) and is intentionally OUT OF SCOPE here. Document the dependency in the gate's comment so a future reader knows the barrier's trustworthiness rests on that external signal.\n\nOUT OF SCOPE (context only, no repo change, no commit): one-time ~/Mail data repair for the duplicate UID 15 collision in Gmail/.All_Mail, and the one-time `notmuch new` index reconciliation of the ~38-file INBOX phantom drift. These are manual mail-data operations, not code.",
      "last_updated": "2026-07-13T19:58:31Z",
      "session_id": "sess_1783972043_17c8e3",
      "completion_summary": "Added reindex-on-failure to sync_all_mail() in mail.lua: extracted run_notmuch_new(cb) and chained it in both mbsync on_exit branches so notmuch new runs even when mbsync -a fails; on_done(false) is hard-coded on failure (never fail-open), so the existing refuse-and-report aerc gate now evaluates a reconciled index. Added a census.nix signal-quality doc note and fixed a header keymap typo. Gate logic unchanged; census.nix signal improvement remains an external ~/.dotfiles follow-up."
    },
    {
      "project_number": 858,
      "project_name": "mail_launch_gate_performance",
      "title": "Fast, responsive, non-failing <leader>me aerc launch",
      "status": "completed",
      "task_type": "neovim",
      "topic": "neovim",
      "effort": "medium",
      "dependencies": [],
      "artifacts": [
        {
          "path": "specs/858_mail_launch_gate_performance/reports/01_launch-gate-performance-diagnosis.md",
          "type": "report",
          "summary": "Seed diagnosis: three overlapping sync layers on the launch-blocking path, mbsync -a worst-case slowness, the 857+109 interaction that doubles sync work, and the core design flaw (gate conflates mbsync success with index-read safety, permanently blocking aerc on an unrepairable-by-sync duplicate-UID). Includes design desiderata and open questions for --hard research."
        },
        {
          "path": "specs/858_mail_launch_gate_performance/reports/02_hard-research-fast-responsive-launch.md",
          "type": "report",
          "summary": "Hard-mode (fable) research. CONFIRMED: 2 serial full-sync layers on the launch path (mbsync -a + hook-triggered mail-sync both); gate couples launch to mbsync exit code (mail.lua:49,72). NEW: block is over-determined — census logos fallback is FALSE [STALE] due to census.nix honoring search.exclude_tags (22 trash-tagged INBOX msgs dropped; 337=337 under --exclude=false), violating wrapper-contracts.md audit rule. himalaya inbox sync REFUTED as a per-press trigger (independent 15-min autosync). notmuch new --no-hooks measured 0.194s. Recommends: gate launch on foreground notmuch new --no-hooks (~0.2s) only, open aerc immediately, background one deduplicated hook-ful sync that warns-not-blocks, delete mbsync -a, drop census from launch path."
        },
        {
          "path": "specs/858_mail_launch_gate_performance/plans/02_fast-responsive-aerc-launch.md",
          "type": "plan",
          "summary": "4-phase plan: gate <leader>me launch on foreground notmuch new --no-hooks only, add one deduplicated background hook-ful notmuch new pipeline shared quiet/loud by <leader>me/<leader>mN with warn-never-block semantics, delete mbsync -a and census fallback, disable the himalaya auto-sync timer default, then verify the resilience matrix with the duplicate-UID collision unrepaired."
        },
        {
          "path": "specs/858_mail_launch_gate_performance/summaries/02_fast-responsive-aerc-launch-summary.md",
          "type": "summary",
          "summary": "Implementation summary with verification evidence and plan deviations (lock-wait finding, pre-new || true contract, config.get falsy fix)"
        }
      ],
      "created": "2026-07-13T21:36:39Z",
      "next_artifact_number": 3,
      "file_scope": [
        "lua/neotex/plugins/tools/mail.lua",
        "lua/neotex/plugins/tools/himalaya/ui/main.lua"
      ],
      "description": "The <leader>me aerc launch workflow in lua/neotex/plugins/tools/mail.lua has become slow and now permanently refuses to open aerc. A single press triggers THREE overlapping sync layers -- (1) a himalaya-plugin inbox sync, (2) mail.lua sync_all_mail running the forbidden all-channels `mbsync -a` (which syncs Gmail All_Mail ~64k messages and fails code 1 on a pre-existing duplicate-UID collision), and (3) run_notmuch_new calling hook-ful `notmuch new`, whose preNew hook is now `mail-sync both` (dotfiles task 109), recursively driving a full serialized gmail+logos mbsync -- plus two email-census scans. All of it runs synchronously on the launch-blocking critical path, so the user waits for the slowest full sync before anything appears. The gate then refuses to open aerc because it conflates \"mbsync succeeded\" with \"index is safe to read\": the primary barrier requires mbsync -a + notmuch new to exit 0 (impossible while the duplicate-UID persists -- a manual data repair, not a sync fix), and the census fallback honestly reports [STALE]. Net effect: a sync-independent data fault permanently denies the mail client even though, after notmuch new reconciles the index, opening aerc would be safe. GOAL: redesign <leader>me for fast, responsive, non-failing launch -- open aerc promptly on a cheaply-reconciled index (notmuch new --no-hooks) with mbsync deferred/backgrounded and never on the launch-blocking path; decouple launch from mbsync exit code (warn, do not block); eliminate the redundant sync layers (route through the single mail-sync wrapper, use --no-hooks to avoid recursive re-sync, reconcile/suppress the himalaya double-sync); never use mbsync -a; keep the full explicit sync on <leader>mN. Must remain safe against opening onto a genuinely stale/mid-write Xapian index (no return of \"could not get MessageInfo\" races). Non-goal: the one-time duplicate-UID-15 maildir data repair, but the fix must make <leader>me usable despite an unrepaired collision. See reports/01_launch-gate-performance-diagnosis.md for the seed diagnosis; deep study to be produced by a fable --hard researcher.",
      "last_updated": "2026-07-13T22:17:19Z",
      "session_id": "sess_1783979541_058844",
      "completion_summary": "Redesigned <leader>me: launch now gates only on foreground 'notmuch new --no-hooks' (~0.14-0.58s) and opens aerc immediately. Deleted mbsync -a, sync_all_mail, run_notmuch_new, and census fallback. Added a single deduplicated background hook-ful notmuch new pipeline (preNew=mail-sync both) shared quiet/loud by <leader>me/<leader>mN with warn-never-block semantics; disabled the himalaya auto-sync timer default (fixing an M.get falsy-value bug and the toggle command). Works with the duplicate-UID-15 collision unrepaired; 16/16 behavioral checks pass."
    },
    {
      "project_number": 860,
      "project_name": "enforce_plan_compliance_rule",
      "status": "completed",
      "task_type": "meta",
      "topic": "agent-system",
      "description": "Add a .claude/rules/ rule enforcing strict plan compliance for lean-implementation-agent and other formal implementation agents. The rule should: (1) Prohibit agents from \"assessing what's truly minimal\" or inventing alternative approaches when a plan exists. (2) Require agents to follow the plan's exact task sequence step-by-step, in order. (3) Explicitly ban common divergence patterns: skipping intermediate theorems, inlining proofs instead of following the plan's decomposition, routing through different helper lemmas than specified, and \"cleaner approach\" rationalizations. (4) Be auto-applied via glob pattern to formal proof files (e.g. Theories/**, **/*.lean); the glob must generalize across consuming repos rather than hard-coding one project's layout. (5) Reference the repeated failures in BimodalLogic task 157 (8 plan versions, agents diverging every time) as motivation. The rule should be concise but firm -- agents must treat the plan as a contract, not a suggestion.\n\nProvenance: relocated from BimodalLogic task 162 (2026-07-14). Authored here in the agent-system source of truth so the rule syncs out to consuming repos, rather than living only in BimodalLogic/.claude/.",
      "dependencies": [],
      "file_scope": [
        ".claude/rules/"
      ],
      "created": "2026-07-14T20:20:03Z",
      "last_updated": "2026-07-14T20:54:41Z",
      "session_id": "sess_1784060858_701891",
      "artifacts": [
        {
          "path": "specs/860_enforce_plan_compliance_rule/reports/01_plan-compliance-rule.md",
          "type": "report",
          "summary": ""
        },
        {
          "path": "specs/860_enforce_plan_compliance_rule/plans/01_plan-compliance-rule.md",
          "type": "plan",
          "summary": ""
        },
        {
          "path": "specs/860_enforce_plan_compliance_rule/summaries/01_plan-compliance-rule-summary.md",
          "type": "summary",
          "summary": ""
        }
      ]
    }
  ],
  "completed_projects": [],
  "repository_health": {
    "last_assessed": "2026-07-13T19:39:05Z",
    "todo_count": 44,
    "fixme_count": 2,
    "build_errors": 0,
    "status": "healthy"
  },
  "memory_health": {
    "last_distilled": null,
    "distill_count": 0,
    "total_memories": 3,
    "never_retrieved": 2,
    "health_score": 100,
    "status": "healthy"
  },
  "active_topics": [
    "wezterm-notifications",
    "workflow-refactor",
    "agent-system",
    "literature",
    "cslib",
    "extensions",
    "neovim"
  ]
}
