# Phase 2 Handoff: Scratch-only verification of the removal fix

**Immediate Next Action**: Begin Phase 3 — filter `.syncprotect` at the `init.lua` call sites
(`manager.unload`, and the rollback path in `manager.load`), plus thread `opts.project_dir`
through both `remove_installed_files` call sites.

**Current State**: Phase 2 completed. The Phase 1 fix is proven correct against real headless-nvim
execution: Case A source survives post-fix (and is proven destroyed pre-fix via a `loadfile()`
shadow override of the pre-Phase-1 code — see plan Phase 2 notes for the full transcript and the
runtimepath-loader gotcha that required this approach instead of git-stashing). Plan file is
current (Phase 2 heading `[COMPLETED]`, all checklist items checked with transcript evidence).

**Key Decisions Made**: Discriminating-power proof used a `loadfile()`-based shadow module
override (extracting pre-fix content via `git show HEAD~1:...`) rather than git-stashing the
tracked file, because Neovim's runtimepath-based `require()` loader ignores `package.path`
ordering for files under `~/.config/nvim` even with `-u NONE`. This was necessary to avoid ever
touching the real tracked `loader.lua` file during testing.

**Deviations from Plan**: The plan's suggested method ("stashing the Phase 1 change") was not
used; a functionally equivalent but safer `loadfile()`-shadow approach was substituted, achieving
the same discriminating-power proof without any git stash/checkout cycle on the tracked file. This
is a methodology substitution, not a scope or outcome deviation — annotated inline in Phase 2's
checklist.

**What NOT to Try**: Do not attempt to override which `loader.lua` is loaded by manipulating
`package.path` alone in headless nvim tests under this config — the runtimepath loader wins. Use
`loadfile()` + `package.loaded[...]` pre-population instead if a shadow-module comparison is ever
needed again in this task.

**References**: Plan at
`specs/862_fix_loader_symlink_delete_data_loss/plans/01_loader-symlink-delete-data-loss.md`.
Current phase: 2 of 6, completed. No repository files changed in this phase (scratch fixtures and
test script only, under
`/tmp/claude-1000/-home-benjamin--config-nvim/86fe22e3-6df8-4d8d-bcaa-b7402fdaeefc/scratchpad/loader-symlink-test/`).
