# Phase 5 Handoff: Scratch integration test of the full unload/load cycle

**Immediate Next Action**: Begin Phase 6 — restore the two flattened symlinks
(`.claude/agents/literature-agent.md`, `.claude/commands/literature.md`) and write the
`extension-deploy-modes.md` context document. This phase touches the real `.claude/` tree via
`git`/`ln` only (no loader operation).

**Current State**: Phase 5 completed. The real `manager.load`/`manager.unload`/`manager.reload`
functions were exercised end-to-end against a fully isolated scratch project (own
`global_extensions_dir`, own `project_dir`, own `extensions.json`). Confirmed: a symlink-deployed
extension survives a full reload with zero data loss and zero flattening (the exact incident
scenario, reproduced faithfully by loading normally then converting deployed paths to symlinks via
`ln -s`, mirroring what `install-extension.sh` does in practice); an ordinary copy-deployed
extension still loads/reloads normally. Plan file is current (Phase 5 heading `[COMPLETED]`, all
checklist items checked with full transcript evidence). Verified the real `.claude/extensions.json`
diff visible in `git status` predates this session (mtime earlier than session start) and is
unrelated concurrent activity, not caused by this test.

**Key Decisions Made**: Substituted the plan's suggested "hand-author extensions.json" approach
with a more faithful one: call the real `manager.load()` first, then convert deployed paths to
symlinks via `ln -s`, exactly mirroring how the real incident actually arose (copy-load, then
independent symlink conversion via `install-extension.sh`, leaving `extensions.json` state
unaware of the conversion).

**Deviations from Plan**: Methodology substitution only (see above) — no scope or outcome
deviation; all of Phase 5's required assertions were satisfied.

**What NOT to Try**: Do not call any `manager.*` function without explicitly passing
`opts.project_dir` — the default falls back to `vim.fn.getcwd()`, which in this session's shell
context is the real repository root.

**References**: Plan at
`specs/862_fix_loader_symlink_delete_data_loss/plans/01_loader-symlink-delete-data-loss.md`.
Current phase: 5 of 6, completed. No repository files changed in this phase (scratch fixtures and
test script only). Modified files across the task so far:
`lua/neotex/plugins/ai/shared/extensions/loader.lua`,
`lua/neotex/plugins/ai/shared/extensions/init.lua`.
