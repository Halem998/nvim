# Phase 1 Handoff: Close the data-loss path in remove_installed_files()

**Immediate Next Action**: Begin Phase 2 — scratch-only verification of the removal fix. Build
Case A/B/regression fixtures under the scratchpad directory and prove the Case A assertion fails
pre-fix and passes post-fix.

**Current State**: Phase 1 completed. `loader.lua`'s `M.remove_installed_files` is now
symlink-aware: it skips any file-level symlink and any file reached through a symlinked ancestor
directory (bounded by `opts.project_dir`), returning `removed_count, skipped_count`. Plan file is
current (Phase 1 heading `[COMPLETED]`, all Phase 1 checklist items checked).

**Key Decisions Made**: None beyond what the plan already specified — implementation followed the
plan's Phase 1 tasks exactly (skip-and-count design, ownership invariant LuaDoc, no
`protected_paths` parameter added).

**Deviations from Plan**: None.

**What NOT to Try**: Do not add `protected_paths` to `remove_installed_files()` — that is
deliberately deferred to call-site filtering in Phase 3 per the plan's Scope section.

**References**: Plan at
`specs/862_fix_loader_symlink_delete_data_loss/plans/01_loader-symlink-delete-data-loss.md`.
Current phase: 1 of 6, completed. Modified file so far:
`lua/neotex/plugins/ai/shared/extensions/loader.lua`.
