# Phase 5 Handoff: Create Task Mode Step 6.5 advisory

**Immediate Next Action**: Begin Phase 4 (opt-in `--fix` repair for exact-duplicate entries) in
`agent-system/extensions/core/scripts/validate-state.sh` — the last phase before Phase 6.

**Current State**: Phase 5 completed. `agent-system/extensions/core/commands/task.md` now has a
Step 6.5 in Create Task Mode (between Step 6's state-write and Step 7's git commit) that runs the
deployed `validate-state.sh` base mode, filters to `[WARN]`+`file_scope` lines, and never blocks.
A one-line pointer was added to Step 8's output block for when warnings are present.

**Key Decisions Made**:
- Guarded via `if [[ -f .claude/scripts/validate-state.sh ]]` existence check (falls through to a
  note string) plus `|| true` on the grep pipeline, so neither a missing script nor a nonzero
  validate-state.sh exit can propagate.
- Confirmed by hand-execution: ~64ms against live `specs/state.json`, well under a second;
  missing-script simulation produces a note and success return.
- `git diff` confirmed the edit is scoped entirely to Create Task Mode (before `## Recover Mode`);
  no other mode's step numbering was touched. No task-number references introduced
  (`check-task-references.sh --quiet` passes).

**Deviations from Plan**: None.

**What NOT to Try**: Do not make Step 6.5 a second runtime gate — it must never affect Steps 7/8
regardless of what it finds.

**References**:
- Plan: `specs/061_surface_coarse_file_scope_declarations_at_creation/plans/01_coarse-file-scope-advisory.md`
- Current phase: 4
