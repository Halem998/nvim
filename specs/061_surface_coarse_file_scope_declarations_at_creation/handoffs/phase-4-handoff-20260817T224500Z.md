# Phase 4 Handoff: Opt-in `--fix` repair for exact-duplicate entries

**Immediate Next Action**: Begin Phase 6 (regression fixtures, full verification sweep, deploy) —
the last remaining phase.

**Current State**: Phase 4 completed. `--fix` is live in the source-store `validate-state.sh`:
resolves a deployed `state-write.sh` (D3 candidate order), refuses loudly (exit 2, naming every
candidate) when none resolves, refuses (exit 1) on unparseable JSON, applies an order-preserving
dedup filter to Class A duplicates only (Class B untouched, no `file_scope` field introduced on
entries that never had one), reports per-task removal counts or "nothing to repair", and falls
through into the normal Checks 1-9 (repair-then-revalidate).

**Key Decisions Made**:
- Verified success path requires the fixture's directory to resolve, via `git rev-parse
  --show-toplevel`, to a repo whose `.claude/scripts/state-write.sh` is actually deployed — a
  temp dir outside any git repo (or inside a repo with no deployed `.claude/`) correctly refuses
  instead. Tested both paths explicitly.
- `state-write.sh` itself was not modified; `--fix`'s repair filter is spliced entirely within
  `validate-state.sh`'s own jq program, passed to `state-write.sh` as the jq-filter argument.

**Deviations from Plan**: None.

**Observation — foreign uncommitted change (not mine, not reverted)**: while testing `--fix`
against live `specs/state.json`, discovered the file was ALREADY dirty (`git status --short`
showed `M specs/state.json`) from a concurrent session's edit to a different task's `description`
field — unrelated to `file_scope`. Confirmed via `git diff specs/state.json | grep -c file_scope`
= 0 that my `--fix` run (which reported "nothing to repair" and made no write) did not touch or
interact with it. Left untouched, per the observation-duty obligation to report rather than act
on work I did not do.

**What NOT to Try**: Do not add auto-repair for Class B — D2 is explicit that this is a permanent
non-goal (choosing which spelling survives is a judgment call).

**References**:
- Plan: `specs/061_surface_coarse_file_scope_declarations_at_creation/plans/01_coarse-file-scope-advisory.md`
- Current phase: 6
