# Phase 1 Handoff: Amend declared file_scope to cover the regression suite

**Immediate Next Action**: Begin Phase 2 (Check 8 — coarse blast-radius file_scope check) in
`agent-system/extensions/core/scripts/validate-state.sh`.

**Current State**: Phase 1 completed. Plan and progress file are current. Task 61's `file_scope`
in `specs/state.json` now has three entries (the original two plus
`agent-system/extensions/core/scripts/tests/test-validate-state.sh`), no duplicates.

**Key Decisions Made**:
- Used the deployed `bash .claude/scripts/state-write.sh` with an append-only jq filter
  (`(.file_scope) += [...]`), not a wholesale assignment.
- Confirmed `bash .claude/scripts/validate-state.sh --deep specs/state.json` reports a
  pre-existing, unrelated FAIL (`Unknown entry field: priority` on project_number 53) that
  predates this task's work (verified via `git stash` + re-run) — not introduced by Phase 1, and
  out of scope for this task.

**Deviations from Plan**: None.

**What NOT to Try**: Do not hand-edit `specs/state.json` directly for file_scope changes; always
route through `state-write.sh`.

**References**:
- Plan: `specs/061_surface_coarse_file_scope_declarations_at_creation/plans/01_coarse-file-scope-advisory.md`
- Current phase: 2
