# Phase 3 Handoff: Check 9 — duplicate file_scope entries

**Immediate Next Action**: Begin Phase 4 (opt-in `--fix` repair for exact-duplicate entries) in
`agent-system/extensions/core/scripts/validate-state.sh`.

**Current State**: Phase 3 completed. Check 9 is live in the source-store `validate-state.sh`,
WARN-only, two labelled classes (A exact, B normalization-equivalent). Not yet deployed (deploy
happens once more at Phase 6, though nothing stops an earlier deploy — Phase 2's deploy already
covers Check 8; Check 9 is source-store-only until the next deploy).

**Key Decisions Made**:
- Reused `norm` from the same spliced `$FILE_SCOPE_OVERLAP_JQ_DEFS` for Class B rather than
  writing a second local `rtrimstr("/")`.
- Class A via `group_by(.)`; Class B via a pairwise `range`-indexed comparison over
  `.file_scope | unique`.
- Verified against live `specs/state.json`: zero Class A, zero Class B, `log_pass` emitted.
- Verified against a hand-built fixture (`["docs/README.md","docs/README.md","src/foo/","src/foo"]`):
  both classes fire with distinct labels, overall exit 0.

**Deviations from Plan**: None for this phase.

**What NOT to Try**: Do not write Class B auto-repair — D2 is explicit that choosing which
spelling survives is a judgment call and must never be automated.

**References**:
- Plan: `specs/061_surface_coarse_file_scope_declarations_at_creation/plans/01_coarse-file-scope-advisory.md`
- Current phase: 4
