# Phase 1 Handoff: Apply the two-line fix to M.list_extensions

**Immediate Next Action**: Begin Phase 2 — add regression test coverage for both fixed behaviors.

**Current State**: Phase 1 completed. Plan file is current (all Phase 1 checklist items checked).

**Key Decisions Made**:
- Used `vim.fn.readdir(dir, predicate)`'s built-in filter-function form rather than a manual
  post-filter loop, per the research report's verified idiom and the user's "minimal code"
  framing.
- Used `vim.notify_once` (Neovim stdlib) rather than hand-rolled "warned" module state, for the
  same reason — zero new state introduced.

**Deviations from Plan**: None — both edits applied exactly as specified at the cited line
numbers, comments/behavior of surrounding code untouched.

**What NOT to Try**: Do not memoize/cache `list_extensions()` results as an alternative dedup
mechanism — this is an explicit NON-GOAL (would stale `manager.get_status()`'s live
update-available check).

**References**: Plan path
`specs/888_harden_extension_discovery_against_stray_dirs/plans/01_harden-extension-discovery.md`,
current phase 1 (complete), next phase 2.
