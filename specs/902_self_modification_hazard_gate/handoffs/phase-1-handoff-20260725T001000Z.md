# Phase 1 Handoff: Declare the Critical Set as Data

**Immediate Next Action**: Begin Phase 2 — implement the gate in
`agent-system/extensions/core/scripts/orchestrate-batch-admit.sh` (add `--invocation-count`
parsing, load and expand `orchestrator-critical-paths.json`, emit `self_modifying` on every
verdict, bump schema to v2, add `defer_reason`).

**Current State**: Phase 1 completed and committed (commit `147238557`, "task 902 phase 1:
declare self-modification critical-path set as data"). The plan file and progress file are
current. `agent-system/extensions/core/context/reference/orchestrator-critical-paths.json` now
exists with 9 critical paths and 3 scope roots.

**Key Decisions Made**:
- Followed D1 unchanged: 9 files, `scope_roots` = `agent-system/extensions/core`, `.claude`,
  `.opencode`.
- Placed the new "Self-Modification Hazard" guardrails section immediately after the
  Classification Table (thematically the natural spot).
- `check-extension-docs.sh` confirmed 5 pre-existing FAILs (all deploy-drift on files unrelated
  to this phase's edits) — no new failures introduced.

**Deviations from Plan**: None.

**What NOT to Try**: Do not add an `index-entries.json` entry for the new data file — it is a
script-consumed data file, not an agent-context-loaded document, and the plan's Phase 1 file list
does not include it. The `context/reference/` directory deploys wholesale per `manifest.json`, so
no manifest change was needed (confirmed, not assumed).

**References**: Plan at
`specs/902_self_modification_hazard_gate/plans/01_self-modification-hazard-gate.md`. Current
phase: 2.
