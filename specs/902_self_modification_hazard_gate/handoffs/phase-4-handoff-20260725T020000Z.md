# Phase 4 Handoff: Wire the Live Dispatch Paths and the Schema Document

**Immediate Next Action**: Begin Phase 5 — create the fixture and test suite under
`specs/902_self_modification_hazard_gate/`, then update
`specs/900_cross_batch_file_scope_admission/tests/test-batch-admit.sh`'s pinned exact-string
expectations for the v2 schema (this is anticipated, certain-to-fail work per the plan's own
Risks table), and check `specs/901_orchestrate_dry_run_admission_report/tests/` for disturbance.

**Current State**: Phases 1-4 completed. Both live call sites
(`commands/orchestrate.md` Step 3, `skills/skill-orchestrate/SKILL.md` Stage MT-3 step 4.5) pass
`--invocation-count` and branch on `defer_reason` before falling back to `collision_scope`.
`skills/skill-orchestrate/SKILL.md` Stage MT-1 initializes `deferred_self_modifying: []` in
`mt_state_file`; Stage MT-3 step 3 excludes it from eligibility; steps 2 and 4 recognize it as an
intentional exclusion (not "stuck"); Stage MT-5 reports it distinctly and folds it into the
`exit_status` determination (non-empty -> "partial", never silently "completed").
`skills/skill-orchestrate-hard/SKILL.md` confirmed via grep to need no edit (0 references to
`orchestrate-batch-admit`). `docs/architecture/batch-admit-schema.md` fully rewritten to v2 with
a Version History section.

**Key Decisions Made**:
- Two deviations beyond the literal Phase 4 task list, both recorded in the plan file and
  phase-4-progress.json: (1) MT-3 steps 2/4 now recognize `deferred_self_modifying`, and (2)
  MT-5's `exit_status` logic now treats a non-empty `deferred_self_modifying` set as "partial"
  even with zero `failed_tasks`. Both are necessary for the convergence property the plan itself
  demands (Hazard #1 in the delegation context) — without them the loop either misreports a
  stuck-task warning for a deliberate exclusion, or silently reports "completed" when a task was
  actually excluded pending solo re-run.
- Reused the literal checklist item's `defer_reason == "self_modifying"` value (the task text in
  one place said `"self_modified"`, which does not match the actual script-emitted value or D2's
  own definition — implemented the correct value, noted as a plan-typo correction, not a
  deviation).

**Deviations from Plan**: See the two items above, both recorded inline in the Phase 4 section
of the plan file and in `progress/phase-4-progress.json`.

**What NOT to Try**: Do not assume `skill-orchestrate-hard/SKILL.md` needs no changes for ANY
future gate — re-run the grep before assuming inheritance-by-reference still holds; this was
verified for THIS gate at this point in time only.

**References**: Plan at
`specs/902_self_modification_hazard_gate/plans/01_self-modification-hazard-gate.md`. Current
phase: 5. Test targets: `specs/900_cross_batch_file_scope_admission/tests/test-batch-admit.sh`,
`specs/901_orchestrate_dry_run_admission_report/tests/test-dry-run-report.sh`,
`specs/901_orchestrate_dry_run_admission_report/tests/test-triage-classify.sh`.
