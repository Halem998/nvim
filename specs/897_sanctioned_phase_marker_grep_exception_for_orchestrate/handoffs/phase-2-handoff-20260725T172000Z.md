# Phase 2 Handoff

**Immediate Next Action**: Begin Phase 3 — edit
`agent-system/extensions/core/docs/architecture/handoff-schema.md` (replace the unqualified
"NEVER reads... plan files" paragraph with the accurate three-exception statement) and
`agent-system/extensions/core/docs/architecture/orchestrate-state-machine.md` (document the two
additive loop-guard fields).

**Current State**: Phases 1-2 completed. Plan and progress files are current. Hard skill
`skill-orchestrate-hard/SKILL.md` now has the rewritten Read-allowlist category 3 and the
adapted recovery grep block in Stage 5's missing/stale-handoff branch.

**Key Decisions Made**:
- Lowercased "Phase-marker" to "phase-marker" in the Stage 5 recovery block's leading comment
  header in the hard file only, so the case-sensitive verification grep for
  "phase-marker recovery grep" matches both the allowlist prose and the Stage 5 comment.

**Deviations from Plan**:
- Phase 2 verification expected `grep -c "hard-orchestrate\] RECOVERY"` to return 4; actual is 3,
  matching the base skill's own 3 RECOVERY log lines (the block Phase 2 explicitly copies
  verbatim except for two named adaptations). Documented in
  `progress/phase-2-progress.json` and inline in the plan.

**What NOT to Try**: Do not add a `MUST NOT` heading to the hard skill file — its constraint
block is `## Tool Constraints (Pure Dispatcher)`, deliberately kept as the sole source of truth.

**References**:
- Plan: `specs/897_sanctioned_phase_marker_grep_exception_for_orchestrate/plans/01_phase-marker-grep-recovery.md`
- Current phase: 2 (complete) — next is Phase 3
