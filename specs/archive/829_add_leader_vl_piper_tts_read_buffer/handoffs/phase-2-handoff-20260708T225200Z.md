# Phase 2 Handoff: Task #829

**Immediate Next Action**: Begin Phase 3 — register `<leader>vl` in the `<leader>v` voice group
in `lua/neotex/plugins/editor/which-key.lua` and broaden the group's section comment to
"(STT + TTS)".

**Current State**: Phase 2 completed. `lua/neotex/util/tts.lua` now has the full playback
engine: `ensure_cleanup()`, `M.start()`, `M.stop()`, `M.toggle()`. Verified with real
`piper | play` runs (not stubs): empty-buffer guard prevents spawn, mid-playback `stop()` kills
both processes immediately, and `:qa` mid-playback triggers the lazy `VimLeavePre` cleanup with
no orphaned `piper`/`play` processes. Plan file Phase 2 checkboxes are all checked.

**Key Decisions Made**:
- `start()` checks `piper` existence, then model readability, then calls
  `resolve_player(sample_rate)` — sample rate must be read from the `.onnx.json` sidecar before
  a player command string can be formatted, so player resolution happens last.
- Cleanup augroup (`TTSPlayback`) is registered lazily inside `start()`, guarded by
  `cleanup_registered`, per the plan's explicit fix for the `sleep-inhibit.lua`
  unwired-`setup()` gap.

**Deviations from Plan**:
- `M.start()` internal ordering altered (see Phase 2 checklist annotation) — functionally
  equivalent to the plan, no external behavior change.

**What NOT to Try**: N/A — no failed approaches this phase. Real audio playback tests (not
mocks) were used throughout since the environment has piper/play installed and verified.

**References**:
- Plan: `specs/829_add_leader_vl_piper_tts_read_buffer/plans/01_piper-tts-toggle.md`
- Current phase: 2 of 3 (completed)
- Module: `lua/neotex/util/tts.lua`
