# Phase 1 Handoff: Task #829

**Immediate Next Action**: Begin Phase 2 (playback engine) — implement `M.start()`, `M.stop()`,
`M.toggle()`, and `ensure_cleanup()` in `lua/neotex/util/tts.lua`.

**Current State**: Phase 1 completed. `lua/neotex/util/tts.lua` created with module state
(`job_id`, `cleanup_registered`), config locals, `notify`, `command_exists`, `get_sample_rate`,
`get_text`, `resolve_player`, `M.is_playing()`, and `M.health()`. Plan file checkboxes for Phase 1
are all checked.

**Key Decisions Made**:
- `resolve_player(sample_rate)` takes a sample-rate parameter (deviation from the plan's
  no-arg signature) so it can directly emit the `play -r <rate> ...` / `pw-play --rate=<rate> ...`
  command string, avoiding a second string-formatting step in `start()`.
- `health()` checks player existence via `command_exists` directly rather than calling
  `resolve_player`, since it only needs presence/absence, not a formatted command.

**Deviations from Plan**:
- `resolve_player()` signature altered to accept `sample_rate` (annotated inline in plan
  Phase 1 checklist).

**What NOT to Try**: N/A — no failed approaches this phase.

**References**:
- Plan: `specs/829_add_leader_vl_piper_tts_read_buffer/plans/01_piper-tts-toggle.md`
- Current phase: 1 of 3 (completed)
- Module: `lua/neotex/util/tts.lua`
