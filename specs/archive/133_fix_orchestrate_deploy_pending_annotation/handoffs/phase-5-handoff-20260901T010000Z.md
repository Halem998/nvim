# Handoff: Task 133 -- Phases 1-4 Complete, Phase 5 Blocked (Partial)

## Immediate Next Action

Once a deploy has occurred and `.claude/scripts/system-defect-record.sh` contains
`AMBIENT_BINDING_MISMATCH` (verify: `grep -c AMBIENT_BINDING_MISMATCH
.claude/scripts/system-defect-record.sh` is non-zero), run:

```
bash .claude/scripts/system-defect-record.sh --defect-class AMBIENT_BINDING_MISMATCH \
  --detecting-site "skill-base.sh:skill_postflight_update exit-6 block" \
  --message "exit-6 deploy-pending annotation guard read an ambient TASK_DIR that the /orchestrate caller never set, so the guard silently evaluated false and the documented deploy_pending marker never landed" \
  --attributed-path .claude/scripts/skill-base.sh --task 133
```

Then verify: `tail -1 specs/events.jsonl | jq .` parses and names `AMBIENT_BINDING_MISMATCH`.
That completes Phase 5 and the task.

## Current State

Phases 1-4 are complete, verified, and committed:
- Phase 1 (`02dc0d780`): `AMBIENT_BINDING_MISMATCH` registered in
  `system-defect-record.sh`'s case validator/usage listing and
  `system-defect-discrimination.md`'s Signal A table + narrating paragraph. Zero "thirteen"
  occurrences remain in the recorder.
- Phase 2 (`e616d209e`): `skill_postflight_update` in `skill-base.sh` accepts an optional 6th
  `task_dir_override` argument, substituted into the exit-6 block's four `${TASK_DIR:-}`
  references only.
- Phase 3 (`b373f5c6f`): all three `orchestrate-stage5-postflight.sh` call sites (research, plan,
  implement) now pass `"$task_dir"` as the 6th argument. Zero literal `TASK_DIR` references
  remain in that file.
- Phase 4 (`57ecf75c0`): `test-skill-base-lifecycle.sh` gained a harness sanity check (exits 2 on
  a stale sourced `skill-base.sh`) plus 3 new Group 4 cases (primary, regression-guard,
  non-blocking) exercising the real exit-6 annotation path. A throwaway scratchpad harness
  sourcing the source-store copy ran the full suite including the new cases: **28 passed, 0
  failed**. The deployed-tree run of the real suite correctly exits 2 (the new sanity check
  correctly identifying the deployed copy as stale) -- this is expected, not a suite failure.

**Phase 5 is `[PARTIAL]`, not completed**: recording the motivating incident to
`specs/events.jsonl` via `system-defect-record.sh --defect-class AMBIENT_BINDING_MISMATCH ...`
is structurally blocked. The recorder sources `deploy-root-guard.sh`, which requires the
invoking script to physically be the DEPLOYED copy (`.claude/scripts/system-defect-record.sh`);
running it from the source store exits 1 by design (the script's own header says "NOT RUNNABLE
FROM THE SOURCE STORE"). The deployed copy is currently stale and does not yet know the new
class. Closing this requires either a deploy or hand-authoring `.claude/**`, and this plan's own
Non-Goals explicitly forbid deploying, while `.claude/rules/source-store-deploy-boundary.md`
repo-wide forbids hand-authoring `.claude/**`. Neither prohibition was mine to waive. Full
investigation (including a considered-and-rejected fake-`.claude`-directory workaround) is
recorded in `progress/phase-5-progress.json`'s `approaches_tried`.

This is, notably, the exact self-referential scenario the plan's own Risks table names: "This
task's own postflight is refused by the completion-deploy gate, since its `modified_files`
overlap `agent-system/extensions/**`" -- and Part 2 of this very task means that refusal will now
correctly land `deploy_pending: true` in `.return-meta.json` instead of silently deferring, which
is worth checking for on this task's own next postflight report as a live confirmation of the fix.

## Key Decisions Made

- Phase 1: class name `AMBIENT_BINDING_MISMATCH` used verbatim per plan.
- Phase 2/3: `_task_dir` local, `${6:-${TASK_DIR:-}}` default, `skill_run_extension_hook`'s own
  `${TASK_DIR:-}` argument left untouched per CONTRACT 4.
- Phase 4: `write_deploy_gate_return_meta` needed an added `mkdir -p` for the fixture task
  directory (see progress file's `approaches_tried` for the exit-3-vs-exit-6 debugging story).
- Phase 5: closed as `[PARTIAL]` rather than `[COMPLETED WITH EXCLUSIONS]`, because there IS
  concrete residual work (a single follow-up command once deployed) -- this fails admission
  condition 5 ("no residual work") for the exclusions marker, matching `PARTIAL`'s "deferred with
  a tracked follow-up" semantics instead.

## Deviations from Plan

- Phase 1: the plan's `--help exits 0` verification bullet does not match this script's
  pre-existing (unrelated) `usage()` behavior, which always exits 1. Verified via other means
  instead -- see phase-1-progress.json.
- Phase 5: three task items deferred (recorder invocation and its two verification checks) --
  see the plan's own "Phase 5 blocker" note and progress/phase-5-progress.json's `deviations`.

## What NOT to Try

- Do not deploy or regenerate `.claude/` to force Phase 5 through (violates this plan's Non-Goals).
- Do not hand-author `.claude/scripts/system-defect-record.sh` or hand-edit `specs/events.jsonl`
  to fake the recording (both explicitly prohibited).
- Do not construct a fake `.claude/scripts/` directory elsewhere to dodge `deploy-root-guard.sh`
  -- it structurally cannot resolve PROJECT_ROOT to the real repo root from anywhere but the
  real deployed location (see progress file for why).
- Do not modify `test-postflight-deploy-gate.sh`.
- Do not reorder deploy-first candidate resolution in `test-skill-base-lifecycle.sh` itself
  (only a throwaway harness copy, kept out of the repo, may invert it).

## References

- Plan: `specs/133_fix_orchestrate_deploy_pending_annotation/plans/01_fix-deploy-pending-annotation.md`
- Progress files: `specs/133_fix_orchestrate_deploy_pending_annotation/progress/phase-{1,2,3,4,5}-progress.json`
- Follow-up command: `progress/phase-5-progress.json`'s `follow_up_command` field
- Current phase: 5 (partial); phases 1-4 complete
