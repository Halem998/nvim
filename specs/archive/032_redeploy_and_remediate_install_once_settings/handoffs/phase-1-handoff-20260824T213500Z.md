# Handoff: Task #32, after Phase 1

## Immediate Next Action

Begin Phase 2 (Run the Deploy): `bash .claude/scripts/deploy-headless.sh`, capturing full
stdout+stderr to `deploy-output.txt`.

## Current State

Phase 1 (Pre-Deploy Snapshot and Baseline Materialization) is `[COMPLETED]`. Plan and progress
file are current. Committed at `cdbbd796b`. Working tree was clean at Phase 1 start (preflight
commits already landed at HEAD `50ee61342`), so `git-snapshot.sh --no-revert 32` reported
"nothing to snapshot" — that commit is the rollback point.

Artifacts produced: `pre-deploy-findings.txt` (52 lines), `pre-deploy-findings.normalized.txt`
(45 FINDING lines), `changed-source-files.txt` (16 files), `settings.json.bak` (backup of
deployed `.claude/settings.json` before the Phase 4 hand-edit).

All three preconditions confirmed live: lean-lsp grant at `.claude/settings.json:188`, source
`root-files/settings.json` clean (grep -c returns 0), ambient-increment form present at
`.claude/scripts/skill-base.sh:958`.

## Key Decisions Made

- Used research report lines 195-246 (not 195-247 as the plan text states) for the verbatim
  baseline transcription — line 247 is the closing markdown fence, not content. Confirmed correct
  by matching the report's own stated "52 lines" claim exactly.

## Deviations from Plan

- Task 1.2 (baseline extraction): altered line range 195-246 vs plan's stated 195-247, per Key
  Decisions above.
- Scope Hypothesis mismatch: normalized baseline has 45 FINDING lines, not the plan's hypothesized
  28. Not a blocker — recorded in the plan's Scope Hypothesis section as an estimate error, not
  source drift (verbatim transcription of the same report text).

## What NOT to Try

- Do not re-run `verify-deploy.sh` before Phase 2's deploy completes — this would destroy the
  pre-deploy baseline comparison.
- Do not re-derive `pre-deploy-findings.normalized.txt` from anywhere other than the already-
  committed `pre-deploy-findings.txt` — it is frozen input for Phase 5.

## References

- Plan: `specs/032_redeploy_and_remediate_install_once_settings/plans/01_deploy-remediate-stale-grant.md`
- Current phase: 1 (complete); next: 2
- Progress file: `specs/032_redeploy_and_remediate_install_once_settings/progress/phase-1-progress.json`
