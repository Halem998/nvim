# Handoff: Task #32, after Phases 2, 3, 4

## Immediate Next Action

Begin Phase 5 (Baseline-Relative verify-deploy Comparison): run
`bash .claude/scripts/verify-deploy.sh --findings --quiet`, capture to
`post-deploy-findings.txt`, normalize, and compute the regression set via
`comm -13 pre-deploy-findings.normalized.txt post-deploy-findings.normalized.txt`. This is
expected to take several minutes (invokes run-all.sh internally) — allow for it.

## Current State

Phases 1-4 all `[COMPLETED]`, committed through `c946b0e7c`. Deploy ran clean (exit 0, all 16
changed source files reconcile byte-identical to deployed counterparts). The mint-dispatch-seq
fix is confirmed live (test suite: 14/14 passed, including previously-failing Cases B/C/D/E/F).
The stale `mcp__lean-lsp__*` grant has been removed from the deployed `.claude/settings.json`
(23 elements, was 24; JSON valid; source copy untouched).

## Key Decisions Made

- Phase 4's hand-edit was performed via the Edit tool (not Bash `cp`) specifically so the
  `validate-meta-write.sh` advisory hook had a chance to fire, as the plan expected. Finding: the
  hook's `is_meta_path` case list does not cover root-level `.claude/` files (only
  `commands/skills/agents/rules/context/extensions/scripts/hooks` subdirectories and
  `*/CLAUDE.md`), so no warning was actually raised for this edit — a hook coverage gap, not an
  ignored warning. The install-once boundary-exception reasoning is recorded regardless (Phase 4
  checklist annotations, `phase4-checks.txt`) and will carry into the Phase 6 summary and
  `CHANGE_LOG.md`.
- `.claude/settings.json`'s raw key/array ordering differs from the Phase 1 backup even before
  the Phase 4 edit (semantically identical per `jq -S` diff) — attributed to the Claude Code
  harness itself rewriting the file independent of the deploy, not a deploy-mechanism concern.

## Deviations from Plan

- None beyond what's already recorded in each phase's progress file `deviations` array (Phase 1
  only: baseline line-range correction, Scope Hypothesis count correction).

## What NOT to Try

- Do not re-derive `pre-deploy-findings.normalized.txt` — it is frozen Phase 1 input.
- Do not edit `agent-system/extensions/core/root-files/settings.json` under any circumstance —
  already confirmed correct.
- Do not treat a non-empty post-deploy findings set as failure in Phase 5 — only the regression
  set (`comm -13`) gates that phase.

## References

- Plan: `specs/032_redeploy_and_remediate_install_once_settings/plans/01_deploy-remediate-stale-grant.md`
- Phases complete: 1, 2, 3, 4; next: 5
- `specs/032_redeploy_and_remediate_install_once_settings/pre-deploy-findings.normalized.txt` (frozen input for Phase 5)
