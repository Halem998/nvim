# Handoff: Task #983, Phase 9/11 Checkpoint

## Immediate Next Action

Resume at **Phase 10: Convert the remaining marker writers** (`[NOT STARTED]` in the plan). Start
by re-running the phase's Scope Hypothesis probe:
```bash
grep -rl "postflight-pending" agent-system/extensions/*/skills/*/SKILL.md
```
minus every file already converted across Phases 4-9 (see the plan's Phase 10 "Files to modify"
list — `skill-spawn`, `skill-reviser`, `skill-financial-analysis`, the cslib `-hard` skills,
`skill-pr-implementation`, the two `touch`-only cslib pr-review skills — plus whatever the fresh
grep additionally surfaces). `skill-financial-analysis` (`agent-system/extensions/founder/skills/skill-financial-analysis/SKILL.md`)
was deliberately left unconverted in Phase 9 specifically so Phase 10 would do it — it already
uses Shape-A-shaped fields (`stop_hook_active` present) unlike the rest of the Phase 9 family.
`skill-refresh` is a documented Reasoned-Exclusions candidate (read/delete-only, never a writer) —
confirm with its own `find ... -delete` / `rm -f` call sites before excluding.

## Current State

Phases 1-9 of 11 are `[COMPLETED]` in the plan file
(`specs/983_extract_shared_skill_stage_skeleton/plans/01_shared-skill-stage-skeleton.md`) and
committed, one commit per phase, through `task 983 phase 9: convert the founder/present marker
family`.

Phase 9 converted 19 founder/present skills (all of the measured 20-file Shape B family except
`skill-financial-analysis`, reserved for Phase 10) plus added `## MUST NOT (Postflight Boundary)`
sections to 18 of them. Two files (`skill-slides`, `skill-grant`) got marker+cleanup conversion
only — their genuinely conditional per-workflow-type Stage 2 preflight logic (including
`skill-grant`'s true no-status-change `progress_track`/`fix_it_scan` branches) was deliberately
left untouched to avoid domain-body restructuring.

`bash .claude/scripts/deploy-headless.sh && bash .claude/scripts/verify-deploy.sh` passes all 9
gates except the shell-test-suite runner gate, which flaked on `test-four-tier-conflict.sh` on
the first run and passed 31/31 on immediate retry — the same known, pre-existing, load-sensitive
flake documented in the Phase 8 handoff. `lint-postflight-boundary.sh` now passes across the FULL
corpus (not just the Phase 9 file set), confirming Phases 1-9 introduced no regressions.

Working tree is clean except pre-existing, task-983-unrelated modifications
(`.claude-extensions.json`, `lua/neotex/config/options.lua`,
`lua/neotex/plugins/editor/which-key.lua`, `specs/events.jsonl`) — do not touch these.

## Key Decisions Made

- **Measured set was 20 files, not the plan's 15** (Phase 9): `grep -rl "postflight-pending"
  agent-system/extensions/{founder,present}/skills/*/SKILL.md` returned 20 hits. `skill-web-research`
  (named in the plan's hypothesis list) was already converted in Phase 8, as the plan anticipated.
  `skill-funds` was NOT named in the plan's file list at all but was part of the measured set and
  was converted. `skill-financial-analysis` was excluded and left for Phase 10, since Phase 10's
  own plan text explicitly names it in that phase's file list.
- **Operation remapping continues the Phase 4-8 pattern**: `slides_critique` -> `research`
  (skill-slide-critic), `funds_analysis` -> `research` (skill-funds), `timeline_research`/
  `timeline_plan` -> `research`/`plan` via a case statement (skill-timeline). The marker's
  `operation` field now carries the mapped value in all three cases.
- **Conditional multi-workflow skills get a narrower conversion** (skill-slides, skill-grant):
  only the marker heredoc and the cleanup `rm -f` block move onto shared functions; the Stage 2
  status-transition case statement (including skill-grant's two genuine no-status-change branches
  and skill-slides' latent missing-`plan`-case bug) is left exactly as it was. Converting it would
  have required inventing new semantics not specified by the plan (either forcing a status
  transition where none was intended, or silently fixing a pre-existing bug) — both are
  domain-body restructuring, which Phase 9's task list explicitly rules out.
- **skill-timeline WAS fully converted** despite having two workflow types, because both branches
  (`timeline_research`/`timeline_plan`) map cleanly and losslessly to `research`/`plan` with no
  no-op case and no missing-case bug — the risk profile that kept skill-slides/skill-grant
  narrow doesn't apply here.
- **No TTS conversion needed**: none of the 20 Shape B files had an inline TTS/notify stage before
  conversion (verified via grep across all 20 files), so that leg of the phase's task list was a
  true no-op, not a skipped step.
- **MUST NOT section content**: three template variants were used (research-only, plan-only,
  mixed-workflow) matched to each file's operation type, following the wording pattern already
  established in Phase 7/8's `skill-nix-research`/`skill-epi-implement` conversions.

## Deviations From Plan (Phase 9 only; see progress/phase-9-progress.json's `deviations` array
for the machine-readable form, and phases 1-8's cumulative deviations in the Phase 8 handoff)

- Measured 20-file set vs. plan's 15-file hypothesis (see Key Decisions above).
- skill-slides and skill-grant converted marker+cleanup only, not the full Stage 2+3 merge.

## What NOT to Try

- Do not re-derive the marker-schema shape (Shape A) — settled in `skill-base.sh`'s
  `skill_create_postflight_marker`, asserted by `test-postflight-marker-schema.sh`.
- Do not attempt to "fix" skill-slides' missing `plan`-workflow-type case in its Stage 2 case
  statement as part of this task — that's a pre-existing bug outside this task's scope; converting
  it to `skill_preflight_update` would silently change that bug's behavior, which is exactly what
  Phase 9's "do not restructure domain bodies" instruction rules out.
- Do not convert skill-grant's `progress_track`/`fix_it_scan` no-status-change cases to a forced
  `skill_preflight_update` call — that would change real behavior (introducing a status
  transition that currently never happens for those two workflow types).
- Do not re-convert `skill-financial-analysis` in a way that duplicates Phase 10's planned work —
  it was deliberately left inline in Phase 9 specifically because Phase 10 already names it.
- Do not chase `test-four-tier-conflict.sh` flakiness — confirmed pre-existing and unrelated,
  passes on retry.

## Critical Context

- **Binding constraints** (unchanged): edit `agent-system/extensions/**` only, never `.claude/**`;
  run `deploy-headless.sh && verify-deploy.sh` before trusting any phase's verification; no
  task-number references in any deliverable outside `specs/**`; commit at every green phase.
- **Remaining phases**: Phase 10 (remaining marker writers, ~7-8 files) depends on 3, 4 (both
  done) — no blocker. Phase 11 (skill-lifecycle.md rewrite + full verification bar) depends on
  1-10 and must be last.
- **Task-lock / session-registry heartbeat**: refresh at each phase transition if resuming under
  the same lock; best-effort, non-blocking.

## References

- Plan: `specs/983_extract_shared_skill_stage_skeleton/plans/01_shared-skill-stage-skeleton.md`
- Research report: `specs/983_extract_shared_skill_stage_skeleton/reports/01_shared-skill-stage-inventory.md`
- Progress files: `specs/983_extract_shared_skill_stage_skeleton/progress/phase-{1..9}-progress.json`
- Prior handoff: `specs/983_extract_shared_skill_stage_skeleton/handoffs/phase-8-handoff-20260808T000000Z.md`
- Shared blocks: `agent-system/extensions/core/context/patterns/skill-{preflight,postflight}-flow.md`,
  `agent-system/extensions/core/context/patterns/skill-self-execution-fallback.md`
- Phase 9 converted files: 19 under `agent-system/extensions/{founder,present}/skills/*/SKILL.md`
  (see progress/phase-9-progress.json objective 2's `files_touched` for the exact list).
