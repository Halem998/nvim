# Handoff: Task #983, Phase 8/11 Checkpoint

## Immediate Next Action

Resume at **Phase 9: Convert the founder/present marker family** (`[NOT STARTED]` in the plan).
Start by re-running the phase's Scope Hypothesis probe rather than trusting the plan-time list —
per-file heredoc field probe:
```bash
grep -rl "postflight-pending" agent-system/extensions/{founder,present}/skills/*/SKILL.md
```
Convert each measured Shape B writer's inline marker heredoc and inline cleanup block to the
Phase 3 shared blocks (`skill-preflight-flow.md`, `skill-postflight-flow.md`,
`skill-self-execution-fallback.md`), following the exact same pattern already applied in Phases
4-8 (see `agent-system/extensions/z3/skills/skill-z3-research/SKILL.md` or
`agent-system/extensions/epidemiology/skills/skill-epi-research/SKILL.md` as worked references —
epi-research's `operation` remapping note is directly relevant since several founder/present
skills likely have the same "operation string doesn't match update-task-status.sh vocabulary"
issue).

## Current State

Phases 1-8 of 11 are `[COMPLETED]` in the plan file
(`specs/983_extract_shared_skill_stage_skeleton/plans/01_shared-skill-stage-skeleton.md`) and
committed, one commit per phase:
- `task 983 phase 1: settle marker schema and complete skill-base.sh`
- `task 983 phase 2: restore the MUST NOT boundary and make the lint enforce presence`
- `task 983 phase 3: author the shared @-imported lifecycle blocks`
- `task 983 phase 4: convert the three core skills onto the shared skeleton`
- `task 983 phase 5: convert the three core hard skills and replace the prose cross-references`
- `task 983 phase 6: route the team skills through update-task-status.sh and give them a real fallback`
- `task 983 phase 7: collapse the nix/nvim and latex/typst domain pairs`
- `task 983 phase 8: collapse the remaining domain skills`

Working tree is clean except pre-existing, task-983-unrelated modifications
(`.claude-extensions.json`, `lua/neotex/config/options.lua`,
`lua/neotex/plugins/editor/which-key.lua`, `specs/events.jsonl`) — do not touch these, per the
binding constraints of this task.

`bash .claude/scripts/deploy-headless.sh && bash .claude/scripts/verify-deploy.sh` was confirmed
passing 20/20 at the end of every phase in this dispatch. Gate 8 (`tests/run-all.sh`)
intermittently reports `test-four-tier-conflict.sh` failing — this is a confirmed, pre-existing,
load-sensitive flake in a `task-lock.sh` `/tmp`-fixture concurrency test, unrelated to any file
this task touches. Retry 1-2 times if seen; do not spend effort investigating it further.

## Key Decisions Made

- **Rule S discovery (Phase 3)**: `context/patterns/*.md` files @-imported directly from a skill
  body still MUST be registered in `agent-system/extensions/core/index-entries.json` —
  `check-extension-docs.sh`'s Rule S enforces this regardless of whether the file is reached via
  context-discovery. All three Phase 3 shared blocks are registered there.
- **Team-skill operation mapping (Phase 6)**: `update-task-status.sh`'s `target_status` vocabulary
  has no per-family value (`team-research`, `epi_research`, etc.) — every converted skill maps
  onto the plain `research`/`plan`/`implement` operation, and the marker's `operation` field
  follows suit. Apply the same mapping to any founder/present skill with a non-standard operation
  string.
- **Marker-fix scope was broader than the plan's premise (Phase 7)**: ALL 8 nix/nvim/latex/typst
  domain skills lacked a marker (not just the 2 implementation skills the plan named) — the
  measured set governs. The same "measured set governs" principle applies to Phase 9's founder/
  present enumeration: use the grep, not the plan's 15-file list, as authoritative.
- **skill-team-plan's Stage 5b collision (Phase 6)**: the plan named this collision for
  skill-team-plan only; skill-team-research turned out to have the SAME collision
  ("Task Type Routing Decision" at Stage 5b), not named in the plan. Both were resolved by placing
  the self-execution-fallback import at a new non-colliding Stage 4c. Watch for similar
  undocumented collisions in founder/present skills.

## Deviations From Plan (cumulative, phases 1-8)

- Phase 2: `does_skill_delegate()` predicate tightened; scope grew from 5 to 8 files
  (3 additional: skill-email-implementation, skill-nix-research, skill-neovim-research).
- Phase 3: the three shared blocks WERE registered in `index-entries.json`, contradicting the
  plan's stated "follow lit-stage4a-flow.md's not-registered precedent" (that precedent was
  factually wrong — lit-stage4a-flow.md IS registered).
- Phase 4: skill-planner's Stage 9 (Git Commit) interleaves between Stage 8a and cleanup; the
  postflight-flow.md import was split at existing stage boundaries rather than imported
  contiguously.
- Phase 6: team skills' Stage 4a self-execution fallback now invokes the underlying subagent
  directly (Agent tool) rather than wholesale-redelegating to the whole wrapping skill — a real
  behavior fix, not just a text change.
- Phase 7: marker-fix scope was all 8 domain skills, not the 2 the plan named (see above).
- No phase required a `#### Reasoned Exclusions` entry — every task-status call site the plan
  anticipated mapped cleanly.

Full per-phase deviation detail is recorded inline in the plan file's per-phase Deviation notes
and in each phase's `specs/983_extract_shared_skill_stage_skeleton/progress/phase-N-progress.json`.

## What NOT to Try

- Do not re-derive the marker-schema shape (Shape A) — it is settled in `skill-base.sh`'s
  `skill_create_postflight_marker` and asserted exactly by
  `test-postflight-marker-schema.sh`. Any new marker writer converts onto it via
  `skill_create_postflight_marker`, never a hand-rolled shape.
- Do not chase `test-four-tier-conflict.sh` flakiness (see Current State above) — it is
  unrelated to this task's file set.
- Do not attempt to register `context/patterns/*.md` blocks as "not needing an index entry" —
  Rule S requires it (see Key Decisions above).
- Do not stage the entire working tree on commit — always use
  `.claude/scripts/git-commit-scoped.sh` with an explicit path list (task dir + the specific
  converted files), matching every phase commit so far.

## Critical Context

- **Binding constraints** (unchanged from this task's original dispatch): edit
  `agent-system/extensions/**` only, never `.claude/**`; run
  `deploy-headless.sh && verify-deploy.sh` before trusting any phase's verification; no
  task-number references in any deliverable outside `specs/**`; commit at every green phase.
- **Remaining phases and their dependency chain** (per the plan's Dependency Analysis table):
  Phase 9 depends on 3, 4 (both done) — no blocker. Phase 10 depends on 3, 4 (both done) — no
  blocker; Phase 10 also explicitly permits a `#### Reasoned Exclusions` entry for
  `skill-refresh` (a read/delete-only false positive, not a true marker writer — do not convert
  it). Phase 11 depends on 1-10 — must be last, and is the only phase carrying the task's full
  four-item verification bar (zero-heredoc grep, marker-schema test, lint-both-polarities test,
  and a live `/research --lit` Stage-4a-reached smoke check).
- **Task-lock heartbeat / session registry heartbeat**: per this agent's own operating contract,
  refresh `task-lock.sh heartbeat` and `task-lock.sh session-heartbeat` at each phase transition
  if resuming under the same lock; best-effort, non-blocking.

## References

- Plan: `specs/983_extract_shared_skill_stage_skeleton/plans/01_shared-skill-stage-skeleton.md`
- Research report: `specs/983_extract_shared_skill_stage_skeleton/reports/01_shared-skill-stage-inventory.md`
- Progress files: `specs/983_extract_shared_skill_stage_skeleton/progress/phase-{1..8}-progress.json`
- Shared blocks: `agent-system/extensions/core/context/patterns/skill-{preflight,postflight}-flow.md`,
  `agent-system/extensions/core/context/patterns/skill-self-execution-fallback.md`
- Worked references for Phase 9's conversion pattern: any Phase 7/8 file, e.g.
  `agent-system/extensions/epidemiology/skills/skill-epi-research/SKILL.md` (operation-remapping
  case) or `agent-system/extensions/web/skills/skill-web-implementation/SKILL.md` (0/1-4/5/6 to
  Stage-N renumbering case, if any founder/present skill uses that older layout).
