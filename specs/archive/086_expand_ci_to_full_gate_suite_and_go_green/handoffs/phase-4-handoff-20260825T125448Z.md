# Phase 4 Handoff — Task 86

## Immediate Next Action
Start Phase 5: rewrite .github/workflows/check-extension-docs.yml to deploy-then-verify.

## Current State
Phases 1-4 completed. Plan and progress files are current.
- Phase 4: `--minimal-init DIR` opt-in flag added to both deploy-headless.sh and verify-deploy.sh (threaded from deploy-headless.sh's inline verify-deploy.sh call). Confirmed end-to-end via a real deploy-headless.sh run against a scratch clone: deploy landed, gate5 (verify_all), gate13 (find_orphans), and gate14 all PASS under the hatch; only the pre-existing gate3 doc-lint (Rule R, deferred to Phase 7) failed, matching the working repo's own state. Confirmed unset/default behavior is byte-for-byte unchanged (0-line findings diff vs Phase 3 baseline).

## Key Decisions Made
- Flag name: `--minimal-init DIR` (not `--clean` alone) to avoid confusion with nvim's own `--clean` flag; DIR is a required argument, validated to exist.
- deploy-headless.sh threads `--minimal-init "$MINIMAL_INIT_DIR"` into its own inline verify-deploy.sh call automatically when set.
- Implementation uses a `NVIM_ARGS` bash array (`(nvim --headless)` or `(nvim --headless --clean --cmd "set rtp+=DIR")`) rather than string interpolation, for correct quoting.

## Deviations from Plan
- None.

## What NOT to Try
- Inline nvim `-c "lua ..."` with embedded single-quoted paths containing hyphens caused an E5107 Lua parse error during probing; write the lua to a temp file and use `-c luafile <path>` instead for any future ad hoc nvim headless probes.
- Do not fix Rule R line_count mismatches yet (deferred to Phase 7).

## References
- Plan: specs/086_expand_ci_to_full_gate_suite_and_go_green/plans/01_expand-ci-full-gate-suite.md
- Current phase: 5 (next)
