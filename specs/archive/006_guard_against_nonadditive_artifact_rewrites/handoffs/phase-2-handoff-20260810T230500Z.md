# Phase-End Handoff: Phase 2 Complete

**Immediate Next Action**: Begin Phase 4 (State the Append-Only Rule and Add the Agent Contract)
and/or Phase 3 (Bidirectional Fixture Tests) -- both are in Wave 2/3 per the plan's dependency
table and depend only on Phase 1/2, which are both now complete.

**Current State**: Phase 2 completed. `agent-system/extensions/core/scripts/validate-state.sh`
now has the D5 per-type artifact-loss check (FAIL-level, per Phase 1's decision) and the
repeatable `--allow-artifact-removal <project_number>[:<type>]` CLI flag. `bash -n` passes,
`--help` output verified complete and untruncated after the sed range bump (2,57p -> 2,74p), and
the malformed-flag hard-error path (`--allow-artifact-removal notanumber`, `--allow-artifact-removal 6:`)
both confirmed to exit 2.

**Key Decisions Made**:
- D5 reuses D4's already-fetched `prior_json` — no second `git show`.
- Untyped entries group under the sentinel key `(untyped)`.
- Project numbers present only in `prior_json` (archived) are skipped.
- Suppression via `--allow-artifact-removal` always emits a `log_warn` line naming the opt-in —
  never silent.
- Confirmed the only two real invocation consumers are `verify-deploy.sh` gate 10 and
  `scripts/tests/test-validate-state.sh`; `manifest.json`'s hit is a deploy-file declaration, not
  an invocation.
- Running the source-store copy of `validate-state.sh` directly (not the deployed copy) trips a
  **pre-existing, unrelated** D2 (TODO.md sync) failure, because `generate-todo.sh` refuses to run
  from the source-store tree. Confirmed via `git stash` that this failure exists on the
  unmodified script too — not caused by this task's changes. Phase 5 will validate against the
  deployed copy, matching Phase 1's baseline invocation.

**Deviations from Plan**: None.

**What NOT to Try**: Do not "fix" the source-store D2 (TODO.md sync) failure — it is a pre-existing,
orthogonal limitation of `generate-todo.sh` when invoked outside a deployed tree, unrelated to
this task's declared file scope.

**References**: Plan at
`specs/006_guard_against_nonadditive_artifact_rewrites/plans/01_guard-nonadditive-artifact-rewrites.md`,
current phase: 3 or 4 (both unblocked).
