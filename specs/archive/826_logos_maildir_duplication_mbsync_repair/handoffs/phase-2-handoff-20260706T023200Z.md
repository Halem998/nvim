# Phase 2 Handoff: Apply mbsync.nix Config Fix

**Status**: COMPLETED
**Timestamp**: 2026-07-06T02:32:00Z

## Immediate Next Action

Begin Phase 3: Verify Labels-mirror redundancy (read-only deletion-safety gate) — extract
Message-Id sets from each `.Labels.*` folder and each canonical folder, compute the set
difference, and produce a verification report before any Phase 4 deletion.

## Current State

Phase 2 completed. `~/.dotfiles/modules/home/email/mbsync.nix` no longer includes
`logos-labels` in `Group logos` (channel definition retained for manual inspection only,
`logos-folders` unchanged). Verified end-to-end:
- `nix build .#homeConfigurations.benjamin.activationPackage` succeeded before switching.
- `home-manager switch --flake .#benjamin` applied cleanly (only warnings: dirty git tree from
  an unrelated pre-existing modification to `agent-tools/census.nix`, and a deprecated `nix
  profile install` alias warning — neither blocking).
- Live `~/.mbsyncrc` (`readlink -f` -> `/nix/store/2z8awni9m4cnlpfrmk31nd8lrzz79c3n-hm_.mbsyncrc`)
  confirmed: `Group logos` now lists exactly `logos-inbox, logos-sent, logos-drafts,
  logos-trash, logos-archive, logos-folders` — no `logos-labels`.
- Committed in `~/.dotfiles` as commit `a8f65ad` ("email: remove logos-labels from Group logos
  (nvim task 826)"), staged only `modules/home/email/mbsync.nix` (targeted staging; did not
  touch the unrelated pre-existing `census.nix` modification).

## Key Decisions Made

- Added explanatory comments in two places in `mbsync.nix`: above the `Channel logos-labels`
  definition (why it exists but isn't grouped) and above `Group logos` (why it's omitted),
  mirroring the two-comment pattern already used for `gmail-trash`/`gmail-spam`.
- Verified the config fix at three levels before considering it done: Nix build succeeds,
  home-manager switch applies without error, and the *live* generated `.mbsyncrc` (not just the
  Nix source) no longer lists `logos-labels` under `Group logos`.

## Deviations from Plan

- None. All Phase 2 checklist items completed as specified.

## What NOT to Try

- Do not run `mbsync logos` yet (still pending Phases 3-6 completion per the plan's Wave
  ordering) — the config fix alone stops further bloat but does not resolve the existing
  `.Trash`/`.Archive` duplicate-UID corruption or the headerless `.Sent` test message.
- Do not stage `agent-tools/census.nix` in `~/.dotfiles` — that modification is unrelated to
  this task and pre-existed before this session.

## References

- Plan: `specs/826_logos_maildir_duplication_mbsync_repair/plans/01_logos-mbsync-maildir-repair.md`
- Current phase: 2 of 8 (completed)
- Cross-repo commit: `~/.dotfiles` commit `a8f65ad`
