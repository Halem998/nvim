# Phase 4 Handoff: Preserve symlinks on the copy path

**Immediate Next Action**: Begin Phase 5 — scratch integration test of the full unload/load
cycle. This is the highest-risk remaining phase: it runs the real `manager.unload`/`manager.load`/
`manager.reload` functions, and every call MUST pass `opts.project_dir = <scratch>` explicitly
before the first invocation. Never call any manager function with a default `project_dir`.

**Current State**: Phase 4 completed. `loader.lua`'s `M.copy_simple_files` and `M.copy_skill_dirs`
now check `vim.fn.getftype(target) == "link"` before copying and skip entirely (recording nothing
in `copied_files`) when the deployed target is already a symlink, closing the silent-flattening
bug (Finding 4 in the research report). Both functions now return a 4th value,
`symlink_skipped_count`, distinct from the existing `.syncprotect`-only `skipped_count`. `init.lua`
threads this through `manager.load`'s copy sequence (`total_symlink_skipped`) and surfaces it in
the load notification. Plan file is current (Phase 4 heading `[COMPLETED]`, all checklist items
checked with scratch-test evidence).

**Key Decisions Made**: None beyond what the plan specified — implementation followed Phase 4's
tasks exactly (getftype check before copy, separate symlink-skip counter, LuaDoc mirroring
Phase 1's ownership-invariant wording, load notification extended).

**Deviations from Plan**: None.

**What NOT to Try**: Do not conflate `skipped_count` (`.syncprotect`) and the new
`symlink_skipped_count` — callers must capture both as separate return values; only 4 of the
copy_* functions (`copy_simple_files`, `copy_skill_dirs`) return this 4th value, the others
(`copy_context_dirs`, `copy_scripts`, etc.) are unchanged and still return 3 values, since Phase 4
scope is limited to agents/commands/rules/skills per the plan.

**References**: Plan at
`specs/862_fix_loader_symlink_delete_data_loss/plans/01_loader-symlink-delete-data-loss.md`.
Current phase: 4 of 6, completed. Modified files so far:
`lua/neotex/plugins/ai/shared/extensions/loader.lua`,
`lua/neotex/plugins/ai/shared/extensions/init.lua`.
