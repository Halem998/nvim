# Phase 2 Handoff

**Immediate Next Action**: Begin Phase 3 — replace `sentence_boundary_glue_count`'s body in
`agent-system/extensions/literature/scripts/literature-convert.sh` with the two-pass exemption
strip, and extend its docstring.

**Current State**: Phase 1 and Phase 2 are `[COMPLETED]`. Plan and progress files are current.
Fixture builders `build_biblio_quantifier_pdf` and `build_fused_word_pdf` are added to
`agent-system/extensions/literature/scripts/tests/generate-test-fixtures.py` with `main()`
dispatch branches `biblio-quantifier` / `fused-word` and an updated module docstring `Usage:`
block. Re-measured raw glue counts through the real generator + real converter: negative fixture
7, positive fixture 3 — matching the Phase 1 spike exactly. Binder characters (`∀x.P`, `∃y.E`,
`∃x.Q`) and `Ph.D` confirmed present verbatim in the extracted markdown.

**Key Decisions Made**:
- Fixture text is exactly the Phase 1 spike-confirmed text (no further tuning needed).
- Binder line uses `fitz.TextWriter` + `fitz.Font("helv")`, per the plan's Phase-1 finding.
- Fused-word lines kept under ~70 chars each (see "What NOT to Try" below).

**Deviations from Plan**: None (implementation followed plan).

**What NOT to Try**: Do not write a single long `insert_text()` line for a fused-boundary
fixture line — PyMuPDF's `insert_text()` does not wrap, and a line wider than the page's usable
width (~540pt at x=72 on a 612pt-wide page) is silently truncated mid-word rather than wrapped,
which nearly hid the intended fusion boundary during the Phase 1 spike.

**References**: `specs/040_fix_convert_quality_gate_glue_false_positives/plans/01_glue-gate-exemptions.md`
(Phase 3), `specs/040_fix_convert_quality_gate_glue_false_positives/progress/phase-2-progress.json`.
