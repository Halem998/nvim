# Phase 2 Handoff: Check 8 — coarse (blast-radius) file_scope declarations

**Immediate Next Action**: Begin Phase 3 (Check 9 — duplicate file_scope entries) in
`agent-system/extensions/core/scripts/validate-state.sh`, immediately after Check 8.

**Current State**: Phase 2 completed. Plan and progress file are current. Check 8 is live in the
source-store `validate-state.sh`, WARN-only, and deployed (`deploy-headless.sh` ran successfully).

**Key Decisions Made**:
- Sourced `file-scope-overlap.sh` with the exact same deploy-tree-first / source-store-fallback
  candidate-list idiom as `status-vocabulary.sh`.
- Spliced `$FILE_SCOPE_OVERLAP_JQ_DEFS` verbatim; added only `is_terminal` and the scan logic
  locally (no `rtrimstr`/`startswith` re-derivation — confirmed via grep).
- `FILE_SCOPE_COARSE_MIN_OVERLAP` env var, default 3, exit 2 on non-integer override.
- `--help` sed range moved from `2,74p` to `2,94p` to keep pace with header growth (D4).
- Confirmed the Scope Hypothesis mismatch (8 WARN lines instead of the predicted 6) is caused by
  Phase 1's own deliberate `file_scope` amendment (adding an overlap against tasks 20 and 50 via
  the shared `.../scripts/tests/` path) — not a mis-spliced predicate. Recorded in the plan's
  Phase 2 Scope Hypothesis confirmation note.

**Deviations from Plan**:
- Check 8's jq invocation shape: one `jq -c --argjson min ... "$STATE_FILE"` call rather than
  literal `jq -n --slurpfile`, matching this script's existing Check 5/6/7 idiom. Recorded in the
  plan's Phase 2 task checklist and progress file.

**What NOT to Try**:
- Do not "fix" the exit-1 result of `bash validate-state.sh specs/state.json` by touching Check
  8/9 — the sole FAIL is `Unknown entry field: priority (on project_number(s): 53)`, confirmed
  via `git stash` to pre-date this task and be unrelated to `file_scope`.
- Do not attempt to fix the doc-lint index-entries.json line-count drift or the
  `test-lint-state-writer-boundary.sh` `--verbose` sub-case failure surfaced by
  `verify-deploy.sh` — both confirmed pre-existing and out of task 61's scope.

**References**:
- Plan: `specs/061_surface_coarse_file_scope_declarations_at_creation/plans/01_coarse-file-scope-advisory.md`
- Current phase: 3
