# Phase 6 Handoff: Regression fixtures, full verification sweep, deploy

**Immediate Next Action**: All 6 phases are now complete. Proceed to Stage 5a (plan-marker
backstop verification), Stage 6 (implementation summary), and Stage 7 (final metadata).

**Current State**: Phase 6 closed as `[COMPLETED WITH EXCLUSIONS]` (see the phase's `#### Reasoned
Exclusions` table). Four new fixture blocks added to `test-validate-state.sh`; suite is 18/18
passing from both invocation sites. Deploy ran successfully. `verify-deploy.sh` reports 3 of 23
gates FAIL, all three confirmed pre-existing (via `git stash` against the pre-Phase-1 commit
`18fcd2472`) and unrelated to `file_scope`/Check 8/Check 9/`--fix`.

**Key Decisions Made**:
- Phase 6 was closed via the reasoned-exclusion mechanism rather than left `[PARTIAL]`, because
  all five admission conditions hold: the exclusions are deliberate (not abandonment), tightly
  scoped to 3 named pre-existing failures, each carries a documented reason, each is evidenced via
  `git stash` byte-identical comparison, and no residual work remains — this task's own Check
  8/Check 9/`--fix`/Step 6.5 work is fully done and verified.
- The `--fix` regression fixture is placed inside this repo's own git tree (`specs/_tmp_fso_fix_fixture_$$`,
  cleaned up immediately) rather than a generic `/tmp` workdir, so the D3 git-toplevel candidate
  resolution can reach the real deployed `state-write.sh` — gracefully SKIPPED (not FAILED) when
  no deployed copy exists yet.

**Deviations from Plan**: Recorded in the phase's `#### Reasoned Exclusions` table and in
`progress/phase-6-progress.json`'s `deviations` array — both concern pre-existing, unrelated
failures surfaced by `verify-deploy.sh`/`validate-state.sh --deep` against live state, not
anything this task introduced.

**What NOT to Try**: Do not attempt to fix the doc-lint index-entries.json line-count drift, the
`test-lint-state-writer-boundary.sh` `--verbose` sub-case, or task 53's `priority` field as part
of this task — all three are out of task 61's declared `file_scope` and fixing them here would be
an undeclared scope expansion.

**References**:
- Plan: `specs/061_surface_coarse_file_scope_declarations_at_creation/plans/01_coarse-file-scope-advisory.md`
- All phases complete.
