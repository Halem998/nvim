# Research Report: Task #149

**Task**: 149 - Delete team mode from the agent system
**Started**: 2026-09-02T00:00:00Z
**Completed**: 2026-09-02T00:00:00Z
**Effort**: Large (many small edits across ~20 source-store files, one agent deletion, two context-doc deletions)
**Dependencies**: 125 (delete base lifecycle skills) — must land first per specs/PATH.md Stage A.0/A.0b ordering
**Sources/Inputs**: Codebase grep (agent-system/extensions/**, .claude/** mirror, .opencode/**, .memory/**), specs/PATH.md
**Artifacts**: - this report
**Standards**: report-format.md, subagent-return.md

## Executive Summary

- Team mode lives almost entirely inside `skills/skill-orchestrate/SKILL.md` in one contiguous
  block — **Stage 3.6 (Team Fan-Out) + Stage 3.6a (Teammate-Plan Builder)**, lines 952-1262,
  measured at **19,508 bytes / 311 lines** — plus ~13 smaller scattered references (Stage 1's
  `team_size_eff` derivation, five `team_mode` dispatch forks, one MT-1 diagnostics block).
- `synthesis-agent` has **exactly one caller**: `skill-orchestrate` Stage 3.6a, entirely inside
  the block being deleted. No other skill, agent, or script dispatches it — it is safe to delete
  in full, along with its `manifest.json` registration and its two `index-entries.json` rows.
- Two context docs are **team-mode-exclusive** and should be deleted outright:
  `context/formats/team-metadata-extension.md` and `context/patterns/team-orchestration.md` —
  both are referenced only by `index-entries.json`'s `load_when.agents: ["synthesis-agent"]`,
  which itself goes away. Two other docs (`context/patterns/multi-task-operations.md`,
  `context/patterns/context-protective-lead.md`) hold a "See Also"/"Related Patterns" pointer to
  `team-orchestration.md` that will dangle once it is deleted.
- `.claude/**` is a byte-for-byte deploy mirror of `agent-system/extensions/core/**` for every
  file found here — confirmed same file set, same line content. It is regenerated by the deploy
  process, not a separate edit target.
- **Two important false-positive classes** the acceptance grep must not misclassify as survivors
  needing justification:
  1. `general-implementation-agent.md:112` — `### Stage 3.6: Observation Duty` is that agent's
     own unrelated stage numbering; it has no relation to `skill-orchestrate`'s Stage 3.6.
  2. `context/formats/handoff-artifact.md` and `context/formats/progress-file.md` use
     "teammate"/"successor teammate" as **generic** vocabulary for "the next agent that picks up
     a context-exhaustion handoff" — a naming coincidence with, not a reference to, Stage 3.6's
     fan-out teammates. These files are NOT team-mode-specific and must not be edited for this
     task.
- **Scope boundary finding**: `.opencode/**` (a separate, already-frozen legacy system — see
  specs/PATH.md tasks 22 and 27) and `.memory/**` (historical memory records, e.g.
  `MEM-pattern-synthesis-agent-team-research.md`) both contain `skill-team`/`teammate` hits under
  a literal "repo-wide excluding specs/**" grep, but neither is `agent-system/extensions/**`
  source, and both have their own separate deletion/freeze lineage (`.opencode`) or are inert
  historical records (`.memory`). Recommend the acceptance grep additionally exclude
  `.opencode/**` and `.memory/**`, or treat every hit there as pre-justified by reference to this
  paragraph.
- Three extension agents (`cslib-research-hard-agent.md`, `lean-research-hard-agent.md`) carry a
  literal team-mode carve-out (`teammate_letter` field, `{NN}_teammate-{letter}-findings.md` path
  branch) that references the deleted Stage 3.6a and should be removed under the "remove
  team-mode carve-outs" exception to the extension-agents MUST-NOT. `cslib-implementation-hard-agent.md`
  has no team reference (implement has no letter-based artifact convention). Two more extension
  files reference `--team` incidentally in prose (`hard-mode-selection.md`, `literature/merge-sources/claudemd.md`)
  and one (`tts-stt-integration.md`) mentions `--team` mode in a TTS-suppression justification —
  all three need a small prose edit, not a carve-out removal.
- `founder-implement-agent.md` and `founder/agents/project-agent.md` use `team_size`/"Team" to
  mean a **startup's headcount metric** in pitch-deck content — completely unrelated to
  orchestrator team mode. Do not touch.
- `parse-command-args.sh` also exports `EXPLOIT_FLAG`/`EXPLORE_FLAG` whose doc comment says
  "hint for **team research**" — these are not named `TEAM_*`, have zero consumers anywhere in
  the repo today, and are explicitly protected by the task's own MUST NOT ("never change any
  non-team export"). Leave them exactly as-is; flag as a separate, out-of-scope dead-export
  observation only.
- No dedicated "team-mode contract test" file exists under `scripts/tests/` today (49 files
  surveyed, zero reference "team" in any form). The WORK item's instruction to "delete, do not
  skip" the team contract tests has no literal target file to act on — note as N/A rather than
  invent a deletion.

## Context & Scope

Scanned `agent-system/extensions/**` (the source store; `.claude/**` is its disposable deploy
mirror per `agent-system/extensions/core/rules/source-store-deploy-boundary.md` and was spot
verified to match file-for-file, not separately edited), plus `.opencode/**` and `.memory/**` for
the acceptance grep's literal repo-wide scope. `specs/PATH.md` (Stage A.0b, "Decisions" section
items 5, "Recommended batches") supplied the decision rationale and the task's place in the
dependency chain (`149 <- [125]`; `145 <- [149]`).

## Findings

### Codebase Patterns

**1. `skills/skill-orchestrate/SKILL.md` (agent-system/extensions/core/skills/skill-orchestrate/SKILL.md`, 4506 lines total)**

| Site | Lines | Content | Disposition |
|---|---|---|---|
| Stage 3.6 + 3.6a (contiguous) | 952-1262 | `### Stage 3.6: Team Fan-Out` through the line before `### Stage 4: State Handlers` | Delete whole block — 19,508 bytes / 311 lines |
| Stage 1 team-size derivation | 61-80 | `team_mode`/`team_size`/`team_size_explicit` defaults, `team_size_eff` derivation code block | Delete |
| `not_started` fork (research, first full statement) | 1267, 1286-1301ish | First "`team_mode` fork" — stated fully here, referenced by every later occurrence | Delete fork + its `if` body; keep the `else`/fallthrough path as the only path |
| `researched`-adjacent plan fork | 1359-1366ish | Pointer-only `team_mode` fork for `phase=plan` | Delete |
| second plan fork | 1491-1499ish | Pointer-only `team_mode` fork | Delete |
| third plan fork | 1628-1636ish | Pointer-only `team_mode` fork | Delete |
| hard-mode interaction note | 1926-1927 | Cross-reference to Stage 3.6a's hard-mode empty-teammate-plan behavior | Delete (moot once 3.6a is gone) |
| implement fork | 1953-1962ish | Pointer-only `team_mode` fork for `phase=implement` | Delete |
| MT-1 diagnostics-only block | 2930-2936 | `team_mode` read "for DIAGNOSTICS ONLY" in multi-task mode; emits the accepted-and-ignored notice | Delete this bullet; multi-task mode then has one fewer accepted-and-ignored flag to diagnose (only `force_phases` remains) |
| line 1267 back-reference prose | 1267 | "Stated once here, exactly as the `team_mode` fork paragraph below" | Rewrite/remove — refers to the deleted fork |

Each of the five "`team_mode` fork" occurrences follows an identical shape: `if [
"${team_mode:-false}" = "true" ]; then` ... `Run **Stage 3.6: Team Fan-Out** ...` ... `else` ...
(the real, surviving single-agent dispatch). Deleting a fork means deleting the `if`/team-branch
and un-indenting/keeping only the `else` branch's dispatch logic as the unconditional path — this
is NOT a simple line-delete; each site needs the surrounding state-handler prose read to confirm
where the `else` branch's single-agent Agent-tool dispatch begins and ends, since the plan/task
description's "every dispatch site in Stage 4" phrase (from Stage 3.6's own heading note) implies
there are more than the 5 fork occurrences found by the `team_mode fork` search string — the
literal fork paragraphs found were 5 (`not_started`×1 stated fully, then 4 "(see the `not_started`
handler ... for the full statement)" pointer repeats at plan×3, implement×1). A careful line-range
read of Stage 4 (1263-2214) immediately before editing is needed to confirm no 6th site was missed
by the string search (e.g. a `researching`/`in_progress` resume-mid-team-cycle branch).

Also note: `## Context References` (lines 12-21) has **no** team reference — the WORK item's
claim of a "Context References" team mention was not found there; it was likely conflating the
in-block `- @.claude/context/formats/team-metadata-extension.md` / `patterns/team-orchestration.md`
references that live INSIDE Stage 3.6/3.6a itself (both deleted with the block) — confirm no
stray reference remains after deletion via a final grep, not a separate edit target.

**2. `commands/orchestrate.md` (811 lines)**

| Site | Lines (approx) | Content | Disposition |
|---|---|---|---|
| frontmatter `argument-hint` | 4 | Already excludes `--team` | No action |
| Constraints bullet | 24-26 | "`--team` is single-task only: in multi-task mode it is accepted and ignored..." | Delete bullet |
| Options table rows | 49-50 | `--team` / `--team-size` rows | Delete both rows |
| Step 1 parser-thread prose | ~76-90 | "`TEAM_MODE` ... `team_size_eff` derivation" explanatory paragraph | Delete paragraph |
| MT dispatch `args:` string | 471 | `team_mode={TEAM_MODE} team_size={TEAM_SIZE} team_size_explicit={TEAM_SIZE_EXPLICIT}` | Remove these three tokens from the args string |
| MT delegation-context JSON | 490-492 | `"team_mode"`, `"team_size"`, `"team_size_explicit"` keys | Delete the three keys |
| MT diagnostics-only prose | 497-499 | "`team_mode`/`team_size`/`team_size_explicit` are carried here for diagnostics only..." | Delete paragraph |
| single-task dispatch `args:` string | 680 | same three tokens | Remove |
| single-task delegation-context JSON | 702-704 | same three keys | Delete |

19 total lines in the file mention team/Team/TEAM (measured by `grep -c`); the table above accounts for all of them.

**3. `scripts/parse-command-args.sh` (208 lines)**

| Site | Lines | Content | Disposition |
|---|---|---|---|
| Doc comment | 14-18 | `TEAM_MODE`/`TEAM_SIZE`/`TEAM_SIZE_EXPLICIT` export docs | Delete these 3 comment lines |
| Default assignment | 89-91 | `TEAM_MODE="false"`, `TEAM_SIZE=2`, `TEAM_SIZE_EXPLICIT="false"` | Delete |
| `--team` parse | 106-108 | `if [[ "$remaining" =~ --team ]]; then TEAM_MODE="true"; fi` | Delete |
| `--team-size` parse | 109-114 | two-armed regex for `--team-size=N` / `--team-size N` | Delete |
| Flag-strip pipeline | 176-177 | `sed 's/--team-size.../g'`, `sed 's/--team//g'` | Delete these two `sed` stages from the `FOCUS_PROMPT` pipeline |
| Final `export` line | 205 | `TEAM_MODE TEAM_SIZE TEAM_SIZE_EXPLICIT` among the exported names | Remove these three names only — every other name on that line (`TASK_NUMBERS`, `REMAINING_ARGS`, `EFFORT_FLAG`, `MODEL_FLAG`, `CLEAN_FLAG`, `FORCE_FLAG`, `DRY_RUN_FLAG`, `LOCAL_FLAG`, `EXPLOIT_FLAG`, `EXPLORE_FLAG`, `LIT_FLAG`, `ALLOW_SELF_MODIFYING_FLAG`, `ALLOW_SCOPE_COLLISION_FLAG`, `CONTINUE_BUDGET_FLAG`, `FORCE_PHASES_FLAG`, `FOCUS_PROMPT`) must stay byte-identical, per the task's MUST NOT |

`EXPLOIT_FLAG`/`EXPLORE_FLAG` (lines 26-27 doc comment, `false` defaults) mention "team research"
in their own doc comment but are separate flags with zero consumers repo-wide — out of scope,
untouched.

No test file targets this script directly (`scripts/tests/` has 49 files; none reference
`parse-command-args` by name or exercise `TEAM_MODE`/`--team`). The closest coverage is
`scripts/tests/test-force-phases.sh`, which likely sources the same parser for its own flag —
worth running after the edit as a smoke test even though it doesn't assert on team flags. Run
`scripts/tests/run-all.sh` (the full suite) post-edit per the task's acceptance bar.

**4. `agents/synthesis-agent.md` (agent-system/extensions/core/agents/synthesis-agent.md)**

Confirmed **single caller**: every reference to `synthesis-agent` outside its own file resolves
to either (a) documentation/registration (`manifest.json:32`, `index-entries.json:509` and
`:1224` — the `load_when.agents` arrays of `team-metadata-extension.md` and
`team-orchestration.md` respectively, `merge-sources/claudemd.md:154`,
`context/reference/skill-agent-mapping.md:56`, `scripts/lint/lint-agent-contracts.sh:315`'s
comment) or (b) `skills/skill-orchestrate/SKILL.md` lines 1166-1258 — entirely inside the Stage
3.6a block being deleted. **Delete `agents/synthesis-agent.md` in full**, remove its
`manifest.json` line 32 registration, and remove both `index-entries.json` rows (which disappear
anyway once their host files — `team-metadata-extension.md`, `team-orchestration.md` — are
deleted; no dangling reference to `synthesis-agent` survives).

**5. `merge-sources/claudemd.md`**

| Site | Lines | Content | Disposition |
|---|---|---|---|
| Multi-task syntax sentence | 116 | "...`--team` are single-task only — accepted and ignored (with a notice)..." | Remove `--team` from this sentence's flag list (keep the `--research`/`--plan`/`--implement` framing) |
| Skill-to-Agent Mapping table row | 154 | `skill-orchestrate (internal, Stage 3.6a synthesis step) \| synthesis-agent` | Delete row |
| "Team Mode" paragraph | 170-176 | Full paragraph + cost note | Delete |
| Cost Impact table rows | 211-212 | `--team` / `--hard --team` rows | Delete both |
| Composability bullet | 216 | "`--hard` works with `--team`: ..." | Delete |

After this edit, regenerate `CLAUDE.md` (deploy/reload) and confirm — this repo's own root
`.claude/CLAUDE.md` (visible in the system prompt at session start) still shows the pre-edit text
verbatim (Team Mode paragraph, cost table rows, composability bullet, and the
`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` env var name), confirming today's `.claude/CLAUDE.md` is
the stale merged output this edit must regenerate.

**6. Context/docs sweep**

| File | Site | Disposition |
|---|---|---|
| `context/formats/team-metadata-extension.md` | whole file (119 lines) | Delete — exclusively documents the (already-noted-as-unused) team-execution return-meta schema |
| `context/patterns/team-orchestration.md` | whole file (146 lines) | Delete — exclusively documents Stage 3.6/3.6a's wave model (Primary/Alternatives/Critic roles) |
| `index-entries.json` | ~493-513, ~1207-1227 | Delete both entries (for the two files above) |
| `context/patterns/multi-task-operations.md` | line 675 table row `Team mode support \| No \| Single-task only` | Delete row (or reword — the table is comparing multi-task vs. single-task dispatch models; the row becomes meaningless, not just dangling) |
| `context/patterns/multi-task-operations.md` | line 685 `team-orchestration.md` -- Wave-based... | Delete this "See Also" bullet — its target file is gone |
| `context/patterns/context-protective-lead.md` | line 259 `**Team Orchestration** (patterns/team-orchestration.md) -- ...` | Delete this "Related Patterns" bullet |
| `context/patterns/skill-lifecycle.md` | 267-269 | "**Multi-task vs. team mode** (orthogonal dimensions)..." paragraph | Delete paragraph (multi-task mode's own explanation stands alone once team mode is gone) |
| `context/patterns/skill-self-execution-fallback.md` | 12-15, 32, 39, 49-50 | Multiple mentions of "now-retired per-mode team skills" and "team fan-out" as the surviving analog | These describe the ALREADY-retired (task 125 lineage) per-mode team skills as historical color for a still-relevant fallback-defect pattern; once Stage 3.6 is also gone, the "surviving analog" framing (lines 13-15) needs rewriting since there is no surviving team analog left — but the historical mention of retired skills (line 12) can stay as-is (matches the file's own "now-retired" framing already used elsewhere in this codebase). Needs a close read at edit time, not a blind delete. |
| `context/meta/meta-guide.md` | 238 | "`/meta` does not support ... `--team` ..." flag-exclusion list | Remove `--team` from the list (the sentence's point — /meta takes no effort/model flags — still holds without it) |
| `context/reference/state-management-schema.md` | 162-163 | "**Team Mode Naming**: Teammate artifacts use `{NN}_{letter}-findings.md`..." | Delete subsection (see WORK item 7 — convention becomes historical, remove from format docs) |
| `context/reference/skill-agent-mapping.md` | 52-62, 138-139 | Full "## Team Mode" section + a "see Team Mode section above" cross-reference | Delete section and the cross-reference sentence (fold anything load-bearing about `--team` NOT being on `/research`/`/plan`/`/implement` into prose only if still true/relevant post-deletion — likely moot, those three commands are already deleted per task 124) |
| `context/standards/git-staging-scope.md` | 144 (commit message text), 151 ("unlike the team-research example above") | Example `git commit -m "task ... complete team research (${team_size} teammates)"` and its follow-on prose | Reword the illustrative commit message to a non-team example (e.g. "complete research") and adjust the follow-on sentence that contrasts it against "the team-research example above" |
| `docs/architecture/handoff-schema.md` | (no `[Tt]eam` hits found on direct grep — file appeared in the broader `Stage 3\.6\|teammate` sweep only) | Re-check at edit time; likely a `teammate`/generic-handoff false positive akin to `handoff-artifact.md`/`progress-file.md` — do not edit unless a genuine Stage-3.6-specific line is found |
| `docs/fork-patterns.md` | 75-76 (table rows), 153-169 (`## Team-Mode Optimization Opportunity (Future Work)` section) | Delete both table rows and the entire section (moot future-work item once the feature is deleted) |
| `docs/guides/creating-commands.md` | 82-83 (export comment), 87 (`--team` in flag list prose) | Update the `# Exports:` comment to drop `TEAM_MODE, TEAM_SIZE`; drop `--team` from the flags-list sentence |
| `docs/templates/command-template.md` | 23 (Options row), ~35 (`# Exports: ... TEAM_MODE, TEAM_SIZE, ...` comment) | Delete the `--team` row; update the exports comment — this is a scaffold template for NEW commands, so leaving `--team` in it would reintroduce the flag in future work |
| `rules/git-workflow.md` | 72 | "...the proven `--team` staging convention..." (context for `git-staging-scope.md`'s reference template) | Reword to drop the `--team`-specific framing |
| `hooks/wezterm-task-number.sh` | 48 | Comment: `# strip from first "--" (removes flags like --team)` | Cosmetic — either leave (the mechanism strips ANY `--flag`, `--team` is just the illustrative example named in the comment) or swap the example flag name; low priority, not required for correctness |
| `scripts/lint/lint-postflight-boundary.sh` | 68-74 | Comment history + regex referencing "team skills"/"Agent tool for team coordination" phrasing from the ALREADY-deleted (task 125) per-mode team skills | Not in scope for 149 — this predates Stage 3.6 and describes different (already-gone) skills; verify at edit time whether the regex clause `Spawn teammates using Agent tool\|Agent tool for team coordination` still matches anything live (if zero files match it post-125, it is dead code from a PRIOR task's cleanup, not this one) |
| `scripts/orchestrate-recover-outcome.sh` | (appeared in broad sweep; no direct `[Tt]eam` hit under a plain grep) | Re-check at edit time for a `Stage 3.6` or `team_mode` literal; likely a `teammate`-generic or `Stage 3.6` numbering coincidence akin to `general-implementation-agent.md` |

### Extension-file sweep (per the "extension agents" MUST NOT's carve-out exception)

| File | Site | Disposition |
|---|---|---|
| `agent-system/extensions/cslib/agents/cslib-research-hard-agent.md` | 194-195 | `teammate_letter` field + team-mode report-path branch, mirroring core `general-research-agent.md` | Remove (carve-out referencing the deleted stage) |
| `agent-system/extensions/cslib/agents/cslib-implementation-hard-agent.md` | none found | No team reference exists (implement has no letter-artifact convention) | No action |
| `agent-system/extensions/lean/agents/lean-research-hard-agent.md` | 156, 258 | Same `teammate_letter` field + report-path branch | Remove |
| `agent-system/extensions/cslib/context/project/cslib/domain/hard-mode-selection.md` | 46 | "Composing `--hard --team` compounds both multipliers (~15-25x)..." | Remove this sentence/example (or reword to describe `--hard` alone) |
| `agent-system/extensions/literature/merge-sources/claudemd.md` | 85 | "`--lit` works with `--team`, `--hard`, `--fast`, and model flags" | Drop `--team` from this composability list |
| `agent-system/extensions/nvim/context/project/neovim/guides/tts-stt-integration.md` | 99 | "This eliminates random ... announcements during `--team` mode and mid-workflow orchestrator pauses." | Drop "`--team` mode and" from the sentence |
| `agent-system/extensions/founder/agents/founder-implement-agent.md` | 889, 912 | `{team_size}` as a pitch-deck "Team Size" metric | **False positive — do not touch** (startup headcount, unrelated) |
| `agent-system/extensions/founder/agents/project-agent.md` | 441, 457, 477 | `team_size`/"Team" as project-staffing metric in generated project reports | **False positive — do not touch** |
| `agent-system/extensions/email/context/project/email/design/email-to-memory-preferences.md` | 7 | Historical citation of an actual on-disk artifact filename (`01_team-research.md`) from a real completed task | **Leave as-is** — matches WORK item 7's "leave existing artifacts on disk" instruction; this is citing history, not describing a live convention |

### Out-of-scope but grep-visible (acceptance-grep survivors requiring justification, not action)

- `.opencode/**` (`AGENTS.md`, `extensions.json`, `skills/skill-team-research/`, `skills/skill-team-plan/`,
  `skills/skill-team-implement/`) — a separate, already-frozen legacy system with its own
  deletion/freeze tasks (specs/PATH.md tasks 22, 27; task 27 was completed the same day this
  research ran, per `git log`). Not `agent-system/extensions/**` source and not part of this
  task's edit target.
- `.memory/memory-index.json` and `.memory/10-Memories/MEM-pattern-synthesis-agent-team-research.md`
  — a historical memory record describing the (already superseded) synthesis-agent + per-mode
  team-skills pattern. Inert data, not live documentation; a future `/distill` pass is the
  correct place to prune or update it, not this task.
- `general-implementation-agent.md:112` (`### Stage 3.6: Observation Duty`) — coincidental stage
  numbering, unrelated to `skill-orchestrate`'s Stage 3.6.
- `context/formats/handoff-artifact.md`, `context/formats/progress-file.md` — generic
  "successor teammate" handoff vocabulary, not Stage-3.6-specific.
- `founder-implement-agent.md`, `founder/agents/project-agent.md` — startup/project team-size
  metrics, unrelated domain meaning of "team".

### Recommendations

1. Sequence the SKILL.md edit first and re-run the acceptance grep after each file, since the
   fork-paragraph deletions (5 sites) are the highest-risk edits (need to preserve the `else`
   branch as the sole path, not just delete text) — do this file in its own pass before touching
   `commands/orchestrate.md`, per the task's own dependency note (149 precedes 145, "so the
   command file is slimmed against the final flag set").
2. Delete `synthesis-agent.md` + its two context docs + their `manifest.json`/`index-entries.json`
   registrations as one atomic sub-step, verified by a fresh `grep -rn synthesis-agent
   agent-system/extensions/core` returning zero hits.
3. Treat the `.opencode/**` and `.memory/**` hits as pre-justified (cite this report) rather than
   editing them — editing `.opencode` under this task would conflict with tasks 22/27's own
   ownership of that directory's lifecycle.
4. Re-verify `docs/architecture/handoff-schema.md` and `scripts/orchestrate-recover-outcome.sh`
   at edit time — this research's broader multi-pattern sweep (`Stage 3\.6|teammate|...`) flagged
   them but a narrow `[Tt]eam` grep found nothing textual to change; they may be pure false
   positives like `general-implementation-agent.md`.
5. After all edits, the two required acceptance numbers (bytes removed from `SKILL.md` and from
   `orchestrate.md`) should be computed as `wc -c` on a `git diff` of just the deleted line spans,
   not estimated — this report's 19,508-byte figure for the SKILL.md Stage 3.6/3.6a block is
   measured (`sed -n '952,1262p' | wc -c`) and should match closely to the final diff once the
   scattered fork/diagnostics lines are added on top.

## Decisions

- `synthesis-agent` and its two exclusive context docs are deleted outright (single caller,
  fully contained in the deleted block).
- `.opencode/**` and `.memory/**` are treated as out of this task's edit scope; their grep hits
  are pre-justified by this report rather than edited.
- `founder-implement-agent.md`/`project-agent.md` `team_size` usages and
  `general-implementation-agent.md`'s `Stage 3.6: Observation Duty` heading are false positives
  and must not be touched.
- `handoff-artifact.md`/`progress-file.md`'s generic "teammate" vocabulary is unrelated to team
  mode and must not be touched.

## Risks & Mitigations

- **Risk**: Deleting a `team_mode` fork's `if` branch without correctly identifying where the
  `else` branch's real dispatch logic starts/ends could corrupt Stage 4's state-handler control
  flow. **Mitigation**: read each fork's full surrounding handler (not just the fork paragraph)
  before editing; there are only 5 occurrences, each short.
- **Risk**: The WORK item's instruction to delete "team-mode contract tests under scripts/tests"
  has no literal target — a planner or implementer might invent a test file to satisfy the
  letter of the instruction. **Mitigation**: this report documents the zero-hit search; treat as
  N/A, not a gap to fill.
- **Risk**: Over-broad grep-driven edits could touch the false-positive class (startup team-size
  metrics, generic "teammate" handoff vocabulary, coincidental Stage 3.6 numbering).
  **Mitigation**: this report's explicit false-positive table should be consulted before any
  bulk find/replace.

## Context Extension Recommendations

- **Topic**: Acceptance-grep false-positive allowlist for this task family.
- **Gap**: No existing context file documents that "teammate"/"Stage 3.6"/"team_size" are
  overloaded strings used by unrelated subsystems (generic handoff vocabulary, founder-extension
  business metrics, other agents' own stage numbering).
- **Recommendation**: Not worth a standing context file for a one-time deletion task; the
  false-positive table in this report is sufficient for the implementation phase. No new context
  file recommended.

## Appendix

### Search queries used

- `grep -rn "team_mode|team_size|--team|teammate|skill-team|Stage 3.6" agent-system/ --include="*.md" --include="*.sh" --include="*.json" -l`
- `grep -n "^### Stage 3\.|^### Stage 4" skills/skill-orchestrate/SKILL.md`
- `sed -n '952,1262p' skills/skill-orchestrate/SKILL.md | wc -c` (19508) and `| wc -l` (311)
- `grep -n "team_mode\|team_size\|teammate\|Team Fan-Out\|CLAUDE_CODE_EXPERIMENTAL" skills/skill-orchestrate/SKILL.md | awk -F: '$1<952 || $1>1262'`
- `grep -rn "team-orchestration.md\|team-metadata-extension.md" .`
- `grep -rn "synthesis-agent" agent-system/ --include="*.md" --include="*.sh" --include="*.json"`
- `grep -n "TEAM\|team" scripts/parse-command-args.sh`
- `find . -path "*scripts/tests*" -type f -exec grep -l "team\|TEAM" {} \;` (zero hits across 49 files)
- Per-file `grep -n "[Tt]eam"` sweeps across every WORK-item-named file plus every extension
  agent/context file surfaced by the repo-wide `-l` search
- `grep -rlE "team_mode|team_size|--team\b|teammate|skill-team|Stage 3\.6" .` (full repo,
  611 total hits, bucketed by top-level directory to separate specs/vault, .opencode/**,
  .memory/**, and agent-system/extensions/** from each other)
- `git log --format="%ad %s" -- .opencode/` (confirms .opencode has its own separate,
  in-progress deletion/freeze task lineage — tasks 22, 27)

### References

- `specs/PATH.md` — Stage A.0b entry, "Decisions" item 5, "Recommended batches" (Batch 2),
  dependency chain `149 <- [125]`, `145 <- [149]`
- `agent-system/extensions/core/skills/skill-orchestrate/SKILL.md` (4506 lines)
- `agent-system/extensions/core/commands/orchestrate.md` (811 lines)
- `agent-system/extensions/core/scripts/parse-command-args.sh` (208 lines)
- `agent-system/extensions/core/agents/synthesis-agent.md`
- `agent-system/extensions/core/merge-sources/claudemd.md`
- `agent-system/extensions/core/rules/source-store-deploy-boundary.md`
