# Phase 3 Handoff — task 828

**Immediate Next Action**: Phase 4 — `.Archive` pilot: hand-verify the 2 already-computed
MATCHED decisions, then execute with `--execute` and verify.

**Current State**: Phase 3 completed. Repair script written and tested in dry-run against the
real `.Archive/cur` (2 pairs) as the "small input" verification. No `mv`, no server write
occurred. Decision log and IMAP verb log both written. Plan file current (Phase 3 marked
[COMPLETED]).

**Key Decisions Made**:
- Script: `~/Mail/.logos-backup-20260706/task-828/repair-uid-collisions.py`
  (`--folder {Trash|Archive} --maildir <path> [--execute]`), default dry-run.
- Cross-checked the current 860 `.Trash` duplicate-UID set against
  `phase5/trash-dup-pairs.tsv` — byte-identical, confirming no drift since task 826.
- `.Archive` dry-run decisions (both MATCHED, computed from live IMAP fetch):
  - uid=1: rename `1770669071.1006834_1.hamsa,U=1:2,S` -> `U=864` (server msgid matched
    `1770669127.1007943_939.hamsa,U=1:2,S`, which keeps token 1).
  - uid=2: rename `1770669071.1006834_2.hamsa,U=2:2,S` -> `U=865` (server msgid matched
    `1770669127.1007943_940.hamsa,U=2:2,S`, which keeps token 2).
  - Fresh tokens (864, 865) computed correctly above folder max (863).
- Decision log is append-safe JSONL; this dry-run pass already wrote 2 `"executed": false`
  entries. Phase 4's dry-run re-run (if performed) and execute run will append further entries
  for the same uids — this is expected/by design (append-safe, non-destructive log).

**Deviations from Plan**: Decision log implemented as JSONL only (not TSV alternative), with
extra `new_filename`/`executed` fields beyond the minimum schema — additive, not a narrowing.

**What NOT to Try**: Do not use `os.remove`/`os.unlink` anywhere — script contains none. Do not
skip the fetch-then-act ordering (all Message-Ids are fetched before any decision is computed).

**References**: Plan at
`specs/828_logos_trash_archive_uid_collision_repair/plans/01_logos-uid-collision-repair.md`.
Current phase: 4. Script/logs under `~/Mail/.logos-backup-20260706/task-828/`.
