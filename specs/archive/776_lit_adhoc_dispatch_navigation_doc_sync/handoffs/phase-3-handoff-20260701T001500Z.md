# Phase 2+3 Handoff — Task 776

**Immediate Next Action**: Proceed to Phase 4 (cross-artifact verification and follow-up
documentation).

**Current State**: Phases 1, 2, and 3 completed. Plan and progress files are current.
- Phase 2: added `adhoc-navigation-directive.md` entry to
  `.claude/extensions/literature/index-entries.json` (line_count=92, verified against `wc -l`).
- Phase 3: rewrote the Literature Mode preamble + "What `--lit` Does" in
  `.claude/extensions/core/merge-sources/claudemd.md` to the live `literature-briefing.sh` ->
  `<literature-briefing>` model, dropped `TOKEN_BUDGET`/`MAX_FILES`, deleted the orphaned scoping
  note, and added a new "Ad-Hoc / Conversational Literature Requests" pointer subsection.
  Deployed `.claude/CLAUDE.md` verified untouched (`git status --porcelain` empty).

**Key Decisions Made**:
- Placed the new "Ad-Hoc / Conversational Literature Requests" subsection between "What `--lit`
  Does" and "Interactive Sub-Index Setup Detection" (logical flow: model description -> ad-hoc
  alternate path -> skill-dispatch-specific missing-sub-index flow).
- "Interactive Sub-Index Setup Detection" body (former lines 335-379) left byte-for-byte
  unchanged except the scoping-note deletion immediately preceding it.

**Deviations from Plan**:
- Phase 3 Task 5 (consistency sweep) named the Memory Extension "Literature-Augmented Research"
  paragraph as an item to reconcile, but that paragraph lives in
  `.claude/extensions/memory/EXTENSION.md` — a different merge-source file, not `claudemd.md`.
  Left untouched per the explicit single-file scope instruction ("only edit the merge-source
  claudemd.md") and the plan's own caveat ("do not expand into other files"). Recorded in
  `progress/phase-3-progress.json` deviations array.

**What NOT to Try**: Do not edit `.claude/extensions/memory/EXTENSION.md` or any other
merge-source file for this task — out of scope. Do not edit deployed `.claude/CLAUDE.md`.

**References**:
- Plan: `specs/776_lit_adhoc_dispatch_navigation_doc_sync/plans/01_lit-adhoc-navigation-doc-sync.md`
- Current phase: 3 of 4 (completed); Phase 4 remains.
