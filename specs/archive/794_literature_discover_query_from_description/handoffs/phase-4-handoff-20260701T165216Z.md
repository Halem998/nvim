# Handoff: Task 794 -- literature-discover.sh query fix

## Immediate Next Action

All 4 phases complete. Proceed to Stage 5/6 (final verification already done in Phase 4; write summary and metadata).

## Current State

All phases [COMPLETED] in the plan file. Canonical and flat copies of literature-discover.sh
are byte-identical and both edited with FIX 1 (description+title query) and FIX 2 (Zotero
missing-export stderr hint + swallow-point cleanup).

## Key Decisions Made

- Dropped `.project_name` (slug) entirely from the `--task N` query path per plan; query now
  built solely from `.description // ""` + `.title // ""` with null/empty guards and exit 2
  on both-empty.
- Only the `tier2_search` call site's outer `2>/dev/null` was removed (tier1_search and
  tier3_search call sites left untouched -- out of scope per plan).
- Added `2>/dev/null` parity to the two previously-unredirected `python3 -c` calls building
  `authors_arr` inside `tier2_search`.

## Deviations from Plan

- None (implementation followed plan).

## What NOT to Try

- Do not touch `literature.md` (~lines 125-139) whole-script stderr capture -- explicitly out
  of scope; documented as a known follow-up gap (hint only surfaces on direct invocation of
  the script, not through `/literature N`).
- Do not create `zotero-library.json` -- user action, out of scope.

## References

- Plan: specs/794_literature_discover_query_from_description/plans/01_discover-query-fix.md
- Canonical: .claude/extensions/literature/scripts/literature-discover.sh
- Flat: .claude/scripts/literature-discover.sh
- Report: specs/794_literature_discover_query_from_description/reports/01_discover-query-fix.md
