# Phase 4 Handoff — task 121

**Immediate Next Action**: Begin Phase 5 (prune dead agent references from index-entries.json
across core/cslib/lean).

**Current State**: Phases 1-4 completed and committed. The seven -hard files are deleted from
disk; core/cslib manifests are surgically edited; lean is untouched (verified empty git diff).
Repo-wide census: 201 occurrences across 41 files remain (down from 359/60), matching the plan's
expected post-Phase-4 scope. All gates green except `check-extension-docs.sh`'s expected
"deployed script content drift" FAILs for the 4 Phase 2/3-edited lint/test scripts (self-resolves
at Phase 8's deploy regen).

**Key Decisions Made**:
- Deferred the `lint-task-lookup-adoption.sh` EXCLUDED_FILES edit from Phase 2 to Phase 4 (see
  Phase 2's Deviation note) — removing the exemption before the files were deleted surfaced 4
  genuine violations; landing both together kept every phase green.
- Removed (not retargeted) `test-routing-resolution.sh`'s hardcoded op_default loop — Assert 1's
  mechanical matrix loop already covers the same pairs, and command-route-skill.sh is no longer a
  live research/plan/implement dispatch path.
- Dropped (not retargeted) `test-resume-scan-nonconformance.sh` Site B — skill-orchestrate/SKILL.md
  has only one resume-scan gate region, no second site to retarget onto.
- cslib manifest surgery removed 8 raw key entries (4 op.subkey pairs × 2 blocks), not the "six"
  the plan's summary line stated — the plan's own itemized list (verified against disk) was
  followed.

**What NOT to Try**: Don't attempt to make `check-extension-docs.sh`'s drift FAILs disappear
before Phase 8 — they're expected until the deploy regenerates.

**Critical Context**: cslib's and lean's own `-hard` skills/agents (8 total) are verified still
present and routed — out of scope, must survive to Phase 8's final check.

**References**: plans/01_delete-hard-mode-lifecycle-files.md, progress/phase-4-progress.json,
.reference-census-before.txt.
