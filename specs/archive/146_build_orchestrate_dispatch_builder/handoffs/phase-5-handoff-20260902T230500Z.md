# Handoff: Task #146, Phases 3-5 Complete

## Immediate Next Action

Start Phase 6: replace the 8 single-task Stage 4 dispatch sites in
`agent-system/extensions/core/skills/skill-orchestrate/SKILL.md` with a single call to
`orchestrate-build-dispatch.sh` plus the fixed pointer prompt. First re-run
`grep -n "Run \*\*Stage 3.5" agent-system/extensions/core/skills/skill-orchestrate/SKILL.md` to
re-derive the current line numbers (they drift as earlier phases edit the file) and confirm all
11 occurrences are still present before editing.

## Current State

Phases 1-5 are `[COMPLETED]` in the plan
(`specs/146_build_orchestrate_dispatch_builder/plans/01_orchestrate-dispatch-builder.md`).
`agent-system/extensions/core/scripts/orchestrate-build-dispatch.sh` exists, is deployed, and its
own test suite (`tests/test-orchestrate-build-dispatch.sh`, 49 assertions) passes standalone and
via `run-all.sh`. The full `bash .claude/scripts/tests/run-all.sh` gate is green (62 passed, 0
failed) as of this checkpoint. Git tree is clean at this point (each phase committed separately:
"task 146 phase 1", "phase 2", and this phase-3-5 commit to follow).

## Key Decisions Made

- Phases 3 and 4 were implemented as one script-authoring pass (both target the same new file),
  then verified separately against each phase's own criteria — see phase-3/4 progress files'
  `deviations`.
- Used `skill_validate_input` (skill-base.sh) instead of a fresh jq query for description/task_type
  — same effect, avoids a second implementation.
- Continuation-pointer resolution extracted into `scripts/lib/continuation-pointer-lib.sh`
  (Class C, sourced-only), used by both `orchestrate-build-dispatch.sh` and
  `orchestrate-triage-classify.sh`'s `continuation_ok` predicate — the intended non-fallback
  outcome, not the jq-verbatim fallback.
- Test suite uses a lightweight fixture (only 3 stub scripts + specs/state.json fixture) rather
  than copying skill-base.sh/lib/*.sh wholesale, since `SKILL_REPO_ROOT` env-override is honored
  by skill-base.sh's own default pattern.

## What NOT to Try

- Do not re-copy the continuation jq expression by hand anywhere — always source
  `scripts/lib/continuation-pointer-lib.sh`'s `resolve_continuation_pointer` function.
- Do not assume 2 `ephemeral_excludes` array literals in `git-staging-scope.md` — there are 2
  there plus 1 `candidate_excludes` in `git-commit-scoped.sh` (3 total), all now updated with
  `.dispatch/`.
- When adding a new per-task ephemeral runtime-file pattern in the future, remember to also check
  test fixtures that hand-author a scratch `.gitignore` block (`test-deploy-orphans.sh`,
  `test-deploy-propagation.sh`) and sandbox dependency-copy lists (`test-orchestrate-triage-classify.sh`)
  — these are NOT auto-derived from the real files and will silently go stale otherwise. This bit
  us once already in this task (Phase 5's full-suite gate run) and was fixed.

## Critical Context

- `orchestrate-build-dispatch.sh`'s CLI: `<task_number> <phase> --session SID --seq N [--clean]
  [--lit] [--hard] [--fast] [--model M] [--focus "..."] [--territory "..."] [--dispatch-start-ts TS]`.
  Prints `{"dispatch_file": "...", "model": "..."}` on stdout.
- Fixed pointer prompt shape for Phase 6/7 sites: "You are dispatched by /orchestrate for task
  {N}, phase {phase}. Read {dispatch_file} first and execute it exactly; it names every input,
  output path and contract."
- 11 physical `Run **Stage 3.5**` occurrences confirmed at task start (8 single-task Stage 4 +
  3 MT-4 loops). Phase 6 handles the 8; Phase 7 handles the 3 plus deletes the Stage 3.5 prose.
- Auxiliary dispatches (H4 re-verification x2, Stage 5a fork/reviser, Stage 5b audit, Stage 6
  fork/reviser/re-implement) are OUT OF SCOPE — never touch them.

## References

- Plan: `specs/146_build_orchestrate_dispatch_builder/plans/01_orchestrate-dispatch-builder.md`
- Report: `specs/146_build_orchestrate_dispatch_builder/reports/01_orchestrate-build-dispatch.md`
- Progress files: `specs/146_build_orchestrate_dispatch_builder/progress/phase-{1,2,3,4,5}-progress.json`
