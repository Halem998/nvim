# Handoff: Task #35 — Phase 1 Complete

## Immediate Next Action

Begin Phase 2: redeploy (`bash .claude/scripts/deploy-headless.sh`), run `verify-deploy.sh`,
confirm the deployed `update-task-status.sh` carries the fix (`grep -c 'update-phase-status.sh'`
returns 0), then run both `test-update-task-status.sh` and `test-skill-base-lifecycle.sh` as a
green baseline.

## Current State

Phase 1 completed and committed (`task 35 phase 1: delete preflight phase auto-advance
convenience`). Plan and progress file are current. Source-store edit is in
`agent-system/extensions/core/scripts/update-task-status.sh` only; the deployed `.claude/`
tree has NOT yet been regenerated to pick it up.

## Key Decisions Made

- Deleted the entire `if [[ "$operation" == "preflight" ]]` auto-advance block (comment through
  closing `fi`), plus the nested 3-line dry-run preview inside the `DRY_RUN` branch.
- Added an explanatory comment at the retained `target_status != implement` guard recording (a)
  it is the sole bound on plan-file side effects and (b) the function no longer touches per-phase
  markers.
- Left the pre-existing `update-plan-status.sh`/`update-phase-status.sh` comment mentions at
  lines ~7, 274, 307 untouched — those belong to the `--phase-check` backstop section (PHASE 0),
  which the plan's Non-Goals explicitly excludes from this task's scope.

## Deviations from Plan

- None (implementation followed plan). Note: the Scope Hypothesis's literal
  `grep -c 'update-phase-status.sh'` after-count is 2, not 0, because two pre-existing comment
  lines elsewhere in the file (PHASE 0 `--phase-check` backstop, explicitly a Non-Goal) also
  contain that substring. The actual functional call and its containing block were fully deleted
  and confirmed via `git diff` read-through — no code path invokes `update-phase-status.sh`
  anymore.

## What NOT to Try

- Do not touch the `--phase-check` backstop section (PHASE 0, lines ~266-364) — explicit
  Non-Goal, and it still needs the `phase-heading-patterns.sh` sourcing block and
  `has_nonconforming_phase_headings`.

## Critical Context

- `bash -n` clean, `git diff` confirms only the two deleted regions and the one added comment
  changed.

## References

- Plan: `specs/035_stop_preflight_spurious_phase_advance/plans/01_delete-preflight-phase-autoadvance.md`
- Progress: `specs/035_stop_preflight_spurious_phase_advance/progress/phase-1-progress.json`
- Current phase: 1 (complete) → next: 2
