{
  "$schema": "orchestrator-handoff-v1",
  "phase": "research",
  "status": "researched",
  "summary": "All 4 claimed defects confirmed live (not stale): subshell bug swallows 58 real warnings (also dormant on the error-side for 3 other checks); line_count is a real budget input (validate-context-budgets.sh, currently unwired); 14 deployed context files (11 core, 3 extension) have zero source-level index entries, no literature stray exists (extension not loaded); no version/generated stamp is ever written. True scope is source-level across all 19 extensions (444 entries): 226 numeric mismatches + 94 null line_counts (72% need correction), beyond the 164-entry deployed subset the review inspected. Recommended wiring point: extend check-extension-docs.sh (already verify-deploy.sh gate 3) with two new rules (index-orphan gate, line-count drift gate), not validate-context-index.sh (invoked by nothing automated) and not a new verify-deploy.sh gate.",
  "artifacts": [
    {
      "type": "report",
      "path": "specs/978_fix_index_validators_and_line_counts/reports/01_context-index-validation-truth.md"
    }
  ],
  "blockers": [],
  "next_action_hint": "plan",
  "files_modified": [],
  "decisions_made": [
    "Subshell bug affects error-reporting too (path/domain/deprecated-replacement checks), not only warnings -- fix all 4 pipe-into-while blocks in the same edit via process substitution",
    "Regenerator must target source agent-system/extensions/*/index-entries.json (444 entries, 19 extensions), not only the 164 currently-deployed entries",
    "Index-orphan gate and line-count drift gate should be two new rules in check-extension-docs.sh (reusing $EXT_DIR/orphan_report()/ORPHAN_GATE_MODE-style infrastructure), which already flows through verify-deploy.sh gate 3 with no verify-deploy.sh changes needed",
    "All 14 current orphans (11 core + email + memory + nvim) should be indexed; none warrant exclusion -- all contain real, current content",
    "Literature extension is not currently loaded (.claude-extensions.json active set is core/email/memory/nix/nvim only) so it has zero deployed context files and zero orphans; task description's literature-stray mention does not match live state"
  ],
  "dead_ends": [],
  "phases_completed": 0,
  "phases_total": null,
  "plan_markers_verified": false
}
