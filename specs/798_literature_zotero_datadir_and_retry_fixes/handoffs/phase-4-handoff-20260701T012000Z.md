# Phase 4 Handoff: literature.md NOT_RUNNING Branch Restructured

**Immediate Next Action**: Begin Phase 5 (full integration verification + re-sync audit) --
run the complete verification battery across all edited files.

**Current State**: Phase 4 completed. `.claude/extensions/literature/commands/literature.md`
now has: `ZOTERO_EXPORT_MISSING_RUNNING` with its original two-option prompt (unchanged
behavior); `ZOTERO_EXPORT_MISSING_NOT_RUNNING` with a new three-option prompt ("Open Zotero,
then retry" primary, "Generate an offline snapshot" secondary, "Skip this run"); a bounded
(max 3 attempts) retry loop with freshly-authored enable-API guidance
("Zotero Settings -> Advanced -> API -> Allow other applications...") surfaced after the cap is
exhausted; and the orchestrator-mode paragraph split into distinct RUNNING and NOT_RUNNING
descriptions per Decision D.

**Note on dual-copy**: `.claude/commands/literature.md` is a SYMLINK to
`.claude/extensions/literature/commands/literature.md` (verified via `ls -la`), not a separate
flat copy -- no `cp -p`/`diff` sync step is needed or possible for this file (editing the
canonical file automatically updates what the symlink resolves to).

**Key Decisions Made**:
- The "reuse verbatim" instruction for the secondary Path 3 handling was implemented via an
  explicit cross-reference ("using the SAME success/failure handling as 'Generate now' above")
  rather than literally duplicating the prose block, to avoid future drift between the two
  descriptions in this prose-pseudocode spec file. Logged as a deviation in
  `progress/phase-4-progress.json`.
- The `ZOTERO_EXPORT_UNAVAILABLE` branch had one clause updated (hardcoded
  `~/Zotero/zotero.sqlite does not exist` -> `no zotero.sqlite was found at the resolved sqlite
  path`) for accuracy post-FIX-1; structure otherwise untouched. Logged as a deviation.

**Deviations from Plan**: See `progress/phase-4-progress.json` `deviations` array (one entry,
described above) and the inline `*(deviation: ...)*` annotation on the corresponding checklist
item in the plan file.

**What NOT to Try**: N/A — phase completed cleanly.

**References**:
- Plan: `specs/798_literature_zotero_datadir_and_retry_fixes/plans/01_zotero-datadir-retry-fixes.md`
- Current phase: 4 of 5 (completed)
- Edited: `.claude/extensions/literature/commands/literature.md` (canonical only; symlinked from
  `.claude/commands/literature.md`)
