# Phase 2+3 Handoff: Callers Wired to Resolver, Orchestrator Loud-Failure Hardened

**Immediate Next Action**: Begin Phase 4 (restructure `literature.md` NOT_RUNNING branch) --
depends on Phases 2 and 3, both now complete.

**Current State**: Phases 2 and 3 completed (same wave). `zotero-export-status.sh` and
`zotero-generate-export.sh` (canonical + flat, byte-identical) both now call
`zotero-resolve-sqlite-path.sh` instead of the hardcoded `${HOME}/Zotero/zotero.sqlite`
default. Verified on this machine:
- Classifier emits exactly one directive token (`ZOTERO_EXPORT_MISSING_NOT_RUNNING`) on stdout,
  rationale on stderr, correctly detecting the real `/home/benjamin/Documents/Zotero/zotero.sqlite`.
- Generator reconstructs exactly 1819 entries via both auto-detection and explicit
  `ZOTERO_SQLITE_PATH` override.
- Orchestrator-mode no-data-source case (`ZOTERO_SQLITE_PATH=/nonexistent --orchestrator-mode
  true`) now exits 1, prints a visible `[zotero:auto] Error:` message to stderr, and writes NO
  output file at all (previously wrote a silent `ITEMS='[]'` to a real file).

**Key Decisions Made**:
- Orchestrator else-branch hardening removed the `ITEMS='[]'` / `SOURCE="none-orchestrator-default"`
  / `write_output` call entirely rather than special-casing it after `write_output` -- the branch
  now `exit 1`s before reaching any write logic, symmetric with the non-orchestrator
  `manual_fallback_text; exit 1` sibling branch.
- Exit code doc table (script header) updated so exit code 1 now explicitly documents covering
  both orchestrator and non-orchestrator "no data source" cases.

**Deviations from Plan**: None. All planned doc-comment locations (paths references in the
header, ENVIRONMENT section, OPTIONS section, manual_fallback_text heredoc) were updated for
consistency with the resolver-driven behavior; the plan only explicitly named lines 24, 60, and
197, but all `~/Zotero/zotero.sqlite`-hardcoded mentions in doc comments were updated for
consistency with the new resolution order.

**What NOT to Try**: N/A — both phases completed cleanly and verified.

**References**:
- Plan: `specs/798_literature_zotero_datadir_and_retry_fixes/plans/01_zotero-datadir-retry-fixes.md`
- Current phase: 3 of 5 (completed); Phase 2 also completed
- Edited: `.claude/extensions/literature/scripts/zotero-export-status.sh` +
  `.claude/scripts/zotero-export-status.sh` (flat)
- Edited: `.claude/extensions/literature/scripts/zotero-generate-export.sh` +
  `.claude/scripts/zotero-generate-export.sh` (flat)
