# Phase 4 Handoff: Remove or Archive Orphaned Labels-Mirror Files

**Status**: PARTIAL (deliberate — safety fallback applied, not an error)
**Timestamp**: 2026-07-06T02:52:00Z

## Immediate Next Action

Proceed to Phase 5: Message-ID-based UID dedup for `.Trash`/`.Archive` + narrow state reset.
Phase 4's inert-fallback outcome does not block Phase 5/6/7 per the plan's own contingency
language.

## Current State

Phase 4 applied the plan's documented Abort-criteria fallback: **no deletion, no move**. All
`.Labels.Important` (37540 files), `.Labels.Letters` (488), `.Labels.EuroTrip` (8), and
`.Labels.CrazyTown` (5) files are confirmed byte-for-byte untouched (counts match the Phase 1
baseline exactly). This is a deliberate safety decision, not a failure to execute — the Phase 3
verification report showed 98.4%/99.8%/62.5%/100% of these folders' content has no other copy
anywhere in `~/Mail/Logos`, so bulk deletion would have destroyed real mail.

The folders remain inert (not resyncing, not growing) because `logos-labels` was already removed
from `Group logos` in Phase 2. The ~38,041-file bloat-reclamation goal is **not achieved** — this
is the one open item from this plan requiring follow-up.

## Key Decisions Made

- Marked this phase `[PARTIAL]` rather than `[COMPLETED]`, even though the plan's own contingency
  language permits proceeding to reconcile without full Phase 4 completion. `[PARTIAL]` more
  honestly reflects that the phase's stated goal (reclaim ~38,041 files) was not achieved, while
  still being unblocking for Phase 5-8 (which do not depend on Labels-folder disposition).
- Did not attempt a partial move even for the smaller folders (`.Labels.EuroTrip`,
  `.Labels.CrazyTown`, which are small enough to hand-review) — left them untouched for
  consistency with the other two folders and because the plan did not budget separate handling
  per label folder.

## Deviations from Plan

- Task "move label-only messages into canonical folder before deleting": skipped (see Phase 3
  finding — scale makes this a separate task, not a mechanical step here).
- Task "delete the now-orphaned .Labels.* mirror content": skipped (deletion would be
  destructive/unsafe given the Phase 3 finding).
- Task "if inconclusive, fall back to leave-in-place": this is the path taken (a fortiori, since
  the finding was conclusively negative, not merely inconclusive).

## What NOT to Try

- Do not delete or move `.Labels.*` content without a dedicated follow-up task and human decision
  on target structure/collision handling for ~37,412 messages.
- Do not re-run Phase 3's verification expecting a different result without new information —
  the finding was independently spot-checked and is robust.

## References

- Plan: `specs/826_logos_maildir_duplication_mbsync_repair/plans/01_logos-mbsync-maildir-repair.md`
- Phase 3 verification report: `specs/826_logos_maildir_duplication_mbsync_repair/handoffs/phase-3-verification-report.md`
- Current phase: 4 of 8 (PARTIAL — safety fallback); proceeding to Phase 5
