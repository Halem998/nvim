# Phase 1 Handoff: Freeze and Back Up

**Status**: COMPLETED
**Timestamp**: 2026-07-06T02:26:00Z

## Immediate Next Action

Begin Phase 2: apply the `mbsync.nix` config fix in `~/.dotfiles/modules/home/email/mbsync.nix`
(remove `logos-labels` from `Group logos`, keep the channel definition), then rebuild home-manager.

## Current State

Phase 1 completed. `email-freeze` confirmed no mbsync/isync process running and wrote its own
Gmail-scoped backup tarball to `~/Mail/.syncstate-backups/`. Additionally, a Logos-specific backup
was created (email-freeze does not cover `~/Mail/Logos`):

- `~/Mail/.logos-backup-20260706/mbsyncstate/` — 28 `.mbsyncstate*`/`.uidvalidity` files, relative
  paths preserved (covers root, `.Archive`, `.Drafts`, `.Trash`, `.Sent` incl. `.journal`/`.new`,
  and all 8 `.Labels.*` folders).
- `~/Mail/.logos-backup-20260706/tarballs/` — 7 per-folder tarballs: `.Trash.tar.gz` (58M, 1951
  entries), `.Archive.tar.gz` (1.7M, 60 entries), `.Sent.tar.gz` (2.4M, 20 entries),
  `.Labels.Important.tar.gz` (4.6G, 37546 entries), `.Labels.Letters.tar.gz` (73M, 494 entries),
  `.Labels.EuroTrip.tar.gz` (52K, 14 entries), `.Labels.CrazyTown.tar.gz` (140K, 11 entries). All
  verified readable via `tar -tzf`.
- `~/Mail/.logos-backup-20260706/baseline-counts.txt` — per-folder `cur`/`new` file counts;
  TOTAL: 43660 (matches research report baseline exactly).

Plan file phase 1 checklist fully checked off with inline completion notes.

## Key Decisions Made

- Ran `email-freeze` as instructed by the plan, but recognized its backup scope is Gmail-only
  (`MAIL_ROOT="$HOME/Mail/Gmail"` in `~/.dotfiles/modules/home/email/mbsync.nix`), so it does not
  satisfy the Logos-specific backup requirement on its own. Performed a separate, independent
  Logos backup (state files + per-folder tarballs + baseline counts) to satisfy Phase 1's actual
  requirements before any Phase 2+ mutation.
- Ran the large `.Labels.Important` tarball (37,540 files) via `run_in_background` after the
  foreground command's 2-minute timeout truncated a first attempt; the truncated tarball was
  deleted and redone cleanly in the background, then verified with `tar -tzf`.

## Deviations from Plan

- None. All Phase 1 checklist items completed as specified (with the email-freeze scope caveat
  noted above, which does not change the outcome — the Logos backup is complete regardless).

## What NOT to Try

- Do not rely on `email-freeze`'s own tarball for Logos rollback — it only covers `~/Mail/Gmail`.
  Use `~/Mail/.logos-backup-20260706/` for any Logos restore.
- Do not run a foreground `tar` over `.Labels.Important` with a short timeout — it takes several
  minutes for 37,546 entries; use `run_in_background` or a generous timeout.

## References

- Plan: `specs/826_logos_maildir_duplication_mbsync_repair/plans/01_logos-mbsync-maildir-repair.md`
- Current phase: 1 of 8 (completed)
- Backup dir: `~/Mail/.logos-backup-20260706/`
