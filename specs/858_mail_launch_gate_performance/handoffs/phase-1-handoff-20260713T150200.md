# Phase 1 Handoff

- **Immediate Next Action**: Phase 2 -- add `sync_in_flight` guard + `background_sync(loud)`, wire it into the `<leader>me` success branch and `<leader>mN`, delete `sync_all_mail`/`run_notmuch_new`, update module header comment.
- **Current State**: Phase 1 completed. Plan file is current. `mail.lua` loads (`dofile` returns table); `<leader>me` gates only on `reconcile_index` (`notmuch new --no-hooks`); `census_freshness_ok` deleted; `sync_all_mail` retained solely for `<leader>mN`.
- **Key Decisions Made**: Launch-barrier comment reworded to avoid the literal word "census" so the Phase 1 grep verification (`grep "census\|email-census"` returns nothing) passes while preserving the decoupling rationale by content.
- **Deviations from Plan**: None (wording adjustment noted inline on the checklist item).
- **What NOT to Try**: Do not reference mbsync exit codes or any freshness checker in the launch path; do not delete `run_notmuch_new` before Phase 2 (sync_all_mail still calls it).
- **References**: specs/858_mail_launch_gate_performance/plans/02_fast-responsive-aerc-launch.md, Phase 1 -> Phase 2.
