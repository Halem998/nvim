#!/usr/bin/env bash
# test-lake-build-guard.sh - Placeholder stub for lake-build-guard.sh's regression suite.
#
# This stub exists ONLY so that the manifest.json registration added alongside it never
# references a nonexistent file at any commit boundary (check-extension-docs.sh flags manifest
# entries referencing nonexistent files). The real 13-case suite is authored in a later phase of
# the same task; this file is replaced in full at that point, not extended.

set -uo pipefail

echo "[SKIP] test-lake-build-guard.sh: placeholder stub, real suite not yet authored"
exit 0
